# 遥控启动
本文档说明如何使用仓库根目录下的 `start_r2_teleop.sh` 一键启动 R2 遥控链路。

脚本会同时启动以下 5 部分：

- `rc26_merge_odom` 底盘执行链路
- `rc26_mechanism` 机构执行链路
- `joy/joy_node` 手柄驱动
- `rc26_telecontrol` 或 `rc26_telecontrol_dpad` 遥控节点
- `rc26_telecontrol_front_track_test` 前置履带按钮测试节点

默认遥控输出话题为 `cmd_vel`。

当前运行时已经删除 `terrain_speed_limit` 全链路；无论测试 `stick` 还是 `dpad`，teleop 都不会受到 terrain 限速影响。

## 使用前提

- 当前工作目录位于仓库根目录：`RC_2026`
- 已完成编译，并存在 `install/setup.bash`
- 手柄已连接，默认设备名为 `Xbox 360 Controller`

## 常用命令

### 1. 默认启动

```bash
./start_r2_teleop.sh
```

说明：

- 使用 `dpad` 模式
- 当前脚本默认按 `tracked_diff` 启动履带底盘
- 同时启动 `rc26_mechanism mechanism.launch.py hal_type:=shared_serial`
- 脚本会自动执行 `/mechanism_server` 的 `configure` 和 `activate`
- 默认不启动 EKF，因此此命令不会额外打开“融合位姿模式”
- 十字键上下 `axes[7]` 控制前后
- `X(button[2])` / `B(button[1])` 控制旋转
- 十字键左右 `axes[6]` 只有在 `mecanum_4wheel` 模式下才控制横移
- `Y(button[3])` 触发抬升前置履带
- `A(button[0])` 触发放下前置履带
- 左摇杆 `axes[0]/axes[1]`、右摇杆 `axes[3]/axes[4]` 在默认 dpad 遥控节点里不参与速度输出
- 线速度上限默认 `0.2 m/s`
- 角速度上限默认 `0.5 rad/s`

### 2. 启用融合位姿并使用 IMU

```bash
./start_r2_teleop.sh --pose-mode imu
```

说明：

- 会自动打开 `merge_odom.launch.py` 的 EKF
- 最终 `merge_odom` 会继续融合 `DM_IMU`
- 适合需要当前默认融合位姿口径的联调场景

### 3. 启用融合位姿但不使用 IMU

```bash
./start_r2_teleop.sh --pose-mode no-imu
```

说明：

- 会自动打开 `merge_odom.launch.py` 的 EKF
- 最终 `merge_odom` 只融合 wheel/CAN 里程计，不再把 `DM_IMU` 作为 EKF 输入
- 这个模式只影响 EKF 的最终融合位姿，`dm_imu_node`、PoseSender 和底层保护逻辑仍然保留 IMU

### 4. 十字模式

```bash
./start_r2_teleop.sh --mode dpad
```

说明：

- 使用 `dpad` 模式
- 当前脚本默认按 `tracked_diff` 启动履带底盘
- 十字键上下 `axes[7]` 控制前后
- 十字键左右 `axes[6]` 只有在 `mecanum_4wheel` 模式下才控制横移
- `X(button[2])` / `B(button[1])` 控制旋转
- `Y(button[3])` 触发 `/mechanism/execute` 的前置履带抬升
- `A(button[0])` 触发 `/mechanism/execute` 的前置履带放下
- `select`、`start`、`mode` 当前未被 teleop 消费
- 适合需要离散方向控制的场景

### 5. 调整速度

```bash
./start_r2_teleop.sh --mode stick --v-linear 0.4 --v-angular 0.8
```

说明：

- 若显式切到 `stick`，速度控制会回到摇杆模式
- `--v-linear 0.4` 将线速度上限设置为 `0.4 m/s`
- `--v-angular 0.8` 将角速度上限设置为 `0.8 rad/s`
- 可按实际测试情况逐步增减，不建议一开始设置过大

### 6. 最小直连 MCU 遥控链

```bash
./start_r2_mcu_teleop.sh --mode dpad
```

说明：

- 只启动 `pose_sender_node`、`joy_node` 和 `rc26_telecontrol`，不拉起 `rc26_merge_odom` 总装节点，也不拉起 `rc26_mechanism`
- 适合只验证目标串口下发和手柄速度控制的最小链路
- 如果反馈串口不存在、与目标串口相同，或显式传 `--feedback-serial-port __disabled__`，脚本会自动禁用反馈串口，只保留目标串口下发

### 7. 查看所有参数

```bash
./start_r2_teleop.sh --help
```

说明：

- 查看脚本支持的全部参数
- 包括模式切换、速度限制、死区、看门狗、急停保持等选项

## 补充说明

- 脚本启动后，`merge_odom`、`mechanism_server`、`joy_node`、teleop 节点和前置履带测试节点会在同一个终端输出日志
- 按 `Ctrl+C` 会同时停止整条遥控链路
- 当前运行时已删除 `terrain_speed_limit` 上下游链路，手动遥控不会再被地形限速抑制
- 脚本会显式给 `rc26_merge_odom` 和 `rc26_telecontrol` 传 `chassis_model:=tracked_diff`，不再依赖各包内部默认值
- 脚本会显式给 `rc26_mechanism` 传 `hal_type:=shared_serial`，避免和 `rc26_merge_odom` 竞争打开同一个目标串口
- 脚本会在拉起 `mechanism_server` 后自动做一次 lifecycle `configure` 和 `activate`
- `--pose-mode imu|no-imu` 会自动把 `start_ekf:=true` 传给 `merge_odom.launch.py`
- 兼容旧用法：单独传 `--start-ekf` 时，行为仍保持为“启动 EKF 且 EKF 使用 IMU”
- 当前默认手柄设备名是 `Xbox 360 Controller`
- 如需切换输出话题，可使用：

```bash
./start_r2_teleop.sh --cmd-vel-topic cmd_vel
```

- 如需直接手写 `ros2 launch rc26_merge_odom merge_odom.launch.py` 开启“融合位姿但不使用 IMU”，请使用：

```bash
ros2 launch rc26_merge_odom merge_odom.launch.py start_ekf:=true use_imu_for_ekf:=false
```
