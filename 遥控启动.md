# 遥控启动
本文档说明如何使用仓库根目录下的 `start_r2_teleop.sh` 一键启动 R2 遥控链路。

脚本会同时启动以下 3 部分：

- `rc26_merge_odom` 底盘执行链路
- `joy/joy_node` 手柄驱动
- `rc26_telecontrol` 或 `rc26_telecontrol_dpad` 遥控节点

默认遥控输出话题为 `cmd_vel`。

为避免手动测试时被外部 `terrain_speed_limit` 误抑制，`start_r2_teleop.sh` 默认会关闭 `PoseSender` 的地形限速输入。
这意味着无论测试 `stick` 还是 `dpad`，线速度都不会再被地形限速话题自动压成 `0`。

## 使用前提

- 当前工作目录位于仓库根目录：`RC_2026`
- 已完成编译，并存在 `install/setup.bash`
- 手柄已连接，默认设备名为 `Xbox 360 Controller`

## 常用命令

### 1. 默认启动

```bash
./start_r2_teleop.sh
```

说明：

- 使用 `stick` 模式
- 左摇杆控制前后/横移
- 右摇杆控制旋转
- 默认关闭 `terrain_speed_limit` 对线速度的抑制，适合手动遥控联调
- 线速度上限默认 `0.2 m/s`
- 角速度上限默认 `0.5 rad/s`

### 2. 十字模式

```bash
./start_r2_teleop.sh --mode dpad
```

说明：

- 使用 `dpad` 模式
- 十字键控制前后/横移
- `X` 右旋，`B` 左旋
- 默认关闭 `terrain_speed_limit` 对线速度的抑制，适合十字键联调
- 适合需要离散方向控制的场景

### 3. 调整速度

```bash
./start_r2_teleop.sh --mode stick --v-linear 0.4 --v-angular 0.8
```

说明：

- `--v-linear 0.4` 将线速度上限设置为 `0.4 m/s`
- `--v-angular 0.8` 将角速度上限设置为 `0.8 rad/s`
- 可按实际测试情况逐步增减，不建议一开始设置过大

### 4. 查看所有参数

```bash
./start_r2_teleop.sh --help
```

说明：

- 查看脚本支持的全部参数
- 包括模式切换、速度限制、死区、看门狗、急停保持等选项
- 包括是否重新启用 `terrain_speed_limit` 输入

## 补充说明

- 脚本启动后，3 个进程会在同一个终端输出日志
- 按 `Ctrl+C` 会同时停止整条遥控链路
- 脚本默认禁用 `terrain_speed_limit_topic`，避免手动遥控时线速度被外部 terrain 节点压成 `0`
- 如需切换输出话题，可使用：

```bash
./start_r2_teleop.sh --cmd-vel-topic cmd_vel
```

- 如需恢复 terrain 限速联动，可使用：

```bash
./start_r2_teleop.sh --enable-terrain-speed-limit
```

- 如需指定其他 terrain 限速话题，可使用：

```bash
./start_r2_teleop.sh --terrain-speed-limit-topic your_speed_limit_topic
```

- 如需直接手写 `ros2 launch rc26_merge_odom merge_odom.launch.py`，禁用 terrain 限速时请使用：

```bash
ros2 launch rc26_merge_odom merge_odom.launch.py terrain_speed_limit_topic:=__disabled__
```
