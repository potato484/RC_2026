# 遥控启动

```bash
./start_r2_teleop.sh
```
默认全链遥控；`dpad + tracked_diff`，不开 EKF。

```bash
./start_r2_teleop.sh --pose-mode imu
```
全链遥控，启动 EKF，并把 `DM_IMU` 融入最终 `merge_odom`。

```bash
./start_r2_teleop.sh --pose-mode no-imu
```
全链遥控，启动 EKF，但最终融合位姿不使用 IMU；`dm_imu_node` 和执行保护链仍保留 IMU。

```bash
./start_r2_teleop.sh --pose-mode wheel-only
```
全链遥控，启动 EKF，只用 `wheel_odom` 做最终融合位姿，且完全不读取 IMU。

```bash
./start_r2_teleop.sh --mode stick --v-linear 0.4 --v-angular 0.8
```
切到摇杆速度控制，并把线速度、角速度上限改到 `0.4 / 0.8`。

```bash
./start_r2_teleop.sh --stack minimal-mcu
```
最小串口遥控链，只启动 `pose_sender_node + joy_node + telecontrol`。

```bash
./start_r2_mcu_teleop.sh
```
兼容旧入口；等价于 `./start_r2_teleop.sh --stack minimal-mcu`。

```bash
./start_r2_teleop.sh --help
./start_r2_mcu_teleop.sh --help
```
查看统一入口和兼容入口的参数说明。
