# Robocon 2026 定位链路执行方案评分表

> **评估目标**：验证执行方案是否在 4 分钟比赛节奏下，严格达成了「禁踩 KFS 0 违规」、「不规划死锁」、「可证明实时性」及「可控降级」。
> **计分规则**：满分 100 分。**所有 P0 级（标红）条目为一票否决项（Blocker）**，若任一 P0 项得分为 0 或未完成闭环验证，总方案不予通过。

## 第一部分：物理属性与传感器边界（总分 30 分）

| 评分项与对应等级 | 考核点及代码/参数落位要求 | 量化验收标准 (得分依据) | 得分 |
| --- | --- | --- | --- |
| **1.1 EMA 跳变与台阶门控**<br>

<br>🔴 **[P0]** (10分) | `terrain_semantic_node.cpp`：需修改 `jump_thresh_m`，引入台阶工况门控（不冻结）与姿态急停补偿，设定冻结上限。 | **10分**：200mm 台阶过渡 `ground_z_filtered` ≤300ms 收敛（误差≤50mm）；冻结帧数≤3；急停无长期冻结。<br>

<br>**0分**：存在永久冻结死锁或未闭环。 | /10 |
| **1.2 BFS 去噪与稀疏边界**<br>

<br>🔴 **[P0]** (10分) | 根据 profile 动态切换 `min_obstacle_area_cells` 和邻域模式，MF 工况必须允许单格保留。 | **10分**：斜向接近台阶单格保留率≥90%；正常模式反光单格噪点渗入率≤5%。<br>

<br>**5分**：参数可切但无自动化验证。<br>

<br>**0分**：未实现按工况切换。 | /10 |
| **1.3 近场盲区与强反光**<br>

<br>🟡 [P1] (5分) | 0.6m 近场动态放宽感知阈值；为 `top_z` 引入基于 `ground_z` 的最大差值上限保护。 | **5分**：0.2~1.0m 近场无大面积伪障碍闪烁；`top_z` 无超限异常尖峰（>ground+0.7m）。<br>

<br>**0分**：未抑制近场噪点。 | /5 |
| **1.4 对角盲区兜底**<br>

<br>🟡 [P1] (5分) | MF profile 启用 360° 跌落观测、保守 unknown 策略及 edge8；决策侧增加防切角约束。 | **5分**：防切角逻辑生效，路线不穿越高差角点。<br>

<br>**0分**：存在切角进入禁区风险。 | /5 |

## 第二部分：竞赛逻辑与空间几何（总分 30 分）

| 评分项与对应等级 | 考核点及代码/参数落位要求 | 量化验收标准 (得分依据) | 得分 |
| --- | --- | --- | --- |
| **2.1 Keepout 几何与可达性**<br>

<br>🟡 [P1] (10分) | `map_resolution` 对齐 costmap(0.1m)；掩码从圆形膨胀改为方块外接几何 + 合理 margin。 | **10分**：KFS 顶面稳定禁行；紧邻 KFS 空块合法站位规划成功率 100%（不贴边）。<br>

<br>**5分**：修改了膨胀半径但未改形状。<br>

<br>**0分**：存在相邻空块不可达死锁。 | /10 |
| **2.2 概率释放死锁修复**<br>

<br>🔴 **[P0]** (10分) | `kfs_block_fuser.cpp`：修复永不释放 Bug（修改衰减目标/引入滞回带），引入 `ttl_sec` 兜底及拾取强制释放。 | **10分**：强制释放 P95≤0.5s；无新命中时在 TTL 内自动释放，无永久幽灵禁区。<br>

<br>**0分**：死锁逻辑仍存在。 | /10 |
| **2.3 感知溯源与冷启动**<br>

<br>🔴 **[P0]** (5分) | 明确 `merlin_map` 的初始化数据源，修补 `decision_node` 中 `/mf_kfs_state` 冷启动空置问题。 | **5分**：启动时 `merlin_map` 成功获取并发布 KFS 网格初始状态。<br>

<br>**0分**：依赖手动或存在冷启动盲区。 | /5 |
| **2.4 标定与队伍防错**<br>

<br>🟡 [P1] (5分) | `mf_grid_layout.yaml` 增加 team/version 强校验；启动时执行 1.2m 中心距自检。 | **5分**：占位布局或 team 错配自动触发停机/保守模式。<br>

<br>**0分**：错配仍可强制运行。 | /5 |

## 第三部分：混合系统架构与时序鲁棒（总分 25 分）

| 评分项与对应等级 | 考核点及代码/参数落位要求 | 量化验收标准 (得分依据) | 得分 |
| --- | --- | --- | --- |
| **3.1 启动时序与放行门控**<br>

<br>🔴 **[P0]** (10分) | `bringup.launch.py` 顺序重构（Keepout 先于 Nav2）；决策侧增加 `INIT_WAIT` 掩码 age < 300ms 放行门控。 | **10分**：彻底消除“导航启动但禁区未就绪”窗口；超时自动 fallback。<br>

<br>**0分**：时序依然倒置或无决策门控。 | /10 |
| **3.2 动态更新与端到端延迟**<br>

<br>🟡 [P1] (10分) | KFS 状态发布压至 5Hz (200ms)；优化 QoS。 | **10分**：状态变化到掩码发布 P95≤100ms；掩码到 costmap 生效 P95≤300ms。<br>

<br>**5分**：仅提升了发布频率未验证生效时间。 | /10 |
| **3.3 IPC 抖动与参数确认**<br>

<br>🔴 **[P0]** (5分) | `terrain_mode_adapter.cpp`：`set_parameters` 增加重试机制（至少2次）及回读校验；失败触发降级。 | **5分**：100 次参数切换成功率≥99%；连续失败可正确触发 safe 或停机动作。<br>

<br>**0分**：仍存在超时静默失败风险。 | /5 |

## 第四部分：热边界与高负载（总分 15 分）

| 评分项与对应等级 | 考核点及代码/参数落位要求 | 量化验收标准 (得分依据) | 得分 |
| --- | --- | --- | --- |
| **4.1 热降频轻量 Fallback**<br>

<br>🟢 [P2] (10分) | 定义并提供 CPU 降载情况下的低配参数（voxel_leaf, radius, costmap Hz 降低）。 | **10分**：4分钟降频压测不崩溃；Fallback 模式下 terrain 延迟 P95≤22ms 且禁区/跌落功能存活。<br>

<br>**0分**：未提供降级配置。 | /10 |
| **4.2 延迟超限硬干预**<br>

<br>🟢 [P2] (5分) | 将 `latency_error_ms > 20ms` 从仅打日志升级为动作触发器（限速/停机/虚拟围栏）。 | **5分**：超限连续 N 帧在 ≤350ms 内成功触发干预动作。<br>

<br>**0分**：仅有 diagnostics 日志无动作闭环。 | /5 |

---

## 验收结论区

* **P0 项通过情况 (必须全部通过)**：[ ] 是 / [ ] 否
* **总分**：___ / 100
* **测试基准**：R2 (QCS8550) + Livox Mid-360, 4分钟全负载压测跑圈日志。
* **审查结论**：[ ] 通过准入 / [ ] 需返工修复并提供验证日志 / [ ] 拒绝合并

---
