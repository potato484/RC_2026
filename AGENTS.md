# AGENTS.md

本文件面向在本仓库内协作的 AI 代理与开发者。

当前仓库以根目录 `README.md` 作为唯一公开说明入口，以 `src/` 作为 R2 自动机器人 ROS 2 运行时工作区。仓库不再维护 `docs/` 约束目录，也不再维护各 ROS 2 包内的独立 README。

## 核心准则

### 1. 以根 README 和真实代码为准

- 建立项目上下文时，先阅读根目录 `README.md`，再进入 `src/`、脚本、配置和接口文件核对真实实现。
- 根目录 `README.md` 用于承载项目总览、运行链路、构建方式、关键接口、验证方式和维护说明。
- 如果 README 与当前代码或真实接口冲突，以当前代码行为和真实接口为准，并在改动中同步修正根 README。

### 2. 不再依赖 docs 目录

- 仓库不再把 `docs/` 作为事实源、约束源或提交前必读入口。
- 新增或修改功能时，不要新建 `docs/` 文档树来承载约束；必要说明优先更新根 README，细节说明优先放在代码注释、配置注释、接口定义或测试中。
- 删除 `docs/` 后，任何脚本、CI、README、注释或贡献说明都不应继续要求先读或回写 `docs/`。

### 3. 不再维护包级 README

- `src/` 下各 ROS 2 包不再维护独立 `README.md`。
- 包职责、运行入口、接口、验证命令和注意事项统一收敛到根 README 或对应源码、launch、配置、IDL、测试文件中。
- 如果后续需要新增面向用户的说明，优先更新根 README；如果只是实现细节，优先写在最接近实现的位置。

### 4. 修改代码时保持现有边界

- `src/` 仍是 R2 自动机器人的主运行时 ROS 2 工作区。
- 修改时优先延续现有包职责、launch 装配方式、topic/service 契约、参数结构和运行路径。
- 涉及职责边界、依赖方向、运行时权威归属或接口语义的变更，应在交付说明中明确变更原因、影响范围、兼容策略和验证方式，并同步更新根 README 中对应事实。

### 5. 验证规则

- 工作环境按 Ubuntu 22.04 + ROS 2 Humble 口径维护；涉及 Python 命令统一使用 `python3`。
- 编译验证统一使用：

```bash
MAKEFLAGS='-j2 -l2' colcon build --symlink-install --executor sequential --parallel-workers 1 --packages-select <pkg...>
```

- 需要提速时优先小幅调高 `MAKEFLAGS`，不要直接提高 `--parallel-workers`。
- 仓库级 CI 用于覆盖可复现的编译和非实机测试；实机 launch、传感器链、串口、底盘和机构动作仍必须在 R2 硬件上受控验收。

### 6. Git 提交信息格式

- 提交标题统一使用 `(<type>):<简短说明>`，例如 `(feat):添加了一个特性`。
- 常用 `type` 包括：`feat` 新功能，`fix` 修复，`docs` 文档，`test` 测试，`ci` CI，`refactor` 重构，`chore` 工程维护。
- 提交正文必须说明 `why`、`what`、`test` 三项；如果某项不适用，也要写明原因。
- `why` 说明为什么要改，包含问题背景、评审要求、实机风险或维护动机。
- `what` 说明具体改了什么，包含涉及的包、接口、配置、文档或脚本。
- `test` 说明验证了什么，包含执行过的构建、测试、dry-run、CI 或无法验证的原因。

推荐格式：

```text
(feat):添加了一个特性

why: 因为需要补齐某个比赛流程或评审要求。
what: 修改了相关节点、配置和 README 说明。
test: 运行了 colcon test --packages-select <pkg...>，或说明当前环境无法验证。
```

## RC_2026 特定事实

- 当前项目仅为 R2 自动机器人设计，R1 只是手动机器人。
- `src/` 是 R2 自动机器人的主运行时工作区；仓库内不维护第一方前端实现。
- R2 当前算力板卡按犀牛派 X1 口径维护，运行环境支持 AidLux（Android 13 + Ubuntu 22.04）深度融合。
- R2 当前运行时统一为麦克纳姆全向底盘。当前里程计与动态 TF 以 Point-LIO 和 `rc26_odom_interface` 为准，底盘速度下发由 `rc26_mcu_transport` 负责，`rc26_telecontrol` 用于人为遥控测试 R2 机器人。

## 默认执行顺序

1. 先读根目录 `README.md`，建立项目总体上下文。
2. 再读相关 `src/` 包源码、launch、配置、IDL 和测试，核对当前真实实现。
3. 在现有结构内做最小必要修改。
4. 执行受影响包的构建和测试验证；无法在当前环境验证时，在交付说明中明确原因。
5. 若改动改变用户可见行为、运行入口、接口或验证方式，同步更新根 README。

一句话要求：

> 当前仓库只保留根 README 作为公开说明入口；做实际修改时以根 README、源码和可验证结果为准，不再依赖或回写 `docs/` 与包级 README。
