# 默认地图配置文件 (占位)
# 请将此文件替换为实际地图，或在启动时指定 map:=<your_map.yaml>
#
# 地图文件格式要求:
#   image: <path_to_pgm>
#   resolution: 0.05
#   origin: [0.0, 0.0, 0.0]
#   negate: 0
#   occupied_thresh: 0.65
#   free_thresh: 0.196

image: default.pgm
resolution: 0.1
origin: [-0.14, -0.3, 0.0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.196
