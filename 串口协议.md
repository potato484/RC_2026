# RC_2026 串口协议

## 1. 协议 A：主控 <-> MCU（`rc26_serial`）

### 1.1 给 MCU 工程师先看这 1 页

这份协议当前最关键的对接口径只有下面几条：

- 串口参数：`UART / 1000000 / 8N1 / 无流控`
- 帧格式：`AA 55 | SEQ | LEN | RETRY | CMD | PAYLOAD | CRC32 | 55 AA`
- 校验算法：`CRC32/MPEG-2`
- 字节序：整条协议按`小端`处理，`float` 也是 `float32 little-endian`
- `payload` 最大长度：`32` 字节
- 只有`下行命令 ID`做了重排，`上行反馈 ID`保持原值不变
- 旧导航下行命令已经删除，不再保留：
  - `NAV_NORMAL`
  - `NAV_STAIR_UP`
  - `NAV_STAIR_DOWN`
- 当前下行 `0x00 ~ 0x02` 已经占用：
  - `0x00 = STOP`
  - `0x01 = GRAB_TIP`
  - `0x02 = ASSEMBLE_WEAPON`
- 以下 4 个下行命令是`连续命令`，`不需要 ACK`，统一按 `50Hz` 下发：
  - `FRONT_TRACK_UP = 0x0E`
  - `FRONT_TRACK_DOWN = 0x0F`
  - `POSE_FEEDBACK = 0x1E`
  - `POSE_TARGET = 0x1F`
- 其余下行命令默认都按`可靠命令`处理，需要 MCU 用`同一个 SEQ`回：
  - `ACK`
  - 或 `NACK`
  - 或 `ACTION_FAIL`
  - 或 `ERROR`
- 当前 R2 是履带底盘，`ODOM_DATA = 0x20` 默认按 `8` 字节上报：

```text
<float v_left><float v_right>
```

- 老的四轮 `16` 字节格式仍兼容，但只有双方明确约定时才用
- 前置履带控制现在不再走 `/mechanism/execute` 兼容路径，已经改成`遥控按钮 -> transport -> serial` 直接连续下发；对 MCU 来说，就是连续接收 `0x0E / 0x0F`

如果只想先把 MCU 跑通，优先实现这 4 件事：

1. 正确收发帧头帧尾、`LEN`、`CRC32`
2. 对可靠命令按原 `SEQ` 回 `ACK/NACK`
3. 对 `POSE_FEEDBACK/POSE_TARGET/FRONT_TRACK_UP/FRONT_TRACK_DOWN` 按`无 ACK、最新值生效`处理
4. 周期上报 `ODOM_DATA(0x20)`，默认格式为 `<v_left, v_right>`

### 1.2 链路参数

- 物理链路：UART
- 波特率：`1000000`
- 串口格式：`8N1`
- 流控：无硬件流控，无软件流控
- 帧头：`AA 55`
- 帧尾：`55 AA`
- 校验：`CRC32/MPEG-2`
- 字节序：小端
- 最大 `payload`：`32` 字节

### 1.3 帧格式

```text
AA 55 | SEQ | LEN | RETRY | CMD | PAYLOAD(N) | CRC32(4B, 小端落线) | 55 AA
```

字段说明：

- `SEQ`：序列号，`uint8`
- `LEN`：`CMD(1B) + PAYLOAD(NB)`，即 `LEN = 1 + N`
- `RETRY`：
  - 可靠命令：主控重发时从 `0x00` 递增到 `0x09`
  - 连续命令：固定 `0x00`
- `CMD`：命令 ID 或反馈 ID
- `PAYLOAD`：业务数据
- `CRC32`：覆盖 `SEQ + LEN + RETRY + CMD + PAYLOAD`

长度关系：

- 最小帧长：`12` 字节
- 总帧长：`11 + LEN`

CRC32 口径：

- 多项式：`0x04C11DB7`
- 初值：`0xFFFFFFFF`
- 末尾不异或
- 结果按`小端`落线：低字节先发

### 1.4 下行命令表（主控 -> MCU）

| 命令 | ID | payload | MCU 是否立即回 ACK | 说明 |
| --- | ---: | --- | --- | --- |
| `STOP` | `0x00` | 空 | 是 | 急停，可靠命令 |
| `GRAB_TIP` | `0x01` | `[tip_index]` | 是 | 抓取端头 |
| `ASSEMBLE_WEAPON` | `0x02` | 空 | 是 | 组装兵器 |
| `ROTATE_POS_90` | `0x03` | 空 | 是 | 旋转 +90° |
| `ROTATE_NEG_90` | `0x04` | 空 | 是 | 旋转 -90° |
| `ROTATE_POS_180` | `0x05` | 空 | 是 | 旋转 +180° |
| `ROTATE_NEG_180` | `0x06` | 空 | 是 | 旋转 -180° |
| `GRAB_KFS` | `0x07` | 空 | 是 | 梅林区夹取 KFS |
| `MECH_UP_MERLIN` | `0x08` | 空 | 是 | 梅林区机构抬升 |
| `MECH_DOWN_MERLIN` | `0x09` | 空 | 是 | 梅林区机构下降 |
| `MECH_UP_DUEL` | `0x0A` | 空 | 是 | 对抗区机构抬升 |
| `PLACE_KFS_GRID` | `0x0B` | `[grid_position, layer]` | 是 | 对抗区放置到九宫格 |
| `PLACE_KFS_GROUND` | `0x0C` | 空 | 是 | 对抗区放置到地面 |
| `HEARTBEAT` | `0x0D` | 空 | 是 | 心跳，回 `ACK` 或 `HEARTBEAT_ACK` 都可以 |
| `FRONT_TRACK_UP` | `0x0E` | 空 | 否 | 连续命令，`50Hz`，抬升前置履带 |
| `FRONT_TRACK_DOWN` | `0x0F` | 空 | 否 | 连续命令，`50Hz`，放下前置履带 |
| `POSE_FEEDBACK` | `0x1E` | `<float vx><float vy><float wz>` | 否 | 连续命令，`50Hz`，反馈速度 |
| `POSE_TARGET` | `0x1F` | `<float vx><float vy><float wz>` | 否 | 连续命令，`50Hz`，目标速度 |

说明：

- 上表中“是否立即回 ACK”只表示`是否要立刻回 ACK 类反馈`
- 对长动作，`ACK`只代表“这条命令已收下并接受执行”，不代表动作已经做完
- 动作真正完成后，MCU 可以再发对应的 `*_DONE`

### 1.5 上行反馈表（MCU -> 主控）

#### 1.5.1 必须实现的通用反馈

| 反馈 | ID | payload | 发送时机 |
| --- | ---: | --- | --- |
| `ACK` | `0x00` | 空 | 可靠命令收下后立即回；`SEQ` 必须与被确认命令一致 |
| `NACK` | `0x01` | 空 | 拒收当前可靠命令；`SEQ` 必须与被拒绝命令一致 |
| `HEARTBEAT_ACK` | `0x10` | 空 | 心跳响应；`SEQ` 必须与 `HEARTBEAT(0x0D)` 一致 |
| `ODOM_DATA` | `0x20` | 见下文 | 周期上报轮速 |
| `ACTION_FAIL` | `0xFE` | 建议 `[failed_cmd_id, error_code]` | 动作执行失败；如果用于终止当前可靠命令，`SEQ` 必须与该命令一致 |
| `ERROR` | `0xFF` | 可空或自定义 | 严重错误；如果用于终止当前可靠命令，`SEQ` 必须与该命令一致 |

`ACTION_FAIL` 的容错口径：

- `2` 字节：`[failed_cmd_id, error_code]`
- `1` 字节：只有 `failed_cmd_id`
- `0` 字节：视为全局失败广播

#### 1.5.2 动作完成反馈

| 反馈 | ID | 说明 |
| --- | ---: | --- |
| `GRAB_TIP_DONE` | `0x02` | 抓取端头完成 |
| `ASSEMBLE_DONE` | `0x03` | 组装完成 |
| `CLIMBING_SLOPE` | `0x04` | 正在爬坡 |
| `SLOPE_DONE` | `0x05` | 爬坡完成 |
| `ROTATE_POS_90_DONE` | `0x06` | +90° 完成 |
| `ROTATE_NEG_90_DONE` | `0x07` | -90° 完成 |
| `ROTATE_POS_180_DONE` | `0x08` | +180° 完成 |
| `ROTATE_NEG_180_DONE` | `0x09` | -180° 完成 |
| `MECH_UP_MERLIN_DONE` | `0x0A` | 梅林区机构抬升完成 |
| `MECH_DOWN_MERLIN_DONE` | `0x0B` | 梅林区机构下降完成 |
| `GRAB_KFS_DONE` | `0x0C` | 夹取 KFS 完成 |
| `MECH_UP_DUEL_DONE` | `0x0D` | 对抗区机构抬升完成 |
| `PLACE_KFS_GRID_DONE` | `0x0E` | 九宫格放置完成 |
| `PLACE_KFS_GROUND_DONE` | `0x0F` | 地面放置完成 |
| `STAIR_CLIMB_DONE` | `0x11` | 上阶梯完成 |
| `STAIR_DESCEND_DONE` | `0x12` | 下阶梯完成 |
| `FRONT_TRACK_UP_DONE` | `0x13` | 抬升前置履带完成 |
| `FRONT_TRACK_DOWN_DONE` | `0x14` | 放下前置履带完成 |

说明：

- `*_DONE` 是`动作结果反馈`，不是 ACK
- 当前主控仍保留这些反馈编号的接收能力
- 由于导航下行命令已经删除，`STAIR_CLIMB_DONE / STAIR_DESCEND_DONE` 当前属于`仅上行保留反馈`

### 1.6 关键 payload 定义

#### 1.6.1 `POSE_FEEDBACK` / `POSE_TARGET`

固定 `12` 字节：

```text
<float vx><float vy><float wz>
```

- `float32` 小端
- 当前统一下发频率：`50Hz`
- 当前履带底盘默认仍保留 `vy` 字段
- 即使上层把 `vy` 压成 `0`，协议格式也不变

#### 1.6.2 `ODOM_DATA`

当前默认使用履带格式 `8` 字节：

```text
<float v_left><float v_right>
```

- `float32` 小端
- 单位：`m/s`
- 这是当前 R2 实车默认口径

兼容老四轮格式 `16` 字节：

```text
<float v_fl><float v_rl><float v_rr><float v_fr>
```

如果 MCU 要按四轮格式发，必须提前和主控明确对齐；否则默认按履带 `8B` 处理。

#### 1.6.3 机构动作命令常见 payload

- `GRAB_TIP (0x01)`：`[tip_index]`
- `PLACE_KFS_GRID (0x0B)`：`[grid_position, layer]`
- 其他当前常用动作命令默认都是空 `payload`

### 1.7 ACK、重发与连续命令规则

#### 1.7.1 可靠命令

除以下 4 个连续命令外，其余下行命令都按可靠命令处理：

- `FRONT_TRACK_UP`
- `FRONT_TRACK_DOWN`
- `POSE_FEEDBACK`
- `POSE_TARGET`

可靠命令规则：

- 主控为这条命令只分配一次 `SEQ`
- 若需要重发，`RETRY` 从 `0x00` 递增到 `0x09`
- 重发时 `SEQ` 不变，只改变 `RETRY`
- 如果收到“相同 `SEQ` + 相同 `CMD` + 更大的 `RETRY`”，说明这是同一条命令的重发，不应当被当成新动作重复执行
- MCU 应在收到合法帧后，尽快回一条`同 SEQ`的反馈：
  - 成功接收：`ACK`
  - 拒绝执行：`NACK`
  - 收下后执行失败：`ACTION_FAIL`
  - 严重异常：`ERROR`

对 MCU 最重要的一句：

- `ACK/NACK/HEARTBEAT_ACK/ACTION_FAIL/ERROR` 这几类用来结束主控等待的反馈，`SEQ` 必须回原值

#### 1.7.2 心跳命令

- 心跳命令号：`HEARTBEAT = 0x0D`
- 空 `payload`
- 主控只发一次，`RETRY = 0x00`
- MCU 回 `ACK` 或 `HEARTBEAT_ACK` 都可以
- 推荐直接回 `HEARTBEAT_ACK`

#### 1.7.3 连续命令

下面 4 个命令统一按`无 ACK 连续流`处理：

- `FRONT_TRACK_UP = 0x0E`
- `FRONT_TRACK_DOWN = 0x0F`
- `POSE_FEEDBACK = 0x1E`
- `POSE_TARGET = 0x1F`

连续命令规则：

- 下发频率统一：`50Hz`
- `RETRY` 固定 `0x00`
- MCU 不需要回 `ACK`
- MCU 应按“最新一帧生效”处理，不要把它当成离散一次性命令
- 允许主控连续发送相同命令，MCU 侧应能接受重复帧

特别说明：

- `FRONT_TRACK_UP / FRONT_TRACK_DOWN` 当前已经不保留 `/mechanism/execute` 兼容路径
- 现在的真实链路就是：`遥控按钮 -> transport -> 串口 -> MCU`
- 所以这两个命令就是`按住期间持续下发，松开后停止`

### 1.8 当前主控到串口的控制链路

这一节只写 MCU 对接需要知道的真实链路，不展开 ROS 细节。

#### 1.8.1 速度链路

目标速度链路：

```text
遥控器 / 上层导航速度 -> 主控速度保护 -> POSE_TARGET(0x1F) -> 串口 -> MCU
```

反馈速度链路：

```text
主控反馈速度估计 -> POSE_FEEDBACK(0x1E) -> 串口 -> MCU
```

当前口径：

- `POSE_TARGET` 与 `POSE_FEEDBACK` 都是 `(vx, vy, wz)`
- 都是 `50Hz`
- 都是无 ACK 连续下发

#### 1.8.2 前置履带链路

```text
遥控器前置履带按钮 -> transport 命令 -> FRONT_TRACK_UP/DOWN -> 串口 -> MCU
```

当前口径：

- 按住上升按钮：连续下发 `FRONT_TRACK_UP(0x0E)`
- 按住下降按钮：连续下发 `FRONT_TRACK_DOWN(0x0F)`
- 下发频率：`50Hz`
- 不等待 ACK
- 动作真正完成后，可再回：
  - `FRONT_TRACK_UP_DONE(0x13)`
  - `FRONT_TRACK_DOWN_DONE(0x14)`

#### 1.8.3 机构动作链路

```text
主控机构动作 -> 可靠命令 -> MCU 立即 ACK -> MCU 执行动作 -> MCU 再发 *_DONE 或 ACTION_FAIL
```

这类命令包括：

- `STOP`
- `GRAB_TIP`
- `ASSEMBLE_WEAPON`
- `ROTATE_*`
- `GRAB_KFS`
- `MECH_*`
- `PLACE_KFS_*`

### 1.9 收发示例

#### 1.9.1 可靠命令示例：`STOP`

主控发送：

```text
AA 55 | 2A | 01 | 00 | 00 | CRC32 | 55 AA
```

解释：

- `SEQ = 0x2A`
- `LEN = 0x01`，因为只有 `CMD`
- `RETRY = 0x00`
- `CMD = 0x00 (STOP)`

MCU 正常接收后立即回复：

```text
AA 55 | 2A | 01 | 00 | 00 | CRC32 | 55 AA
```

这里回复帧的 `CMD = 0x00`，表示 `ACK`，并且 `SEQ` 必须仍是 `0x2A`。

#### 1.9.2 连续命令示例：`POSE_TARGET`

```text
AA 55 | 31 | 0D | 00 | 1F | <vx><vy><wz> | CRC32 | 55 AA
```

解释：

- `SEQ = 0x31`
- `LEN = 0x0D = 1 + 12`
- `RETRY = 0x00`
- `CMD = 0x1F (POSE_TARGET)`
- `payload = 3 个 float32 小端`

这个命令是 `50Hz` 连续下发，MCU 不需要回复 `ACK`。

#### 1.9.3 连续命令示例：`FRONT_TRACK_UP`

主控在按钮按住期间会反复发送：

```text
AA 55 | 40 | 01 | 00 | 0E | CRC32 | 55 AA
AA 55 | 41 | 01 | 00 | 0E | CRC32 | 55 AA
AA 55 | 42 | 01 | 00 | 0E | CRC32 | 55 AA
...
```

说明：

- 每帧 `SEQ` 会继续递增
- `CMD = 0x0E (FRONT_TRACK_UP)`
- 不需要 `ACK`
- 推荐 MCU 直接按“保持上升状态”处理

动作完成后，如果需要通知主控，可以发：

```text
AA 55 | xx | 01 | 00 | 13 | CRC32 | 55 AA
```

其中 `0x13 = FRONT_TRACK_UP_DONE`。

### 1.10 MCU 对接清单

对接前按这份清单核一遍，基本就不会偏：

1. 下行命令编号以本文件为准，`0x00 ~ 0x02` 已占用，不再给旧导航命令留空位
2. 上行反馈编号未重排，仍按当前表格实现
3. `ACK/NACK/HEARTBEAT_ACK/ACTION_FAIL/ERROR` 若用于响应当前可靠命令，必须回原 `SEQ`
4. `POSE_FEEDBACK / POSE_TARGET / FRONT_TRACK_UP / FRONT_TRACK_DOWN` 一律按 `50Hz` 无 ACK 连续流处理
5. 当前默认 `ODOM_DATA` 格式是 `<float v_left><float v_right>` 共 `8B`
6. 整条协议按小端处理，`float` 也按 `float32 little-endian`
7. `CRC32` 用 `MPEG-2` 口径，不要换成 `IEEE` 或别的变体
8. 前置履带不再走 `/mechanism/execute`，只需要对接 `0x0E / 0x0F / 0x13 / 0x14`

---

## 2. 协议 B：达妙 IMU（`dm_imu`）

### 2.1 链路参数

- 物理链路：USB CDC 串口
- 默认波特率：`921600`
- 串口格式：`8N1`
- 帧头：`55 AA`
- 帧尾：`0A`
- 帧长：固定 `19` 字节
- 校验：`CRC16`
- 字节序：小端

### 2.2 帧格式

```text
55 AA | ID | RID | DATA(12B) | CRC16(2B, 小端) | 0A
```

字段说明：

- `ID`：当前驱动不使用
- `RID`：数据类型 ID
- `DATA`：3 个 `float32` 小端
- `CRC16`：当前代码对前 `16` 个字节做校验，再和线上的两字节小端 CRC 对比

当前实现口径：

- CRC 初值：`0xFFFF`
- 查表：`CRC16_TABLE[256]`
- 具体算法以 `dm_crc16()` 为准

### 2.3 RID 定义

当前驱动只接受 `0x01..0x03`：

| RID | 含义 | 单位 |
| ---: | --- | --- |
| `0x01` | 加速度 `accel[x,y,z]` | `m/s²` |
| `0x02` | 角速度 `gyro[x,y,z]` | `°/s` |
| `0x03` | 欧拉角 `euler[roll,pitch,yaw]` | `°` |

注意：

- 当前实现没有 `0x04` 四元数解析分支
- `RID` 不在 `0x01..0x03` 范围内的帧会被直接丢弃

### 2.4 驱动接收与缓存行为

`DmImuDriver` 当前实现特点：

- 接收线程用 `select()`，超时 `10ms`
- 接收缓存 `256` 字节
- 按流扫描帧头 `55 AA`

重同步策略：

- 找不到帧头：只保留最后 1 个字节，`frames_no_header++`
- 找到帧头但长度不够 `19B`：返回等待更多数据，`frames_short++`
- 帧尾不是 `0A`：跳过当前帧头后继续找
- CRC 错：`frames_crc_error++`

成功解析后：

- 按 `RID` 更新 `latest_data_` 中对应的三元组
- 分别更新时间戳：
  - `accel_ts`
  - `gyro_ts`
  - `euler_ts`
- 对应的 `*_valid` 标志置 `true`

统计项：

- `frames_ok`
- `frames_crc_error`
- `frames_short`
- `frames_no_header`

### 2.5 `dm_imu_node` 发布语义

#### 2.5.1 话题与 QoS

- 发布话题：`DM_IMU`
- 消息类型：`sensor_msgs/msg/Imu`
- QoS depth：`50`
- 默认 QoS：`reliable`
- 可通过参数切成 `best_effort`

#### 2.5.2 发布条件

节点不是收到一帧就立刻发布，而是：

- 用 `5ms` 定时器轮询 `driver_.getLatestData()`
- 在至少收到过一帧有效欧拉角之前，不发布任何 `Imu`
- 一旦 `euler_valid=true`，只要 `accel/gyro/euler` 三者里有任一时间戳比上次发布时更新，就发布一次

因此当前真实语义是：

- “必须先有姿态”
- “之后允许使用最新缓存值组合成一帧 `Imu` 发布”

#### 2.5.3 字段填充

- `orientation`
  - 使用最新缓存的欧拉角
  - 角度先从度转弧度
  - 再转四元数
  - 最后归一化

- `angular_velocity`
  - 使用陀螺仪数据
  - 从 `°/s` 转成 `rad/s`

- `linear_acceleration`
  - 直接使用驱动缓存的加速度值
  - 当前代码假定其已经是 `m/s²`

#### 2.5.4 Hampel 滤波

当 `hampel_enable=true` 时，节点当前只对三个分量做 Hampel 去尖峰：

- `linear_acceleration.x`
- `linear_acceleration.y`
- `angular_velocity.z`

不会对以下量做 Hampel：

- `linear_acceleration.z`
- `angular_velocity.x`
- `angular_velocity.y`
- `orientation`

#### 2.5.5 协方差口径

当前节点写死以下协方差：

- `orientation_covariance` 对角：`0.02`
- `angular_velocity_covariance` 对角：`0.01`
- `linear_acceleration_covariance` 对角：`0.1`

如果对应数据当前无效：

- 该字段的 covariance 全部填 `-1`
