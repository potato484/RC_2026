# 导航启动

本文档对应 R2 当前唯一有效的导航链路，核心模块为 `rc26_topo_nav + rc26_nav_mode_manager + rc26_omni_controller`。

## 使用前提

- 已完成编译，并存在 `install/setup.bash`
- `map -> odom` TF 正常
- `/control_state` 正常输出
- 已准备好先验点云文件
- 定位、地形和 keepout 链路已稳定

## 编译

```bash
colcon build --symlink-install --parallel-workers 3 \
  --packages-select rc26_interfaces rc26_topo_nav rc26_nav_mode_manager rc26_omni_controller rc26_bringup \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

## 推荐启动方式

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=false \
  use_decision:=false \
  visualization_backend:=rviz \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

说明：

- 该命令会带起定位、地形、topo_nav、xhu_motion_mode_manager 和 xhu_motion_follower
- 若只想看 Web 端，可将 `visualization_backend:=rviz` 改为 `local_web`

## 最小验收

```bash
ros2 topic echo /xhu_nav/motion_mode_state
ros2 topic echo /xhu_nav/tracking_state
ros2 topic echo /xhu_nav/semantic_gate
ros2 topic echo /cmd_vel
```

预期：

- `/xhu_nav/motion_mode_state` 能反映当前运动模式
- `/xhu_nav/tracking_state` 会持续给出执行状态
- `/xhu_nav/semantic_gate` 能体现定位/地形/执行门控结果
- `/cmd_vel` 会随 corridor 执行更新

## 手动切模式

```bash
ros2 service call /set_xhu_motion_mode rc26_interfaces/srv/SetXhuMotionMode \
  "{mode: 'plane_move', timeout: 0.0, reason: 'manual_check'}"

ros2 service call /set_xhu_motion_mode rc26_interfaces/srv/SetXhuMotionMode \
  "{mode: 'mf_traverse', timeout: 10.0, reason: 'watchdog_check'}"
```
