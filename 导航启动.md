# 导航启动
本文档对应 R2 的导航链路，核心模块为 `Nav2 + rc26_nav_mode_manager + rc26_omni_controller`。
导航启动前，先确保重定位已经稳定，否则后续控制和模式管理没有可靠基线。

## 使用前提

- 已完成编译，并存在 `install/setup.bash`
- `map -> odom` TF 正常
- `/odometry` 正常输出
- 已准备好先验点云文件
- 若要联动避障限速，`/terrain_obstacles` 与相关感知链路应已正常

## 编译

```bash
colcon build --symlink-install --parallel-workers 3 \
  --packages-select rc26_omni_controller rc26_nav_mode_manager rc26_bringup \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

## 常用启动方式

### 1. 启动完整导航栈，推荐

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=false \
  use_decision:=false \
  visualization_backend:=rviz \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

说明：

- 本命令会带起定位、Nav2 和控制器链路
- 这里保持 `use_decision:=false`，先单独验证导航模块
- 如果只想用 Web 端，把 `visualization_backend:=rviz` 改为 `foxglove`

### 2. 单独测试控制器

```bash
ros2 launch rc26_bringup test_omni_controller.launch.py
```

### 3. 单独测试导航模式管理器

```bash
ros2 launch rc26_nav_mode_manager nav_mode_manager.launch.py
```

## 最小验收

```bash
ros2 lifecycle get /controller_server
ros2 topic echo /nav_safety_state
ros2 topic echo /compute_time_ms
ros2 topic echo /collision_d_min
ros2 topic echo /v_safe
ros2 topic echo /cmd_vel
```

预期：

- `controller_server` 生命周期状态为 `active`
- `/nav_safety_state` 能反映当前导航档位
- `/compute_time_ms` 稳定，P95 目标不高于 `2ms`
- `/cmd_vel` 会随规划和控制器输出变化

## 常用模式切换命令

```bash
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'normal', timeout: 0.0, reason: 'test'}"
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'mf_traverse', timeout: 10.0, reason: 'test_watchdog'}"
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'stair_up', timeout: 0.0, reason: 'test_stair'}"
```

## 常用在线调参

```bash
ros2 param set /controller_server FollowPath.brake_margin 0.15
ros2 param set /controller_server FollowPath.lateral_error_gain 1.5
ros2 param set /controller_server FollowPath.a_lim_x 1.0
ros2 param set /controller_server FollowPath.a_lim_y 0.6
```
