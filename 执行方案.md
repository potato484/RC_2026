# rc26_xhu_viewer RViz2 定制化可视化方案与阶段性落地状态

## 文档说明

- 本文件同时记录 `rc26_xhu_viewer` 的终态设计目标与当前阶段性落地状态。
- 第 1~5 节描述终态目标、边界和验收口径，不代表 2026-04-07 已全部实现。
- 第 6~8 节描述当前真实实现、已完成的代码级验收和后续阶段拆分；若与旧实现或旧文档冲突，以当前代码和这些状态节为准。

## 摘要

- 终态目标仍然是“可视化域单包、车端优先、原生 RViz2 为主、可选进程内嵌 Web 为辅”的 `rc26_xhu_viewer`。
- 当前已完成的第一阶段落地是：新增 `src/rc26_xhu_viewer/rc26_xhu_viewer` 运行时包，把 bringup 默认可视化后端切到 `rc26_xhu_viewer`，并固化 `operator / engineering / diagnostic` 三套 RViz preset。
- 当前仍保留 `rc26_visualization`，但它已退回到“`r2/diag/*` 诊断聚合 + 迁移期 Web 工具链”定位，不再是 bringup 默认可视化后端。
- 当前尚未完成的关键能力包括：专用 Display/Panel 白名单、进程内嵌 Web、2 小时本地历史库、全量中文 UI、各源模块深度 trace 专用面板。

## 1. 整体架构设计

- 架构采用三层：数据聚合层、可视化渲染层、UI 交互层。
- 数据聚合层统一订阅 ROS2 运行态、诊断、BT、地形、KFS、定位、LIO、视觉、机构、里程计与控制接口；同时读取上游静态场景真源并固化为 viewer 自己的显示真源。
- 可视化渲染层以精简后的 RViz2 核心为底座，只保留最小渲染内核、白名单 Display/Panel 机制、必要视角控制与 RC26 专用显示对象。
- UI 交互层承担布局预设、图层管理、对象钻取、时间轴、历史回放、事件联动、工程操作台与中文界面，不直接面向原始 ROS2 消息。
- 数据路径固定为：源模块 topic/service/action 与静态场景真源进入源适配器，进入统一 viewer model，最后分发给原生 Display、原生 Panel 和可选嵌入式 Web 面板。
- 深度观测严格由源模块各自发布，命名保持在各自模块命名域，不统一迁入 `/r2/trace/*`，也不挂到 viewer 私有命名域。
- viewer 内建本地历史库，支持约 2 小时窗口的事件、状态、趋势与 trace 查询；长时间更深历史不要求在首期依赖外部录制才能浏览。
- 静态场景口径定为：规划和导航真源仍在原模块；`rc26_xhu_viewer` 持有统一的“显示真源”，包括显示所需几何固化副本、语义区、布局预设、显示目录和中文显示元数据。

## 2. 轻量化、减重与部署策略

- bringup 的可视化后端终态只保留 `rc26_xhu_viewer` 与 `none` 两种口径，不再长期保留通用 RViz 或独立本地 Web 后端。
- RViz2 仅保留主渲染窗口、变换管理、配置装载、专用 DisplayFactory、专用 Panel 容器、必要基础渲染原语与少量 RC26 视角控制。
- 删除所有通用型、与车端值守无关或会把产品重新拉回“通用调试器”的能力，包括通用插件浏览、任意添加显示/工具、帮助面板、通用截图向导、无关默认插件、测试框架和演示性渲染资产。
- 部署目标以车端为第一优先级，工程机只作为辅助观测与回归环境，不反过来主导架构。
- 性能目标固定为：车端操作态最低稳定 20 FPS，全信息诊断态最低稳定 15 FPS。
- 当前方案选择“信息最大化”而不是“稳态优先”，因此诊断态允许更高图层并发和更深 trace，但必须通过分层降载维持上述最低帧率。
- 可选 Web 面板必须进程内嵌，且不能引入明显破坏轻量化目标的重型依赖；若资源紧张，系统自动禁用 Web 面板，优先保证主三维、主面板和工程操作台流畅。
- 包内自带中文字体与 3D 标签字体资源，系统字体只作为补充回退，避免 AidLux 或 Ubuntu 环境差异导致乱码和显示不一致。

## 3. 可视化模块与呈现策略

- 定位模块提供主位姿、历史轨迹、协方差、健康度、退化分数、Hessian 指标、后端状态、关键帧、闭环、anchor、路线可观测性与重定位过程的专用显示与专用面板。
- 点云配准全过程首期必须补齐只读深度接口，展示原始点云、下采样点云、局部地图、最终对齐结果、迭代收敛曲线、残差变化、代表性对应关系与失败原因；不再仅停留在健康度摘要。
- LIO 模块提供退化分数、里程计协方差、点云累计地图、残差趋势与估计状态的专用入口，不再只作为上游黑盒。
- 导航模块同时显示全局 route、surface route、corridor、lookahead、局部候选轨迹、选中轨迹、恢复动作、速度剖面、模式状态、跟踪误差、停止原因与动态风险来源。
- 动态障碍预测首期纳入方案，不再作为“后续增强”；对应深度观测由导航相关源模块发布，viewer 负责 3D 预测包络、未来轨迹、时序条带与策略原因展示。
- Terrain 与 Merlin KFS 同时提供贴地图层和专用面板，显示 `FREE / BLOCKED / UNKNOWN / SLOW`、占据概率、drop 风险、climbable 概率、动态膨胀解释层和语义摘要。
- BT 提供三类视图：场地区域高亮视图、树结构与运行路径视图、时间线与事件流视图；BT 时间线、长列表和复杂趋势图允许使用进程内嵌 Web 面板，其余主交互仍走原生。
- 机构模块提供动作状态、通信健康、RTT、重连、错误码、命令耗时和动作历史的专用面板，并与诊断、BT、导航事件联动。
- 视觉、telecontrol、serial 等当前数据较稀疏的模块，首期仍按要求补齐专用面板和专用观测入口，不采用“只给摘要”的收缩策略。
- 整个 viewer 采用工作区布局，而不是让用户自行拼显示树。至少固化操作态、工程态、诊断态三套预设，并用布局切换同时控制订阅深度、图层显隐和渲染质量。

## 4. 中文化、操作台与扩展机制

- 界面 100% 中文化，范围包括菜单、状态栏、Display 名称、Panel 名称、告警、提示、时间轴说明、表格列名、工具栏、布局名称和插件名称。
- BT、黑板键、事件说明优先消费行为树本地化数据，不在 viewer 内部维护与运行时脱节的手工中文副本。
- `rc26_xhu_viewer` 被明确定义为工程操作台，而不再是只读观察屏。
- 操作权限采用“分级门禁 + 二次确认”策略：比赛常用操作可以直达，危险操作必须进入工程/调试页，并经过状态检查、门禁和确认流，不扩展成无门槛的全调试总控。
- 扩展机制需要服从轻量化约束、统一布局体系和中文术语体系。
- 新增深度观测接口的公开变化属于正式契约变更，需同步更新中间层契约文档；viewer 本地推导层只可作为解释层，不能冒充上游算法真源。

## 5. 公共接口变化与终态验收口径

- 对外保留并延续现有聚合诊断接口族，`r2/diag/*` 继续作为稳定公共接口存在，不因包迁移而消失。
- 新增公共接口集中在各源模块自己的深度观测 topic/service/action 家族，而不是新增一个 viewer 专属对外协议层。
- bringup 对外终态只暴露 `rc26_xhu_viewer` 或 `none` 两种可视化后端选择。
- 静态显示真源迁入 `rc26_xhu_viewer` 后，需明确区分“上游规划真源”和“viewer 显示真源”，避免双向写回和真源漂移。
- 首期完整验收必须覆盖：车端启动时间、三套布局切换、操作态与诊断态帧率、Web 面板自动降级、中文无乱码、`r2/diag/*` 兼容性、全运行时模块的专用入口可见性、各深度 trace 的源模块发布链路、工程操作台门禁与确认流程、2 小时本地历史查询能力。
- 回归重点包括：定位 trace 与真实定位状态一致、导航预测与局部候选联动一致、BT 时间线与 subtree 高亮一致、KFS/terrain 概率层与离散格状态一致、机构事件与诊断告警联动一致。

## 6. 当前阶段性落地状态（2026-04-07）

### 已完成

- 在 `src/rc26_xhu_viewer/` 下保留 upstream RViz vendor 包的同时，新增了运行时包 `src/rc26_xhu_viewer/rc26_xhu_viewer/`。
- 新运行时包已经提供：
  - `src/main.cpp`：自定义入口，默认按 `mode/layout` 选择 preset。
  - `launch/viewer.launch.py`：独立 viewer 启动入口。
  - `config/navigation_*.rviz`：导航模式下的 `operator / engineering / diagnostic` 三套 preset。
  - `config/slam_*.rviz`：建图模式下的 `operator / engineering / diagnostic` 三套 preset。
  - `scripts/validate_viewer_configs.py`：代码级配置验收脚本。
- `rc26_bringup/launch/bringup.launch.py` 已把默认可视化后端切到 `visualization_backend:=rc26_xhu_viewer`。
- bringup 已新增 `visualization_layout:=operator|engineering|diagnostic`，显式控制三套 preset。
- `visualization_backend:=rviz` 仅保留为兼容别名，实际仍会转发到 `rc26_xhu_viewer`。
- `rc26_visualization` 未移除；它继续承担 `r2/diag/*` 聚合，并保留 `viewer/ + visualization_server.py` 作为迁移期本地 Web 工具链。
- 启动文档和后端/前端 README 已同步到新的真实口径。

### 当前真实边界

- 当前状态还不是“可视化域单包”，而是 `rc26_xhu_viewer + rc26_visualization` 并存。
- 当前 `rc26_xhu_viewer` 主要完成的是“运行时入口 + preset 固化 + bringup 接入”，还没有实现专用 RC26 Display/Panel 白名单体系。
- 当前没有落地进程内嵌 Web，也没有落地 2 小时本地历史库。
- 当前 `rc26_visualization/viewer` 仍然可以单独用于本地 Web 联调，但不再作为 bringup 默认可视化后端。
- 当前 `r2/diag/*` 仍由 `rc26_visualization` 发布，尚未迁入 `rc26_xhu_viewer`。

### 当前阶段判定

- 本次落地应视为 **M1：运行时入口切换与 preset 基线建立** 已完成。
- 终态方案里更高层的 UI、Panel、Web、历史库和深度 trace 集成，仍属于后续阶段。

## 7. 本次代码级验收记录（2026-04-07）

### 构建验收

执行：

```bash
MAKEFLAGS='-j2 -l2' colcon build \
  --executor sequential \
  --parallel-workers 1 \
  --packages-ignore rviz_assimp_vendor rviz_ogre_vendor rviz_rendering rviz_common rviz_default_plugins rviz_rendering_tests rviz_visual_testing_framework \
  --packages-select rc26_xhu_viewer rc26_visualization rc26_bringup
```

补充：

```bash
MAKEFLAGS='-j2 -l2' colcon build \
  --executor sequential \
  --parallel-workers 1 \
  --packages-select rc26_base_ground
```

结果：

- `rc26_xhu_viewer`、`rc26_visualization`、`rc26_bringup` 构建通过。
- 由于工作区里保留了未参与本次验收的 upstream RViz vendor 包，构建时显式 `--packages-ignore` 这些 vendor 包，直接复用 `/opt/ros/humble` 中已安装的 RViz 依赖。
- `rc26_base_ground` 补建后，`bringup.launch.py` 的参数解析也能完整通过。

### 配置与 launch 验收

执行：

```bash
python3 -m py_compile \
  src/rc26_bringup/launch/bringup.launch.py \
  src/rc26_xhu_viewer/rc26_xhu_viewer/launch/viewer.launch.py \
  src/rc26_xhu_viewer/rc26_xhu_viewer/scripts/validate_viewer_configs.py

source /opt/ros/humble/setup.bash
source /home/potato/RC_2026/install/setup.bash
/home/potato/RC_2026/install/rc26_xhu_viewer/lib/rc26_xhu_viewer/validate_viewer_configs.py
ros2 launch rc26_xhu_viewer viewer.launch.py --show-args
ros2 launch rc26_bringup bringup.launch.py --show-args
```

结果：

- Python 语法检查通过。
- `validate_viewer_configs.py` 通过，6 份 preset 均已安装且包含关键 topic / fixed frame / display 项。
- `viewer.launch.py --show-args` 通过，确认对外暴露 `namespace / use_sim_time / slam / layout`。
- `bringup.launch.py --show-args` 通过，确认对外暴露 `visualization_backend:=rc26_xhu_viewer|none` 与 `visualization_layout:=operator|engineering|diagnostic`。

### 当前未完成的验收项

- 还未完成真实图形界面启动下的 FPS 验收。
- 还未完成中文字体、AidLux 环境、Web 自动降级和 2 小时历史库相关验收。
- 还未完成全运行时核心包专用 Panel/Display 可见性验收，因为这些能力尚未实现。

## 8. 后续阶段拆分

### M2：专用显示与操作台骨架

- 引入 RC26 专用 Display/Panel 白名单。
- 把三套 preset 从“topic 组合”提升为“专用工作区布局”。
- 开始接入中文菜单、工具栏、状态栏与工程操作台门禁。

### M3：深度观测入口补齐

- 为定位、导航、LIO、terrain、KFS、BT、机构、视觉、telecontrol、serial 等模块补齐专用显示入口。
- 明确各深度 trace 的源模块发布链路，并同步更新中间层契约文档。

### M4：进程内嵌 Web 与历史库

- 落地 BT 时间线、长列表和复杂趋势图的进程内嵌 Web 面板。
- 引入本地历史库，满足约 2 小时窗口的事件、状态、趋势与 trace 查询。
- 完成资源紧张下的 Web 自动降级策略。

### M5：终态收口

- 评估并推进 `rc26_visualization` 的进一步收口或拆分，避免长期形成“双 viewer 真相”。
- 统一字体、中文术语、资源路径、安装与发布口径。
- 完成完整首期验收与回归验收。
