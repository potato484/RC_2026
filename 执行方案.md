
  # RViz2 全量中文化定稿方案

  ## Summary

  - 不再保留产品层 assumptions，当前边界已经锁定。
  - 中文化验收范围是 src/rc26_xhu_viewer 可控的全部内建 RViz 页面，以及 RC26 已出厂 preset 会进入的可见页面。
  - 技术标识采用“中文主显 + 原值提示”策略：主界面显示中文，悬浮提示、帮助区或说明文本保留原始 topic/frame/枚举值，内部保存和
    运行仍使用原始英文值。
  - 未收录到词典的未知英文不做机翻；但在本次验收范围内，词典必须补齐到“看不到英文”为止。
  - 用户或外部配置里的自由文本命名不强制改写；只翻内建名称和词典内标准标识。

  ## Key Changes

  - 在 src/rc26_xhu_viewer/rviz2/resources/terminology.yaml 扩展成统一中文术语真源，至少覆盖：
      - 面板、菜单、工具栏、状态栏、对话框、帮助页文案
      - 内建 display/tool/view/panel 名称与描述
      - 属性树字段名、枚举值、常见状态文本
      - RC26 默认链路里的 topic/frame/技术缩写 中文别名
  - rviz_common 增加统一显示映射层。
      - Property 左列名称和右列字符串值统一走中文显示映射。
      - EnumProperty、EditableEnumProperty、RosTopicProperty、TfFrameProperty 的编辑器显示中文，提交时反向回写原始英文值。
      - dock/panel 标题改成“内部稳定 key + 可见中文标题”分离，不能再用英文窗口标题直接充当保存恢复 key。
  - rviz_common 全部内建壳层页面做中文化，覆盖：
      - Displays、Selection、Tool Properties、Views、Time、Transformation、Help 面板
      - Add Display 对话框、截图/保存失败/帮助缺失/About 等弹窗
      - classic 默认配置 src/rc26_xhu_viewer/rviz_common/default.rviz 的默认面板标题、默认 display 名称、默认 view 名称
  - rviz_default_plugins 全部内建可见文本做中文化，覆盖：
      - 内建 display/tool/view 的名称、描述、属性名、枚举项、状态栏提示、错误/警告文案
      - 当前仓库默认会暴露的常用项：Grid、TF、RobotModel、Path、PointCloud2、PointStamped、MarkerArray、Map、Odometry、
        PoseWithCovariance、Move Camera、Select、Focus Camera、Measure、Initial Pose、2D Goal、Publish Point、Orbit、XY
        Orbit、First Person、Top Down Orthographic、Third Person Follower
  - RC26 资产同步补齐并收口到 rviz2。
      - 六份 .rviz preset 中直接展示给用户的名称全部改成中文。
      - RViz2Rc26ViewerFrame、可选 RC26 plugins 和已有中文壳层统一切到同一术语源，不再保留散落硬编码英文。
  - 文档同步更新。
      - 更新 docs/backend/archive/rviz2/README.md，明确 classic 模式功能保持原生，但界面文本已经中文化；保存配置仍保留英文键
        值。
      - 更新 docs/backend/archive/rc26_xhu_viewer/README.md，明确退役目录不再承担 staging 职责，真实术语源已经切到 rviz2。

  ## Acceptance Rules

  - RC26 六份 preset、classic 默认布局、菜单、工具栏、属性树、Add Display、Help、About、截图/保存相关页面，验收时不允许出现
    英文可见文本。
  - 所有内建工具、内建视角、内建面板进入后的状态提示与属性页，验收时不允许出现英文可见文本。
  - topic/frame/技术缩写 在主界面中只显示中文；原始英文值只允许出现在提示层或说明层，不允许继续占据主显示位。
  - 自定义自由文本不改写；若其内容恰好等于内建标准词条，不自动强制覆盖用户命名。
  - 对不在本仓库内建能力和默认 RC26 流程中的外部插件，不承诺一次性穷尽；但凡默认验收路径里实际可见到的外部插件属性名，如果经
    过 Property 系统显示，必须补入词典并中文化。

  ## Test Plan

        rviz_default_plugins rviz2
      - 为 rviz_common 增加 round-trip 测试，确认属性显示中文但 .rviz 读写键和值仍是原始英文。
      - 覆盖 RosTopicProperty、TfFrameProperty、EnumProperty、panel 标题恢复逻辑的“中文显示 / 英文保存”测试。
      - 执行 validate_viewer_configs.py，确认 RC26 六份 preset 仍可正常加载。
  - 手工验收：
      1. rviz2 --rc26-mode navigation --rc26-layout operator
      2. rviz2 --rc26-mode navigation --rc26-layout engineering
      3. rviz2 --rc26-mode navigation --rc26-layout diagnostic
      4. rviz2 --rc26-mode slam --rc26-layout operator
      5. rviz2 --rc26-mode slam --rc26-layout engineering
      6. rviz2 --rc26-mode slam --rc26-layout diagnostic
      7. rviz2 --classic
  - 每个手工场景至少检查：
      - 菜单/工具栏/面板标题/属性树/状态栏无英文
      - Add Display 和 Help/About 无英文
      - 修改 frame/topic/工具参数后保存再重载，行为不变，保存文件仍是英文原值

  ## Locked Defaults

  - 显示策略：中文主显，原值进入提示层。
  - 词典策略：默认验收范围内必须补齐，不对未知项做运行时机翻。
  - 自定义命名策略：不强制改写自由文本。
  - 兼容策略：不修改 ROS 契约，不修改 .rviz 序列化协议，只改 GUI 显示层和编辑层。
