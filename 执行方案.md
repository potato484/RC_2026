# rc26_xhu_viewer RViz2 定制化可视化方案与阶段性落地状态

## 文档说明

- 本文件同时记录 `rc26_xhu_viewer` 的终态设计目标与当前阶段性落地状态。
- 第 1~5 节描述终态目标、边界和验收口径，不代表全部实现。
- 第 6~8 节描述当前真实实现、已完成的代码级验收和后续阶段拆分；若与旧实现或旧文档冲突，以当前代码和这些状态节为准。

## 摘要

- 终态目标是"可视化域单包、车端优先、原生 RViz2 为主、可选进程内嵌 Web 为辅"的 `rc26_xhu_viewer`。
- M1（运行时入口切换）已完成。M2（白名单 + 中文菜单 + 门禁面板 + preset 分组）已完成。M3（深度观测消息定义 + 18 个 Display/Panel 骨架 + 契约文档）已完成。M4（Web 面板基础设施 + 本地历史库 + HTML 页面）已完成。M5（收口评估 + 术语统一 + 文档回写）已完成。
- `rc26_xhu_viewer` 已完成对原 `rc26_visualization` 的硬切并入；状态聚合、本地 Web adapter 和前端都统一收口到 `rc26_xhu_viewer`。

## 1. 整体架构设计

- 架构采用三层：数据聚合层、可视化渲染层、UI 交互层。
- 数据聚合层统一订阅 ROS2 运行态、诊断、BT、地形、KFS、定位、LIO、视觉、机构、里程计与控制接口；同时读取上游静态场景真源并固化为 viewer 自己的显示真源。
- 可视化渲染层以精简后的 RViz2 核心为底座，只保留最小渲染内核、白名单 Display/Panel 机制、必要视角控制与 RC26 专用显示对象。
- UI 交互层承担布局预设、图层管理、对象钻取、时间轴、历史回放、事件联动、工程操作台与中文界面，不直接面向原始 ROS2 消息。
- 数据路径固定为：源模块 topic/service/action 与静态场景真源进入源适配器，进入统一 viewer model，最后分发给原生 Display、原生 Panel 和可选嵌入式 Web 面板。
- 深度观测严格由源模块各自发布，命名保持在各自模块命名域，不统一迁入 `/r2/trace/*`，也不挂到 viewer 私有命名域。
- viewer 内建本地历史库，支持约 2 小时窗口的事件、状态、趋势与 trace 查询；长时间更深历史不要求在首期依赖外部录制才能浏览。
- 静态场景口径定为：规划和导航真源仍在原模块；`rc26_xhu_viewer` 持有统一的"显示真源"，包括显示所需几何固化副本、语义区、布局预设、显示目录和中文显示元数据。

## 2. 轻量化、减重与部署策略

- bringup 的可视化后端终态只保留 `rc26_xhu_viewer` 与 `none` 两种口径，不再长期保留通用 RViz 或独立本地 Web 后端。
- RViz2 仅保留主渲染窗口、变换管理、配置装载、专用 DisplayFactory、专用 Panel 容器、必要基础渲染原语与少量 RC26 视角控制。
- 删除所有通用型、与车端值守无关或会把产品重新拉回"通用调试器"的能力，包括通用插件浏览、任意添加显示/工具、帮助面板、通用截图向导、无关默认插件、测试框架和演示性渲染资产。
- 部署目标以车端为第一优先级，工程机只作为辅助观测与回归环境，不反过来主导架构。
- 性能目标固定为：车端操作态最低稳定 20 FPS，全信息诊断态最低稳定 15 FPS。
- 当前方案选择"信息最大化"而不是"稳态优先"，因此诊断态允许更高图层并发和更深 trace，但必须通过分层降载维持上述最低帧率。
- 可选 Web 面板必须进程内嵌，且不能引入明显破坏轻量化目标的重型依赖；若资源紧张，系统自动禁用 Web 面板，优先保证主三维、主面板和工程操作台流畅。
- 包内自带中文字体与 3D 标签字体资源，系统字体只作为补充回退，避免 AidLux 或 Ubuntu 环境差异导致乱码和显示不一致。

## 3. 可视化模块与呈现策略

- 定位模块提供主位姿、历史轨迹、协方差、健康度、退化分数、Hessian 指标、后端状态、关键帧、闭环、anchor、路线可观测性与重定位过程的专用显示与专用面板。
- 点云配准全过程首期必须补齐只读深度接口，展示原始点云、下采样点云、局部地图、最终对齐结果、迭代收敛曲线、残差变化、代表性对应关系与失败原因；不再仅停留在健康度摘要。
- LIO 模块提供退化分数、里程计协方差、点云累计地图、残差趋势与估计状态的专用入口，不再只作为上游黑盒。
- 导航模块同时显示全局 route、surface route、corridor、lookahead、局部候选轨迹、选中轨迹、恢复动作、速度剖面、模式状态、跟踪误差、停止原因与动态风险来源。
- 动态障碍预测首期纳入方案，不再作为"后续增强"；对应深度观测由导航相关源模块发布，viewer 负责 3D 预测包络、未来轨迹、时序条带与策略原因展示。
- Terrain 与 Merlin KFS 同时提供贴地图层和专用面板，显示 `FREE / BLOCKED / UNKNOWN / SLOW`、占据概率、drop 风险、climbable 概率、动态膨胀解释层和语义摘要。
- BT 提供三类视图：场地区域高亮视图、树结构与运行路径视图、时间线与事件流视图；BT 时间线、长列表和复杂趋势图允许使用进程内嵌 Web 面板，其余主交互仍走原生。
- 机构模块提供动作状态、通信健康、RTT、重连、错误码、命令耗时和动作历史的专用面板，并与诊断、BT、导航事件联动。
- 视觉、telecontrol、serial 等当前数据较稀疏的模块，首期仍按要求补齐专用面板和专用观测入口，不采用"只给摘要"的收缩策略。
- 整个 viewer 采用工作区布局，而不是让用户自行拼显示树。至少固化操作态、工程态、诊断态三套预设，并用布局切换同时控制订阅深度、图层显隐和渲染质量。

## 4. 中文化、操作台与扩展机制

- 界面 100% 中文化，范围包括菜单、状态栏、Display 名称、Panel 名称、告警、提示、时间轴说明、表格列名、工具栏、布局名称和插件名称。
- BT、黑板键、事件说明优先消费行为树本地化数据，不在 viewer 内部维护与运行时脱节的手工中文副本。
- `rc26_xhu_viewer` 被明确定义为工程操作台，而不再是只读观察屏。
- 操作权限采用"分级门禁 + 二次确认"策略：比赛常用操作可以直达，危险操作必须进入工程/调试页，并经过状态检查、门禁和确认流，不扩展成无门槛的全调试总控。
- 扩展机制需要服从轻量化约束、统一布局体系和中文术语体系。
- 新增深度观测接口的公开变化属于正式契约变更，需同步更新中间层契约文档；viewer 本地推导层只可作为解释层，不能冒充上游算法真源。

## 5. 公共接口变化与终态验收口径

- 对外聚合诊断接口统一切到 `r2/xhu_viewer/*`，不再保留 `r2/diag/*` 兼容层。
- 新增公共接口集中在各源模块自己的深度观测 topic/service/action 家族，而不是新增一个 viewer 专属对外协议层。
- bringup 对外终态只暴露 `rc26_xhu_viewer` 或 `none` 两种可视化后端选择。
- 静态显示真源迁入 `rc26_xhu_viewer` 后，需明确区分"上游规划真源"和"viewer 显示真源"，避免双向写回和真源漂移。
- 首期完整验收必须覆盖：车端启动时间、三套布局切换、操作态与诊断态帧率、Web 面板自动降级、中文无乱码、`r2/xhu_viewer/*` 接口可见性、全运行时模块的专用入口可见性、各深度 trace 的源模块发布链路、工程操作台门禁与确认流程、2 小时本地历史查询能力。
- 回归重点包括：定位 trace 与真实定位状态一致、导航预测与局部候选联动一致、BT 时间线与 subtree 高亮一致、KFS/terrain 概率层与离散格状态一致、机构事件与诊断告警联动一致。

## 6. 当前阶段性落地状态（2026-04-08）

### M1（已完成）：运行时入口切换与 preset 基线

- 新增 `src/rc26_xhu_viewer/rc26_xhu_viewer` 运行时包
- `main.cpp` 自定义入口，默认按 `mode/layout` 选择 preset
- 6 份 .rviz preset + launch + validate 脚本
- bringup 默认切到 `visualization_backend:=rc26_xhu_viewer`

### M2（已完成）：专用显示与操作台骨架

- **M2.1 白名单基础设施**：vendor 最小改动（`initMenus()`/`initToolbars()` 加 `virtual`，新增 `setDisplayFactory()`，`display_factory.hpp` 公开），`RC26DisplayFactory` 白名单过滤，`RC26XhuViewerFrame` 子类化，`main.cpp` 重写为直接创建 `RC26XhuViewerFrame`
- **M2.2 中文菜单/状态栏**：文件(&F)/面板(&P)/帮助(&H) 中文菜单，移除 Help Wiki/Add-Remove Tool，状态栏 `帧率: XX FPS`
- **M2.3 Preset 分组升级**：6 份 .rviz 从扁平 topic 列表升级为 Group 分组结构（场景基础/导航态势/点云诊断/地形安全/建图点云/轨迹对比），验收脚本同步验证分组名和帧率
- **M2.4 门禁面板**：`RC26ConsoleGatePanel` 实现操作态（只读）→ 工程态（确认解锁）→ 诊断态（二次确认）三级权限，pluginlib 注册

### M3（已完成）：深度观测入口补齐

- **M3.1 消息定义**：rc26_interfaces 新增 8 个消息（LocalizationKeyframe, LocalizationLoopClosure, LocalizationRelocState, RegistrationDebug, MechanismActionHistoryEntry, MechanismActionHistory, DynamicPrediction, DynamicPredictionArray）
- **M3.2 Display 插件**：5 个自定义 Display 骨架（LocalizationDisplay, RegistrationDebugDisplay, NavCandidatesDisplay, DynamicPredictionDisplay, TerrainSemanticDisplay），全部 pluginlib 注册 + 白名单放行 + "等待数据..."优雅降级
- **M3.3 Panel 插件**：13 个自定义 Panel 骨架（LocalizationPanel, RegistrationPanel, LioPanel, NavigationPanel, TerrainPanel, BtAreaPanel, BtTreePanel, BtTimelinePanel, MechanismPanel, VisionPanel, TelecontrolPanel, SerialPanel, OperatorStatusPanel），全部 pluginlib 注册
- **M3.4 契约文档同步**：`docs/middle/modules/localization.yaml` 新增 6 话题，`navigation.yaml` 新增 2 话题，`mechanism.yaml` 新增 1 话题，新建 `dynamic_prediction.yaml`

### M4（已完成）：进程内嵌 Web 与历史库

- **M4.1 Web 面板基础设施**：`WebPanelBase`（Qt5WebEngine 构建期可选探测，缺失时回退 QLabel 占位）+ `WebBridge`（QWebChannel JS 桥接），CMake `HAS_QT_WEBENGINE` 宏控制编译分支
- **M4.2 本地历史库**：`HistoryStore`（raw SQLite3 C API，3 表 events/state_snapshots/scalar_trends，timestamp_ms 索引，30s 清理线程，2h 保留，/tmp tmpfs）+ `HistoryCollector` 骨架 + `HistoryQueryPanel`（QLineEdit 关键字 + QComboBox 时间窗 + 查询按钮 + 结果展示）
- **M4.3 Web 面板 HTML**：`bt_timeline.html`（BT 事件时间线）、`trend_chart.html`（Canvas 趋势图）、`event_log.html`（可搜索/过滤事件日志），均通过 QWebChannel bridge.lastData 接收数据

### M5（已完成）：终态收口

- **M5.1 硬切并入**：原 `rc26_visualization` 状态聚合节点、Web adapter、viewer 前端和配套测试已经整体迁入 `rc26_xhu_viewer`，旧包与旧入口全部删除，对外接口同步切到 `r2/xhu_viewer/*`
- **M5.2 术语统一**：`resources/terminology.yaml` 中文显示名单一真源，覆盖菜单/工具栏/状态栏/门禁/Panel/Display/分组/通用文案
- **M5.3 文档回写**：`docs/backend/archive/rc26_xhu_viewer/README.md`、`docs/backend/archive/rc26_bringup/README.md` 与相关契约文档已经按单包口径回写

### 构建验证摘要

| 包 | 状态 |
|---|---|
| `rviz_common` | 通过（vendor 改动：virtual + setter + 公开头文件 + FPS 中文化） |
| `rc26_interfaces` | 通过（新增 8 个消息） |
| `rc26_xhu_viewer` | 通过（viewer 主程序 + `rc26_xhu_viewer_status_node` + `librc26_xhu_viewer_plugins.so` + Web adapter） |
| `rc26_bringup` | 通过（默认装配 `rc26_xhu_viewer` 与 `rc26_xhu_viewer_status_node`） |

### 当前真实边界

- 所有 Display/Panel 当前为骨架状态，显示"等待数据..."，待各源模块发布对应 topic 后接入实际订阅
- 源模块（rc26_localization、rc26_local_3d_planner、rc26_mechanism）的新深度观测 publisher 尚未添加
- QWebEngine 在当前构建机不可用，Web 面板走 fallback 占位
- 本地历史库基础设施已就绪，ROS 订阅到 HistoryStore 的接入未完成
- 中文字体包（Noto Sans SC）尚未捆绑到 resources/fonts/
- 还未完成真实图形界面启动下的 FPS 验收和 QCS8550 目标硬件验证

## 7. 代码级验收记录

### M1 验收（2026-04-07）

- 构建通过，配置验收通过，6 份 preset 验证通过
- 详见第 6 节 M1 段落

### M2-M5 验收（2026-04-08）

- `rviz_common` clean rebuild 通过（含 virtual + setDisplayFactory + display_factory.hpp 公开 + FPS 中文）
- `rc26_interfaces` 通过（8 新消息编译 + 安装）
- `rc26_xhu_viewer` full rebuild 通过（38.2s，19 插件类 + SQLite + Web fallback）
- CMake 输出 `Qt5WebEngine not found - web panels will show fallback placeholder`（预期行为）
- plugins_description.xml 注册 5 Display + 14 Panel = 19 个插件

### 待完成的验收项

| 验收项 | 状态 | 说明 |
|---|---|---|
| 启动时间 < 5s（QCS8550） | 待验 | 需目标硬件 |
| Operator FPS ≥ 20 | 待验 | 需图形界面 |
| Diagnostic FPS ≥ 15 | 待验 | 需图形界面 |
| 布局切换无崩溃 | 待验 | 需图形界面 |
| Web 降级自动触发 | 待验 | 需 WebEngine 环境对比 |
| 中文渲染无乱码 | 待验 | 需字体捆绑 + 图形界面 |
| r2/xhu_viewer/* 接口 | 通过 | bringup、Foxglove、Web live bridge 与状态节点已统一切换 |
| 深度观测话题可见 | 待验 | 需源模块 publisher |
| 门禁分级 | 待验 | 需图形界面交互测试 |
| 历史查询 2h 窗口 | 待验 | 需 ROS 订阅接入 |
| 白名单过滤 | 待验 | 需图形界面确认"添加显示"对话框 |

## 8. 后续工作

### 近期（骨架接入实际数据）

- 各源模块添加深度观测 publisher（rc26_localization 5 个新 topic，rc26_local_3d_planner 2 个，rc26_mechanism 1 个）
- Display/Panel 接入真实 ROS 订阅，替换"等待数据..."骨架
- HistoryCollector 接入 ROS 订阅，将事件/标量推入 HistoryStore
- 中文字体捆绑（Noto Sans SC Regular + Bold → resources/fonts/）

### 中期（集成与性能）

- QCS8550 目标硬件 FPS 验收与调优
- Web 面板在有 Qt5WebEngine 环境下的端到端验证
- BtTreePanel 接入真实 bt/model + bt/snapshot，替换 QTreeWidget placeholder
- 门禁联动：operator 态下禁用 Panel 中的服务调用按钮

### 长期

- 评估是否需要裁剪 vendor 中不使用的 upstream 包
- 可选：继续评估 `viewer/` 中未使用的历史页面与 release 资产是否还能进一步裁剪
