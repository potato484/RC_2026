# R2 测试指令速查

这份文件只做现场临时速查；权威说明仍以 `docs/backend/archive/rc26_bringup/README.md`、`docs/backend/archive/rc26_decision/README.md` 和 `src/rc26_bringup/test/README.md` 为准。

当前完整链路默认读取这个文件里的配置：

```text
/home/potato/RC_2026/src/rc26_bringup/config/r2_runtime.yaml
```

现场切换地图、点云或行为树时，只需要先改这里的三类绝对路径：

- `r2_runtime.paths.prior_pcd_file`：定位链使用的先验 PCD 点云地图
- `r2_runtime.paths.nav2_map_file`：Nav2 `map_server` 使用的二维栅格地图 YAML
- `r2_runtime.paths.behavior_tree_file`：决策节点加载的行为树 XML

路径改好后，下面的导航和决策启动命令默认都会吃这份 YAML，不需要再在命令行里重复传地图路径。

## 0. 环境准备

```bash
cd "${RC26_WS:-$HOME/RC_2026}"
source install/setup.bash
```

如果刚改过相关包或刚拉新代码，先按仓库统一口径编译当前主运行闭包：

```bash
MAKEFLAGS='-j2 -l2' colcon build --symlink-install --executor sequential --parallel-workers 1 \
  --packages-select \
  rc26_interfaces rc26_serial rc26_small_gicp rc26_mid360_driver \
  rc26_sensor_extrinsics rc26_point_lio rc26_odom_interface rc26_sensor_scan \
  rc26_localization rc26_merge_odom rc26_mechanism rc26_vision \
  rc26_telecontrol rc26_decision rc26_bringup
source install/setup.bash
```

## 1. 建图调试

直接启动建图链路：

```bash
ros2 launch rc26_bringup test_mapping.launch.py
```

如果雷达能 ping 通但没有点云数据，可让启动入口先恢复 Mid-360 数据流：

```bash
ros2 launch rc26_bringup test_mapping.launch.py recover_mid360_stream:=true
```

建图时建议另开终端检查：

```bash
ros2 topic hz /state_estimation
ros2 topic hz /registered_scan
ros2 topic echo /Laser_map --once --field header.frame_id
```

Point-LIO 正常 `Ctrl+C` 退出后，默认会把新建图结果保存到：

```text
${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd
```

仓库当前也保留了一份样例/已有点云：

```text
${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scan.pcd
```

## 2. 建图时遥控

`test_mapping.launch.py` 只负责建图链路，不负责手柄遥控。需要带车移动时，另开终端启动遥控链：

```bash
cd "${RC26_WS:-$HOME/RC_2026}"
source install/setup.bash
./start_r2_teleop.sh --stack minimal-mcu --mode dpad
```

进入导航或决策测试前先停掉遥控链，避免遥控和 Nav2 同时发布 `/cmd_vel`。

## 3. 生成 Nav2 二维地图

建图得到的 PCD 给 localization 使用；Nav2 还需要一份二维 occupancy map YAML。可用当前仓库脚本从 PCD 投影生成：

```bash
python3 src/rc26_point_lio/scripts/pcd_to_nav2_map.py \
  src/rc26_point_lio/PCD/scans.pcd \
  --output-prefix src/rc26_bringup/map/site_a \
  --resolution 0.05 \
  --z-min 0.05 \
  --z-max 2.0 \
  --min-points-per-cell 3 \
  --overwrite
```

这会生成：

```text
src/rc26_bringup/map/site_a.png
src/rc26_bringup/map/site_a.yaml
```

排查 PCD 与 Nav2 map 覆盖范围时：

```bash
python3 src/rc26_point_lio/scripts/pcd_map_inspector.py \
  src/rc26_point_lio/PCD/scans.pcd \
  --map-yaml src/rc26_bringup/map/site_a.yaml
```

生成或替换地图后，把 `r2_runtime.yaml` 里的路径改成当前要测的文件，例如：

```yaml
r2_runtime:
  paths:
    prior_pcd_file: "/home/potato/RC_2026/src/rc26_point_lio/PCD/scans.pcd"
    nav2_map_file: "/home/potato/RC_2026/src/rc26_bringup/map/site_a.yaml"
    behavior_tree_file: "/home/potato/RC_2026/src/rc26_decision/behavior_trees/main_tree.xml"
```

## 4. Nav2 导航联调

只验证定位、Nav2 生命周期和话题链路；当前 `test_navigation.launch.py` 不再启动底盘执行桥：

```bash
ros2 launch rc26_bringup test_navigation.launch.py
```

实机导航联调时，先另行启动工作区外部 `/cmd_vel` 底盘 consumer，再启动导航链：

```bash
ros2 launch rc26_bringup test_navigation.launch.py
```

当前 bringup 不再读取或透传 `start_pose_sender`、`pose_sender_*`、`merge_odom_*` 参数；`/cmd_vel` 的硬件消费方由工作区外部运行时提供。

导航启动后快速检查：

```bash
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /bt_navigator
ros2 action info /navigate_to_pose
ros2 topic echo /sensor_scan --once
ros2 topic echo /plan --once
ros2 topic echo /local_costmap/costmap --once
ros2 topic echo /global_costmap/costmap --once
ros2 topic echo /cmd_vel --once
```

## 5. 采集 Nav2 导航点

需要把现场位置固化进行为树或 `r2_runtime.yaml` 前，先启动导航联调链路并确认定位稳定。采点脚本只读取 `map -> base_footprint` TF，不发布 `/cmd_vel`，不会接管定位、Nav2 或底盘控制。

先确认当前机器人在 `map` 坐标系下的 TF 可用：

```bash
ros2 run tf2_ros tf2_echo map base_footprint
```

另开终端启动采点脚本：

```bash
cd "${RC26_WS:-$HOME/RC_2026}"
source install/setup.bash

ros2 run rc26_bringup capture_nav_points.py \
  --output nav_points.txt \
  --frame-id map \
  --base-frame base_footprint
```

人工遥控机器人到目标位置后，在采点脚本终端直接按 `Enter` 记录一个自动编号点；也可以先输入点名再按 `Enter`。采完后输入 `q` 退出。输出文件里会同时生成点位表和可直接复制进 BT XML 的目标，例如：

```xml
<NavToPose x="1.2000" y="-0.6000" yaw="1.5708" frame_id="map"/>
```

采点时不要同时执行 Nav2 action、决策树或台阶动作；人工遥控只用于把车移动到待记录位置，确保同一时刻只有一个运动命令来源。

## 6. 手动发送导航目标

链路已经起来后，可以先发一个短距离目标验证 `/navigate_to_pose`：

```bash
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 1.2, y: 0.0, z: 0.0}, orientation: {z: 0.7071, w: 0.7071}}}}"
```

实机验证时先用短距离、小角度目标，确认周围安全且没有并发遥控链。

## 7. 武馆区决策测试

先把 `src/rc26_bringup/config/r2_runtime.yaml` 中的行为树入口切到武馆区：

```yaml
r2_runtime:
  paths:
    prior_pcd_file: "/home/potato/RC_2026/src/rc26_point_lio/PCD/scan.pcd"
    nav2_map_file: "/home/potato/RC_2026/src/rc26_bringup/map/test.yaml"
    behavior_tree_file: "/home/potato/RC_2026/src/rc26_decision/behavior_trees/mc_tree.xml"
```

同时确认同一文件里的 `mc_nav_x`、`mc_nav_y`、`mc_nav_yaw`、相机参数、夹取 service、旋转参数和 MC 返回导航参数已经按现场标定更新。武馆区流程是：

```text
CaptureCurrentPose -> NavToPose(正向) -> VisualServoGrab -> RotateInPlace -> CaptureCurrentPose -> NavToPose(负向返回启动位姿) -> WaitForever
```

然后用完整 bringup 启动决策链路。

终端 A：正常启动完整链路，保留全部节点输出，方便发现 Nav2、定位或硬件链路错误：

```bash
ros2 launch rc26_bringup bringup.launch.py \
  run_mode:=navigation \
  use_decision:=true
```

终端 B：只看行为树 / 决策节点日志：

```bash
ros2 topic echo /rosout --filter "m.name == 'rc26_decision'"
```

如果当前 ROS2 CLI 不支持 `--filter`，用下面这个兼容写法：

```bash
ros2 topic echo /rosout | grep --line-buffered -A 8 -B 2 'name: rc26_decision'
```

也可以在终端 B 直接跟随 ROS 日志文件；这种方式需要终端 A 已经启动过一次：

```bash
tail -F ~/.ros/log/latest/*rc26_decision*
```

如果确实想在同一个终端里边启动边过滤，只看 `rc26_decision` 前缀日志：

```bash
stdbuf -oL -eL ros2 launch rc26_bringup bringup.launch.py \
  run_mode:=navigation \
  use_decision:=true \
  2>&1 | grep --line-buffered -E '^\[rc26_decision-[0-9]+\]'
```

## 8. 注意事项

- `test_mapping.launch.py` 默认产出 PCD，不会直接产出 Nav2 occupancy map。
- `prior_pcd_file` 和 `nav2_map_file` 是两个不同输入：前者给定位，后者给 Nav2。
- 导航和决策默认读取 `/home/potato/RC_2026/src/rc26_bringup/config/r2_runtime.yaml`；切换测试地图时优先改这个文件里的 `r2_runtime.paths.*`。
- `src/rc26_bringup/map/test.yaml` 适合基础联调，不等同于现场最终规划地图。
- 导航模式下 `/cmd_vel` 由 Nav2 输出；遥控测试应和导航/决策链分开启动。
- 决策测试不再使用 `rc26_decision` 独立 launch，必须通过完整 `rc26_bringup/launch/bringup.launch.py` 验证。
