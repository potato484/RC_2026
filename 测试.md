# 建图到导航快速测试

这份文档提供当前 R2 主运行时里，从 `Point-LIO` 建图到 `Nav2` 导航联调的最短启动路径。

当前实现有一个必须先说明的事实：

- `test_mapping.launch.py` 会产出 `PCD` 点云地图，供 `rc26_localization` 做先验点云重定位使用。
- `test_mapping.launch.py` 不会直接产出 `Nav2` 可消费的 `2D OccupancyGrid` 地图。
- 因此“建图 -> 导航”在当前仓库里实际分成两份输入：
  - `prior_pcd_file`：给定位链用的点云地图
  - `nav2_map_file`：给 `map_server` 用的二维栅格地图 YAML

如果你手头只有 Point-LIO 导出的 `scans.pcd`，那可以完成“建图 + 定位 + Nav2 链路冒烟”，但不能把 `src/rc26_bringup/map/default.yaml` 当成真实规划地图验收。

## 1. 前置准备

```bash
cd "${RC26_WS:-$HOME/RC_2026}"
MAKEFLAGS='-j2 -l2' colcon build --executor sequential --parallel-workers 1 \
  --packages-select rc26_bringup rc26_point_lio rc26_localization rc26_merge_odom rc26_telecontrol
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

建议在实机前先确认：

- `Livox Mid-360` 已接好并能正常出流
- 遥控手柄可用，建图阶段需要人为带机器人走一圈
- 导航阶段不要再同时保留遥控 `cmd_vel` 发布者

## 2. 启动建图

直接启动建图调试入口：

```bash
ros2 launch rc26_bringup test_mapping.launch.py
```

如果现场出现“雷达能 ping 通，但 `/livox/lidar` 没数据”，可改用：

```bash
ros2 launch rc26_bringup test_mapping.launch.py recover_mid360_stream:=true
```

建图过程中，建议至少检查一次这些输出：

```bash
ros2 topic hz /state_estimation
ros2 topic hz /registered_scan
ros2 topic echo /Laser_map --once --field header.frame_id
```

## 3. 建图时移动机器人

`test_mapping.launch.py` 只负责建图链本身，不负责底盘遥控。建图时如果需要手柄带车移动，可在另一个终端显式启动遥控链：

```bash
cd "${RC26_WS:-$HOME/RC_2026}"
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
./start_r2_teleop.sh --stack minimal-mcu --mode dpad
```

补充说明：

- 这里显式写 `--stack minimal-mcu --mode dpad`，避免依赖脚本默认值
- 如果现场只有一个目标 MCU 串口，脚本会按当前实现自动尝试降级到单串口口径
- 建图完成后，进入导航前应先停掉这路遥控，避免和 Nav2 抢 `/cmd_vel` 命令权

## 4. 结束建图并确认 PCD 输出

建图结束后，用 `Ctrl+C` 正常退出建图进程。当前默认会把累计点云保存到：

```text
${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd
```

可以先确认文件是否存在：

```bash
ls -lh "${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd"
```

后续导航启动时，把这个文件作为 `prior_pcd_file` 传给定位链。

## 5. 准备导航地图输入

### 5.1 点云先验地图

Point-LIO 建图完成后得到的：

```text
${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd
```

这份文件用于：

- `rc26_localization` 开局一次重定位
- 连续 GICP 跟踪定位

### 5.2 Nav2 二维栅格地图

真实导航还需要一份 `2D occupancy map YAML`，例如：

```text
${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/map/site_a.yaml
```

当前要点：

- `src/rc26_bringup/map/default.yaml` 只是占位图，只用于让 `map_server` 和 Nav2 生命周期节点顺利拉起
- 当前 `test_mapping.launch.py` 不会直接生成这份二维地图
- 如果你使用的是另一套能发布 `/map` (`nav_msgs/OccupancyGrid`) 的建图链，可以用下面命令保存：

```bash
ros2 run nav2_map_server map_saver_cli \
  -f "${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/map/site_a"
```

保存后会得到 `site_a.yaml` 和对应的 `pgm/png` 图像，导航时把 `site_a.yaml` 作为 `nav2_map_file` 传入。

## 6. 启动导航

### 6.1 开发机 / 图结构冒烟

如果当前只想验证定位、Nav2 生命周期和话题链路，不需要真正下发到底盘：

```bash
ros2 launch rc26_bringup test_navigation.launch.py \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd \
  start_pose_sender:=false
```

说明：

- 这一步不强依赖真实串口
- 如果没传 `nav2_map_file`，会退回仓库里的占位地图，只适合冒烟，不适合真实规划验收

### 6.2 实机导航联调

如果已经准备好了真实 `PCD + 2D occupancy map`，并且希望真正把速度指令发到底盘：

```bash
ros2 launch rc26_bringup test_navigation.launch.py \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd \
  nav2_map_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/map/site_a.yaml \
  start_pose_sender:=true
```

如果现场只有目标串口，没有反馈串口，改用：

```bash
ros2 launch rc26_bringup test_navigation.launch.py \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/scans.pcd \
  nav2_map_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/map/site_a.yaml \
  start_pose_sender:=true \
  pose_sender_feedback_serial_port:=__disabled__
```

## 7. 导航阶段快速验证

导航启动后，建议按下面顺序快速看一遍：

```bash
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /bt_navigator
ros2 action info /navigate_to_pose
ros2 topic echo /sensor_scan --once
ros2 topic echo /plan --once
ros2 topic echo /local_costmap/costmap --once
ros2 topic echo /global_costmap/costmap --once
```

如果此时是实机导航模式，还可以继续看：

```bash
ros2 topic echo /cmd_vel
```

预期现象：

- `controller_server`、`planner_server`、`bt_navigator` 都进入 `active`
- `/navigate_to_pose` action 可见
- `/sensor_scan`、`/plan`、局部 / 全局 costmap 至少能读到一次消息
- 实机模式下 `/cmd_vel` 会在导航过程中持续输出

## 8. 手动发送一个导航目标

链路已经起来后，可以手工发一个简单目标：

```bash
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 1.2, y: 0.0, z: 0.0}, orientation: {z: 0.7071, w: 0.7071}}}}"
```

如果是纯冒烟模式：

- 主要看 action 能否建立、Nav2 节点是否进入工作状态
- 不要把占位地图上的规划结果当成真实可用路径

如果是实机模式：

- 先确认当前没有并发遥控链继续发布 `/cmd_vel`
- 先用短距离、小角度目标做首轮验证

## 9. 常见注意事项

- 当前“建图 -> 导航”不是单地图文件闭环，而是 `PCD` 给定位、`OccupancyGrid YAML` 给 Nav2。
- `test_mapping.launch.py` 的默认成果是 `scans.pcd`，不是 `site_a.yaml` 这类二维地图。
- `src/rc26_bringup/map/default.yaml` 只能用于 Nav2 冒烟启动，不能替代真实规划地图。
- 导航模式下 `/cmd_vel` 权威来自 Nav2；遥控测试应与导航分开启动，或在进入导航前先停掉遥控链。
- 如果要排查定位本身而不是导航，可先单独跑 `test_localization_chain.launch.py` 或 `test_relocalization.launch.py`。
