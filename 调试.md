# RC_2026 R2 实机调试手册（按模块执行方案对齐）

适用范围：
- 仅面向 R2 自动机器人（本仓库目标平台），R1 手动机器人不在本文范围内
- OS: Ubuntu 22.04 + ROS 2 Humble
- 约定：所有 Python 命令使用 `python3`

本文目标：
- 把 `/home/potato/RC_2026/MVP技术方案` 中各模块“最新执行方案”的验收口径，落到本仓库的实际 `launch/config/node/topic/param` 上
- 给出一套可复现的实机调试顺序：先底层再上层，最后做决策（行为树）验证

---

## 0. 一次性约定（先看这段，避免走弯路）

### 0.1 推荐的调试顺序（依赖关系）

按依赖从低到高：
1. 串口与底盘闭环（`rc26_serial` + `rc26_merge_odom` + 可选 `rc26_telecontrol`）
2. 里程计 TF 链路（`mid360_driver` + `point_lio` + `rc26_odom_interface` + `rc26_sensor_scan`）
3. 建图与场地标定（仅在“无地图/地图不匹配/需要重建”时做）：生成 `prior.pcd` + `map.yaml/pgm`，并标定 `retry_zone_x/y`、关键点位
4. 定位（`rc26_localization` 发布 `map->odom`）
5. 地面高度与地形语义（`rc26_base_ground` + `rc26_terrain`）
6. Keepout 掩码链路（`rc26_decision` 发布 `/mf_kfs_state`，`rc26_kfs_keepout` 输出 `/kfs_filter_mask`，`costmap_filter_info_server` 输出 `/costmap_filter_info`）
7. Nav2 与控制器（`nav2_bringup` + `rc26_omni_controller`）
8. 导航模式管理（`rc26_nav_mode_manager` 提供 `set_nav_mode` 与 `nav_safety_state`）
9. 决策系统（`rc26_decision` 行为树跑通并能安全发目标）

### 0.2 统一命名与关键话题（必须对齐）

TF 目标链路（来自 TF 变换模块执行方案）：
- `map -> odom -> base_link -> livox_frame`

里程计/点云关键话题（来自 TF 变换与虚拟执行方案）：
- `/state_estimation`（Point-LIO 里程计）
- `/odom`（`rc26_odom_interface` 输出）
- `/registered_scan`（`rc26_odom_interface` 输出）
- `/odometry`（`rc26_sensor_scan` 输出，Nav2 使用此 odom_topic）
- `/terrain_obstacles`、`/terrain_drop`（`rc26_terrain` 输出，Nav2 costmap 使用）

Keepout 关键话题（来自定位执行方案5）：
- `/costmap_filter_info`（KeepoutFilter 元数据）
- `/kfs_filter_mask`（KFS keepout 掩码 OccupancyGrid）
- `/mf_kfs_state`（决策发布，给 keepout fuser 使用）

### 0.3 实机日志建议（避免写入权限问题）

建议在每个终端先设置：
```bash
export ROS_LOG_DIR=/tmp/ros_log
```

### 0.4 RViz2 辅助验收（强烈推荐，但不是硬依赖）

结论先说清楚：
- 本仓库的导航/定位/决策链路不依赖 RViz2 才能运行，但“实机联调效率”和“对齐正确性验收”高度依赖可视化
- 你可以不用 RViz2，只用本文中的 `tf2_echo/topic echo` 做验收；但遇到“坐标系/地图对齐/keepout/地形语义”问题时，纯 CLI 会非常慢

推荐直接用仓库内已有 RViz 配置：
```bash
# 里程计/SLAM链路观察（Fixed Frame 通常用 odom）
rviz2 -d /home/potato/RC_2026/src/rc26_bringup/rviz/slam.rviz

# Nav2 默认视图（定位起来后 Fixed Frame 用 map）
rviz2 -d /home/potato/RC_2026/src/rc26_bringup/rviz/nav2_default.rviz

# 地形语义（Fixed Frame 通常用 odom）
rviz2 -d /home/potato/RC_2026/src/rc26_terrain/rviz/terrain_semantic.rviz
```

在 RViz2 里建议按阶段验收：
1. TF 与点云链路（第4章）
检查 `TF` 显示中是否存在 `odom -> base_link -> livox_frame`；点云（`/registered_scan`）是否稳定随车体运动。
2. 定位（第5章）
把 Fixed Frame 切到 `map`；叠加 `Map`（`/map`）和点云（`/registered_scan`）观察是否对齐；必要时用 RViz 的 `2D Pose Estimate` 发布 `initialpose` 作为重定位种子。
3. Keepout 与 Costmap（第9.1节）
添加 `Map` 显示订阅 `/kfs_filter_mask`，确认 keepout mask 与地图坐标对齐；再观察 Nav2 costmap 是否正确被过滤（无 keepout 时行为树会被 gate 掉）。
4. 地形语义（第6章）
在 `terrain_semantic.rviz` 中观察 `/terrain_obstacles`、`/terrain_drop` 是否与场地一致，确认阈值与延迟干预没有“误伤”。

---

## 1. 参数与文件：哪些要人提供，哪些要提前配置

### 1.1 现场必须提供或确认（每次上车都要核对）

| 类别 | 参数/开关 | 传入方式 | 影响模块 | 备注 |
|---|---|---|---|---|
| 串口路径 | IMU 串口 `imu_port` | `rc26_merge_odom/launch/merge_odom.launch.py` 的 launch arg | `rc26_merge_odom` | 建议用 `/dev/serial/by-id/...` |
| 串口路径 | 底盘反馈口 `feedback_serial_port` | `rc26_merge_odom/launch/merge_odom.launch.py` 的 launch arg | `rc26_merge_odom` | 用于 WheelOdom 读反馈与 PoseSender 发反馈 |
| 串口路径 | 底盘目标口 `target_serial_port` | `rc26_merge_odom/launch/merge_odom.launch.py` 的 launch arg | `rc26_merge_odom` | 用于 PoseSender 下发目标速度 |
| 串口路径 | 机构串口 `serial_port` | `rc26_mechanism/launch/mechanism.launch.py` 的 launch arg | `rc26_mechanism` | 必须与底盘串口区分，避免串口占用冲突 |
| CAN | `use_can_odom`、`can_interface` | `rc26_merge_odom/launch/merge_odom.launch.py` 的 launch arg | `rc26_merge_odom` | 不用 CAN 时保持 `use_can_odom:=false` |
| LiDAR 网络 | `mid360_driver.host_ip` | `src/mid360_driver/config/param.yaml` | `mid360_driver` | 必须是车载机连接 Mid360 的网卡 IP |
| 地图 | `map:=...` | `rc26_bringup/launch/bringup.launch.py` 的 launch arg | `nav2_map_server` | `src/rc26_bringup/map/default.yaml` 是占位图 |
| 先验点云 | `prior_pcd_file:=...` | `rc26_bringup/launch/bringup.launch.py` 的 launch arg | `point_lio` + `rc26_localization` | 默认 `src/rc26_bringup/pcd/default.pcd` 不存在，必须传真实路径 |
| 定位标定 | `retry_zone_x/y`、`retry_zone_enable`、`competition_mode` | `src/rc26_bringup/config/localization.yaml`（或启动时 `-p` 覆盖） | `rc26_localization` | `competition_mode=true` 时若 `retry_zone_x/y=(0,0)` 会拒绝启动（必须实测填入） |
| 队伍 | `team`（`red`/`blue`） | `rc26_decision` 参数（YAML 或 `-p team:=...`） | `rc26_decision` | 决策会按 team 初始化梅林高度矩阵与加载点位文件 |
| 点位文件 | `waypoints_file` | `rc26_decision` 参数（YAML 或 `-p waypoints_file:=...`） | `rc26_decision` | 不填则默认用 `src/rc26_decision/config/waypoints/waypoints_<team>.yaml` |
| Keepout 布局 | `mf_grid_layout.yaml` 的 x/y | 编辑 `src/rc26_kfs_keepout/config/mf_grid_layout.yaml` | `rc26_kfs_keepout` | 必须与真实 `map` 坐标对齐，否则 keepout 全局偏移 |
| RealSense | `use_realsense`、`realsense_serial_no` | `rc26_bringup/launch/bringup.launch.py` 的 launch arg | `realsense2_camera` | 仅在需要视觉时开启 |
| LLM/VLM（离线管线必需） | `SLAM_AGENT_API_KEY`、`SLAM_AGENT_AI_ENDPOINT`、`SLAM_AGENT_VLM_MODEL`、`SLAM_AGENT_LLM_MODEL`（或统一用 `SLAM_AGENT_AI_MODEL` 兜底） | 环境变量 | `src//offline/*`、`scripts/reasoning_client.py` | 仅用于 `` 离线调参提案/诊断；未配置会直接失败退出（非 0） |

### 1.2 需要提前配置并版本化（赛前固化，现场尽量不改）

| 模块 | 关键配置文件 | 作用 |
|---|---|---|
| 串口驱动 | `src/rc26_serial` | 异步重连、ACK 唤醒、防死锁等已落地（见执行方案：串口/执行方案1） |
| 姿态融合与下发 | `src/rc26_merge_odom/config/merge_odom_params.yaml` | PoseSender 门控、slip、自适应协方差、延迟补偿（见执行方案：姿态融合和下发/执行方案1） |
| TF 变换链 | `src/rc26_bringup/launch/odometry.launch.py` | 关闭 Point-LIO TF，确保 `odom->base_link` 权威唯一（见执行方案：TF变换/执行方案1） |
| odom 接口 | `src/rc26_bringup/config/odom_interface.yaml` | `use_input_twist: true` 默认透传上游 twist（见 TF 变换执行方案） |
| sensor_scan | `src/rc26_bringup/config/sensor_scan_generation.yaml` | 输出 `/odometry` 供 Nav2，输出 `/sensor_scan` 供后续链路 |
| 定位 | `src/rc26_bringup/config/localization.yaml` | small_gicp + 绑架恢复 + QCS8550 线程亲和（见定位执行方案5） |
| 地面高度估计 | `src/rc26_base_ground/config/base_ground_estimator.yaml` | `stable_terrain/stable_operation/is_lifted` 与 tol 解耦（见地形跳跃执行方案1） |
| 地形语义 | `src/rc26_terrain/config/terrain_semantic.yaml` | obstacles/drop 输出、延迟干预、热降频链路（见定位执行方案5） |
| 控制器 | `src/rc26_bringup/config/nav2_params.yaml` | `rc26_omni_controller` 插件与 100Hz 控制频率（见控制器执行方案1） |
| 导航模式 | `src/rc26_nav_mode_manager/config/nav_profiles.yaml` | `safe/normal/mf_traverse/mf_exit/stair_up/stair_down/...`（决策依赖 `set_nav_mode`） |
| 决策 | `src/rc26_decision/config/decision_params.yaml` | 行为树 tick、keepout gate、里程计话题等 |
| 决策行为树 | `src/rc26_decision/behavior_trees/*.xml` | `main_tree.xml` 默认串起 MC/MF/Combat |

---

## 2. 编译门禁（每次大改动后先做）

按模块逐步编译，避免一次全量编译浪费时间：
```bash
cd /home/potato/RC_2026
colcon build --parallel-workers 1 --packages-select rc26_serial rc26_merge_odom rc26_mechanism rc26_interfaces
colcon build --parallel-workers 1 --packages-select mid360_driver point_lio rc26_odom_interface rc26_sensor_scan
colcon build --parallel-workers 1 --packages-select rc26_localization rc26_base_ground rc26_terrain rc26_kfs_keepout rc26_nav_mode_manager
colcon build --parallel-workers 1 --packages-select rc26_omni_controller rc26_bringup rc26_decision
source install/setup.bash
```

可选测试（定位执行方案5与虚拟执行方案建议）：
```bash
ROS_LOG_DIR=/tmp/ros_log colcon test --parallel-workers 1 --packages-select rc26_terrain
```

---

## 3. 先调底层：串口与运动闭环（实机必须先保证可控）

对应执行方案：
- 串口：`MVP技术方案/串口/执行方案/执行方案1.md`
- 姿态融合与下发：`MVP技术方案/姿态融合和下发/执行方案/执行方案1.md`

### 3.1 设备枚举与端口确认

在车载机上确认设备路径（推荐 by-id，避免 `ttyUSBx` 漂移）：
```bash
ls -l /dev/serial/by-id/ || true
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null || true
```

确认手柄（如需遥控）：
```bash
ls -l /dev/input/js0 || true
```

### 3.2 IMU 单模块验证（达妙）

启动 IMU：
```bash
ros2 launch rc26_merge_odom dm_imu_only.launch.py imu_port:=/dev/ttyACM0
```

检查：
```bash
ros2 topic echo /DM_IMU --once
```

### 3.3 WheelOdom 单模块验证（底盘反馈）

启动：
```bash
ros2 launch rc26_merge_odom wheel_odom_only.launch.py serial_port:=/dev/ttyUSB0
```

检查：
```bash
ros2 topic echo /wheel_odom --once
ros2 topic hz /wheel_odom
```

### 3.4 merge_odom + EKF + PoseSender（姿态融合与下发验收入口）

启动（按实车端口替换）：
```bash
ros2 launch rc26_merge_odom merge_odom.launch.py \
  use_can_odom:=false \
  imu_port:=/dev/ttyACM0 \
  feedback_serial_port:=/dev/ttyUSB0 \
  target_serial_port:=/dev/ttyUSB1
```

必查话题（姿态融合与下发执行方案的可观测性清单）：
```bash
ros2 topic echo /pose_sender/feedback_protected --once
ros2 topic echo /pose_sender/target_protected --once
ros2 topic echo /pose_sender/imu_spike_active --once
ros2 topic echo /wheel_odom/slip_score --once
ros2 topic echo /wheel_odom/cov_state --once
```

串口拔插复原（串口执行方案1的“必须验收项”）：
- 运行中拔插 USB 串口，观察是否自动重连恢复
- 重点看是否出现“重连风暴”或主线程卡死

### 3.5 遥控闭环（可选，但推荐先做）

目的：在上层导航/决策未稳定前，先保证底盘可低速可控。

1) 启动遥控（默认发布到 `/cmd_vel_teleop`）：
```bash
ros2 launch rc26_telecontrol wheeltec_joy.launch.py
```

2) 让 PoseSender 订阅遥控速度（不改代码的最小做法：给节点叠加第二个 params-file）：
```bash
ros2 run rc26_merge_odom merge_odom_node --ros-args \
  --params-file /home/potato/RC_2026/src/rc26_merge_odom/config/merge_odom_params.yaml \
  --params-file /home/potato/RC_2026/src/rc26_merge_odom/config/pose_sender_cmd_vel_teleop.yaml
```

检查：
```bash
ros2 topic echo /cmd_vel_teleop --once
```

---

## 4. 再调中层：TF 变换与里程计链路（必须先把 TF 打通）

对应执行方案：
- TF 变换：`MVP技术方案/TF变换/执行方案/执行方案1.md`
- 虚拟：`MVP技术方案/虚拟/执行方案/执行方案1.md`

### 4.1 实机启动里程计链（Mid360 + Point-LIO + odom_interface + sensor_scan）

启动：
```bash
ros2 launch rc26_bringup odometry.launch.py use_sim_time:=false
```

必查（TF 变换执行方案的验收口径）：
```bash
ros2 run tf2_ros tf2_echo odom base_link
ros2 topic echo /state_estimation --once
ros2 topic echo /odom --once
ros2 topic echo /odometry --once
ros2 topic echo /registered_scan --once
```

常见问题排查：
- `/tf` 报 `TF_REPEATED_DATA`：优先确认 Point-LIO 的 TF 已被关闭（`src/rc26_bringup/launch/odometry.launch.py` 已设置 `publish.tf_send_en:=False`），以及 `odom->base_link` 是否只有一个发布者
- `/odometry` 无速度：确认 `src/rc26_bringup/config/odom_interface.yaml` 的 `use_input_twist: true`，以及 `/state_estimation.twist` 非零

### 4.2 无雷达时的最小闭环（仅用于验证链路，不等于实机）

执行方案“虚拟/执行方案1”提供了 mock：
```bash
ros2 launch rc26_bringup odometry_mock.launch.py use_sim_time:=false
ros2 launch rc26_terrain terrain_semantic.launch.py
```

### 4.3 建图（先做这个，才能进入“导航模式”）

你说得对：如果你当前没有一份“与真实场地匹配且对齐”的地图，那么必须先建图，再导航。

本仓库导航模式的硬依赖产物有两份（必须同一 `map` 坐标系对齐）：
- 3D 先验点云：给 `rc26_localization` 用（`prior_pcd_file:=...`）
- 2D 栅格地图：给 Nav2 `map_server` 用（`map:=...` 指向 `map.yaml`）

建图模式约定（对应 `src/rc26_bringup/launch/localization.launch.py`）：
- `slam:=true` 时不会启动 `rc26_localization`，而是发布静态 `map->odom=identity`
- 这意味着建图阶段 `map` 就等于 Point-LIO 的 `odom`（所有点位录制也以此为准）

前置条件：
- 先完成第 4.1 节，确保 `/registered_scan` 与 `odom->base_link` 稳定
- 具备低速遥控（建议先按第 3 章跑通底盘闭环）

#### 4.3.1 建图启动（推荐用 bringup 的建图模式）

1) 创建 Point-LIO 的输出目录（Point-LIO 会把 PCD 写到源码目录下的 `PCD/`）：
```bash
mkdir -p /home/potato/RC_2026/src/point_lio/PCD /home/potato/RC_2026/src/point_lio/Log
```

2) 启动建图（关键点：`slam:=true` + `use_decision:=false`）：
```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=true \
  use_decision:=false \
  use_rviz:=true \
  point_lio_config_file:=/home/potato/RC_2026/src/point_lio/config/mid360_mapping_save.yaml
```

说明：
- `point_lio_config_file` 这份配置打开了 `pcd_save.pcd_save_en=true`（只建议建图时用）
- 建图开始后的前 3~5 秒尽量静止，给 IMU 初始化留窗口

3) 建图过程中建议用 RViz2 做“即时质量验收”：
- 点云是否随机器人运动连续
- TF 是否稳定（无明显跳变）
- 如果出现大幅漂移，优先回到第 3 章与第 4.1 节排查 IMU/时间戳/外参

#### 4.3.2 建图路径建议（12m x 12m 场地）

建议至少包含：
- 沿场地外圈绕 2 圈（不同半径）
- 穿越关键通道（走廊/门洞/隔板边缘）
- 梅林区：重点覆盖方块边缘与台阶附近（对定位与地形语义都有帮助）

#### 4.3.3 保存 3D 先验点云（prior PCD）

停止建图：对 bringup 进程 `Ctrl-C` 正常退出。

Point-LIO 在退出时会生成：
- `/home/potato/RC_2026/src/point_lio/PCD/scans.pcd`

检查文件是否生成：
```bash
ls -lh /home/potato/RC_2026/src/point_lio/PCD/scans.pcd
```

若未生成：
- 先确认你使用了 `point_lio_config_file:=.../mid360_mapping_save.yaml`
- 再确认 `src/point_lio/PCD/` 目录存在且可写

### 4.4 从 PCD 生成 Nav2 2D 栅格地图（map.yaml + map.pgm）

本仓库提供了一个“PCD -> Nav2 map”工具脚本（不依赖 VLM/LLM）：
- `tools/map/pcd_to_nav2_map.py`

示例（推荐把产物放到仓库的 `.rc26/maps/` 下，避免和源码混在一起）：
```bash
mkdir -p /home/potato/RC_2026/.rc26/maps/arena_v1

python3 tools/map/pcd_to_nav2_map.py \
  --pcd /home/potato/RC_2026/src/point_lio/PCD/scans.pcd \
  --out_dir /home/potato/RC_2026/.rc26/maps/arena_v1 \
  --map_name arena \
  --copy_pcd \
  --resolution 0.05 \
  --margin 0.5 \
  --obstacle_min_h 0.20 \
  --obstacle_max_h 2.00
```

输出：
- `/home/potato/RC_2026/.rc26/maps/arena_v1/arena.yaml`（给 `map:=...` 用）
- `/home/potato/RC_2026/.rc26/maps/arena_v1/arena.pgm`
- `/home/potato/RC_2026/.rc26/maps/arena_v1/prior.pcd`（给 `prior_pcd_file:=...` 用）

常见调参（只影响 2D map，不影响 prior.pcd）：
- 地图“全黑/障碍太多”：把 `--obstacle_min_h` 调大，或把 `--obstacle_min_hits` 调大
- 地图“几乎全灰(unknown)”：把 `--ground_band_m` 调大一点，或手动指定 `--ground_z`

### 4.5 建图后必须做的“标定写回”（否则导航模式可能直接起不来）

#### 4.5.1 标定 retry zone（rc26_localization 强校验）

原因：
- `src/rc26_bringup/config/localization.yaml` 默认 `competition_mode=true`
- `rc26_localization` 在比赛模式下要求 `retry_zone_enable=true` 且 `retry_zone_x/y` 不能是占位值 `(0,0)`，否则会拒绝启动

做法（在建图模式下即可完成，不需要 rc26_localization）：
1) 把机器人开到“重试区中心点”
2) 读取一次 `map->base_link`：
```bash
timeout 2s ros2 run tf2_ros tf2_echo map base_link
```
3) 将输出的 `Translation: [x, y, z]` 中的 `x/y` 写入：
- `src/rc26_bringup/config/localization.yaml`
  - `retry_zone_x: ...`
  - `retry_zone_y: ...`

#### 4.5.2 点位录制（强烈建议在建图后立刻做）

建图模式下已经有稳定的 `map` 坐标系（静态 `map->odom`），因此可以直接用第 9.2.1 节的 `point_recorder.py` 录制：
- 梅林 12 格中心点 `mf_grid_1..12`
- `mf_entry/mf_exit` 等关键点

---

## 5. 定位：发布 map->odom（导航模式必须有）

对应执行方案：
- 定位：`MVP技术方案/定位/执行方案/执行方案5.md`

### 5.1 准备先验点云与地图（必做）

必须提供：
- 2D 栅格地图：传给 `bringup.launch.py map:=...`
- 3D 先验点云 PCD：传给 `bringup.launch.py prior_pcd_file:=...`

说明：
- `src/rc26_bringup/map/default.yaml` 为占位图
- `src/rc26_bringup/pcd/` 默认没有 `default.pcd`，必须传真实文件路径
- 若你当前还没有这两份产物，先按第 4.3/4.4 节完成建图再继续

### 5.2 单独测试 localization（不启动 Nav2）

```bash
ros2 launch rc26_bringup test_localization.launch.py prior_pcd_file:=/path/to/prior.pcd
ros2 run tf2_ros tf2_echo map odom
```

若 `rc26_localization` 启动即退出，常见原因：
- `competition_mode=true` 但 `retry_zone_x/y` 仍是占位值 `(0,0)`：按第 4.5.1 节先标定写回

---

## 6. 地面高度与地形语义（台阶/跌落/延迟干预）

对应执行方案：
- 地形跳跃（落地状态）：`MVP技术方案/地形跳跃/执行方案/执行方案1.md`
- 定位执行方案5中的 Terrain/Latency/IPC 干预：`MVP技术方案/定位/执行方案/执行方案5.md`

### 6.1 base_ground_estimator 验收（落地/举起/稳定门控）

启动（bringup 会自动带起；也可单起）：
```bash
ros2 launch rc26_base_ground base_ground_estimator.launch.py
```

验收话题（地形跳跃执行方案1）：
```bash
ros2 topic list | rg base_ground || true
ros2 topic echo /base_ground/stable_terrain --once
ros2 topic echo /base_ground/stable_operation --once
ros2 topic echo /base_ground/is_lifted --once
```

参数验收（执行方案要求）：
```bash
ros2 param get /base_ground_estimator tol_level_m
ros2 param get /base_ground_estimator h0_calibration_samples
ros2 param get /base_ground_estimator lift_thresh_m
```

### 6.2 rc26_terrain 验收（obstacles/drop 输出与 diagnostics）

启动（bringup 会自动带起；也可单起）：
```bash
ros2 launch rc26_terrain terrain_semantic.launch.py use_sim_time:=false
```

检查输出：
```bash
ros2 topic hz /terrain_obstacles
ros2 topic hz /terrain_drop
ros2 topic echo /terrain_obstacles --once
ros2 topic echo /terrain_drop --once
ros2 topic echo /diagnostics --once
```

---

## 7. Nav2 与控制器（先用手动 goal 验证，再上决策）

对应执行方案：
- 控制器：`MVP技术方案/控制器/执行方案1.md`

### 7.1 最小 Nav2 测试（不跑决策）

```bash
ros2 launch rc26_bringup test_omni_controller.launch.py
```

手动发一个导航目标：
```bash
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: 'map'}, pose: {position: {x: 1.0, y: 0.0}}}}"
```

验收（控制器执行方案1的关键点）：
```bash
ros2 topic hz /cmd_vel
```
期望：`/cmd_vel` 约 100Hz（以实际算力与负载为准，明显偏离需先查 CPU/TF/costmap）

---

## 8. bringup 一键启动导航系统（不含底盘串口与机构串口）

说明：
- `src/rc26_bringup/launch/bringup.launch.py` 覆盖：里程计链 + 定位 + base_ground + terrain + Nav2 + keepout 相关 + nav_mode_manager +（可选）decision
- 但它不启动：`rc26_merge_odom`（底盘串口闭环）与 `rc26_mechanism`（机构 action server）

推荐以“分进程”方式启动，便于定位问题：

1) 底盘闭环（第3章）先启动并确认可控

2) 启动 bringup（建议先关决策）：
```bash
ros2 launch rc26_bringup bringup.launch.py \
  use_sim_time:=false \
  use_decision:=false \
  map:=/path/to/map.yaml \
  prior_pcd_file:=/path/to/prior.pcd
```

3) 启动机构（决策需要 action server；整体架构执行方案1已将决策侧串口移除）：
```bash
ros2 launch rc26_mechanism mechanism.launch.py serial_port:=/dev/ttyUSB2 serial_baud:=1000000
```

---

## 9. 最后测决策（行为树）：先验证点位与模式，再跑主树

对应文档：
- 整体架构（P0-P4 已落地，决策去串口 + 机构 action 化）：`MVP技术方案/整体架构/执行方案/执行方案1.md`
- 梅林决策与行为树说明：`MVP技术方案/决策/梅林决策方案.md`、`MVP技术方案/决策/梅林区行为树使用说明.md`

### 9.1 先做“决策可发目标”的前置检查（Keepout Gate 会强制）

决策侧 Keepout Gate 默认开启（`src/rc26_decision/config/decision_params.yaml`）：
- 若没有 `/costmap_filter_info` 或 `/kfs_filter_mask`，决策会拒绝发 goal

因此在启动决策前先确认：
```bash
ros2 topic echo /costmap_filter_info --once
ros2 topic echo /kfs_filter_mask --once
ros2 service list | rg set_nav_mode || true
```

Keepout 额外必查（定位执行方案5的“防呆项”）：
- `src/rc26_kfs_keepout/config/mf_grid_layout.yaml` 的 `meta.team` 必须与决策参数 `team` 一致，否则 `kfs_block_fuser` 会判定 team mismatch 并禁用 keepout（同时请求 safe mode）

### 9.2 点位与 profile 必须对齐（这是最容易踩坑的地方）

1) `set_nav_mode` 支持的 profile 列表以 `src/rc26_nav_mode_manager/config/nav_profiles.yaml` 为准，例如：
- `safe`
- `normal`
- `stair_up` / `stair_down`
- `mf_traverse` / `mf_exit` / `mf_approach`

2) `waypoints_<team>.yaml` 中每个点的 `nav_profile` 必须是上述之一
- 当前 `src/rc26_decision/config/waypoints/waypoints_red.yaml` 与 `src/rc26_decision/config/waypoints/waypoints_blue.yaml` 含 `nav_profile: "mf_safe"` 的点位，若未做别名处理会导致 `set_nav_mode` 返回失败

3) `mf_tree.xml` 会引用这些点位名（必须存在于决策加载的 waypoints 文件中）：
- `mf_entry`
- `mf_entry_back`
- `mf_exit`
- `mf_grid_1` ... `mf_grid_12`

若点位缺失或 `nav_profile` 不存在，行为树会直接 `FAILURE`，表现为决策启动后很快失败退出。

### 9.2.1 点位现场录制（推荐做法，避免手填坐标）

工具脚本：`src/rc26_decision/scripts/point_recorder.py`

前置条件：
- 已经有稳定的 TF `map->base_link`（定位模块已工作）

启动录制（会进入交互式 REPL）：
```bash
python3 src/rc26_decision/scripts/point_recorder.py --ros-args -p team:=blue
```

在 REPL 中建议至少录制（示例，按现场队伍与策略调整 `nav_profile`）：
```text
record mf_entry default mf_approach
record mf_entry_back default normal
record mf_exit default mf_exit

# 12 个格子点（NavToMerlinGrid 会找 mf_grid_1..12）
record mf_grid_1 default mf_traverse
record mf_grid_2 default mf_traverse
...
record mf_grid_12 default mf_traverse

save
quit
```

输出文件默认在：
- `~/RC_2026/.rc26/waypoints/waypoints_<team>.yaml`

### 9.3 建议的“先局部后整体”决策测试步骤

1) 先只跑梅林子树（便于聚焦 MF 调试）

推荐直接跑可执行体（便于同时传 `team/waypoints_file` 等参数）：
```bash
ros2 run rc26_decision decision_node --ros-args \
  --params-file /home/potato/RC_2026/src/rc26_decision/config/decision_params.yaml \
  -p tree_file:=mf_tree.xml \
  -p enable_vision:=false \
  -p team:=blue \
  -p waypoints_file:=/home/potato/RC_2026/.rc26/waypoints/waypoints_blue.yaml
```

2) 再跑主树（MC + MF + Combat 串行）：
```bash
ros2 run rc26_decision decision_node --ros-args \
  --params-file /home/potato/RC_2026/src/rc26_decision/config/decision_params.yaml \
  -p tree_file:=main_tree.xml \
  -p enable_vision:=false \
  -p team:=blue \
  -p waypoints_file:=/home/potato/RC_2026/.rc26/waypoints/waypoints_blue.yaml
```

3) 用全链路验收探针做一次 60s 检查（建议在跑决策前先 PASS，再跑决策）：
```bash
python3 src/rc26_bringup/scripts/r2_acceptance_probe.py --duration_sec 60 --expect_obstacle false
```

### 9.4 视觉（可选，等基础导航稳定后再开）

决策独立启动可通过 launch arg 开启视觉推理：
```bash
ros2 launch rc26_decision decision.launch.py enable_vision:=true
```

RealSense 建议先单起相机验证：
```bash
ros2 launch rc26_bringup realsense_d455.launch.py
```

---

## 10. 离线 AI 调参与诊断（仅 `extract/propose/apply`）

对应执行方案：
- `MVP技术方案/全自动调参2/执行方案/执行方案1.md`

### 10.0 当前实现边界（重要）

- `` 已收敛为**仅离线管线**：`extract -> propose(VLM+LLM) -> apply`。
- 主入口是 `python3 -m offline.cli`（在 `src/` 目录执行）。
- 不再提供 ROS2 在线节点与 launch 入口。
- 不再支持旧的 6 个非目标子命令。

### 10.1 一次性准备（离线管线）

```bash
cd /home/potato/RC_2026
python3 -m venv tuning/.venv_offline
source tuning/.venv_offline/bin/activate
pip install -U pip
pip install -r tuning/requirements/offline_requirements.txt
```

可选：提前下载 embedding 模型（FAISS 检索更稳）：
```bash
python3 src//scripts/fetch_embed_model.py \
  --model-name intfloat/multilingual-e5-small \
  --out-dir tuning/models/intfloat__multilingual-e5-small
```

### 10.2 AI 环境变量（硬依赖，缺失即失败）

统一环境变量（OpenAI-compatible `/v1/chat/completions`）：
```bash
export SLAM_AGENT_API_KEY=<non-empty>
export SLAM_AGENT_AI_ENDPOINT=http://127.0.0.1:8000/v1
export SLAM_AGENT_VLM_MODEL=<your_vlm_model>
export SLAM_AGENT_LLM_MODEL=<your_llm_model>
```

可用 `SLAM_AGENT_AI_MODEL` 作为 `VLM/LLM` 的统一兜底，但最终必须同时解析出 `vlm_model` 和 `llm_model`。
任意一个缺失时，`extract/propose/apply` 都会直接退出（返回码 `2`），不再降级。

### 10.3 分步执行（推荐）

```bash
cd /home/potato/RC_2026/src/

# Phase 1: 提取
python3 -m offline.cli extract --bag /abs/path/to/your.bag.mcap --exp-id exp_001

# 取最新 run_dir
run_dir=$(ls -dt /home/potato/RC_2026/tuning/experiments/exp_001/runs/run_* | head -n 1)
echo "$run_dir"

# Phase 2: 提案（模型与 endpoint 只从环境变量读取）
python3 -m offline.cli propose --run-dir "$run_dir"

# Phase 3: 人工审批写入
proposal=$(ls -dt "$run_dir"/bundles/bundle_*/llm_proposal.json | head -n 1)
python3 -m offline.cli apply --proposal "$proposal" --dry-run
python3 -m offline.cli apply --proposal "$proposal"
```

说明：
- `propose` 不再接受 `--endpoint / --vlm-model / --llm-model`。
- 任一 bundle 缺少必需 PNG、VLM/LLM HTTP 错误、或响应非 JSON，`propose` 会立即退出（返回码 `1`）。

### 10.4 产物与联动

产物位置：
- `tuning/experiments/exp_001/runs/run_<ts>/health_card.jsonl`
- `tuning/experiments/exp_001/runs/run_<ts>/bundles/bundle_*/health_card.json`
- `tuning/experiments/exp_001/runs/run_<ts>/bundles/bundle_*/evidence_bundle.json`
- `tuning/experiments/exp_001/runs/run_<ts>/bundles/bundle_*/vlm_report.json`
- `tuning/experiments/exp_001/runs/run_<ts>/bundles/bundle_*/llm_proposal.json`

`param_overrides.yaml` 仍由以下 launch 自动加载：
- `src/rc26_bringup/launch/localization.launch.py`
- `src/rc26_bringup/launch/odometry.launch.py`

上线后抽查参数是否生效（按 proposal 参数名替换）：
```bash
ros2 param get /localization sc_sim_threshold
ros2 param get /point_lio filter_size_surf
```

> 若有 namespace，请用实际节点全名（先 `ros2 node list`）。

---

## 11. 一键“全栈实机联调”参考流程（从零到决策）

按顺序开 4 个终端（每个终端先 `source install/setup.bash`）：

1) 终端 A：底盘闭环（必须）
```bash
ros2 launch rc26_merge_odom merge_odom.launch.py \
  use_can_odom:=false \
  imu_port:=/dev/ttyACM0 \
  feedback_serial_port:=/dev/ttyUSB0 \
  target_serial_port:=/dev/ttyUSB1
```

2) 终端 B：导航系统（先关决策）
```bash
ros2 launch rc26_bringup bringup.launch.py \
  use_decision:=false \
  map:=/path/to/map.yaml \
  prior_pcd_file:=/path/to/prior.pcd
```

3) 终端 C：机构 action server（决策需要）
```bash
ros2 launch rc26_mechanism mechanism.launch.py serial_port:=/dev/ttyUSB2 serial_baud:=1000000
```

4) 终端 D：决策（先 MF 子树，确认没问题再 main_tree）
```bash
ros2 launch rc26_decision decision.launch.py tree_file:=mf_tree.xml --ros-args -p team:=blue
```

若上述 4 步任一步失败，不要直接改一堆参数，按第 3-9 章逐模块回退验证。
