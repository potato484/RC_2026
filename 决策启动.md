# 决策启动
本文档对应 R2 的自动决策链路，核心模块为 `rc26_decision + rc26_kfs_keepout`。
决策模块建立在定位、导航和机制状态都已经正常的前提上，建议始终放在后段联调。

## 使用前提

- 已完成编译，并存在 `install/setup.bash`
- Nav2 生命周期全部为 `active`
- `map -> odom` TF 正常
- `/mechanism/state` 正常发布
- 路点文件存在于 `src/rc26_decision/config/waypoints/`
- 若启用 KFS keepout，对应心跳和过滤链路正常

## 编译

```bash
colcon build --symlink-install --parallel-workers 3 \
  --packages-select rc26_decision rc26_kfs_keepout \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

## 常用启动方式

### 1. 单独启动决策节点，不带视觉

```bash
ros2 launch rc26_decision decision.launch.py enable_vision:=false
```

### 2. 通过整车 bringup 启动自动链路

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=false \
  use_decision:=true \
  visualization_backend:=rviz \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

说明：

- 这是最接近实战的自动模式入口
- 如需浏览器端查看，可把 `visualization_backend:=rviz` 改为 `foxglove`

### 3. 启动带视觉的决策

```bash
ros2 launch rc26_decision decision.launch.py enable_vision:=true
```

如需显式覆盖视觉配置：

```bash
ros2 launch rc26_decision decision.launch.py \
  enable_vision:=true \
  vision_config_file:=$(ros2 pkg prefix rc26_vision)/share/rc26_vision/config/vision_models.yaml
```

## 最小验收

```bash
ros2 node list | grep rc26_decision
ros2 node info /rc26_decision
ros2 topic echo /navigate_to_pose/_action/status
ros2 node list | grep kfs
ros2 topic echo /kfs_filter_mask --once
ros2 topic hz /kfs_keepout_heartbeat
ros2 param get /controller_server FollowPath.v_linear_max
```

预期：

- 决策节点在线且无 Fatal 报错
- 行为树能持续下发导航目标
- `kfs_filter_mask` 正常更新
- 决策切换速度档位后，`FollowPath.v_linear_max` 会跟着变化

## 调试日志

```bash
ros2 launch rc26_decision decision.launch.py --ros-args --log-level rc26_decision:=debug
```

说明：需要查看行为树 tick 节奏和条件分支时，再打开 `debug` 级别日志。
