# 前端行为树编辑器联网调研

日期：2026-03-29

目标：评估“在 Web 端通过拖拽方式编辑行为树，并反向生成兼容 BehaviorTree.CPP 的本地 XML”的可行实现路线，并给出对当前仓库更稳妥的改进建议。

## 范围约束

本文只讨论以下目标：
- 以当前仓库的 `merlin-bt-visualizer` 为基础，扩展为本地前端行为树编辑器。
- 通过拖拽、连线、属性编辑的方式生成兼容 BehaviorTree.CPP 的 XML。
- 以浏览器内本地前端实现为主。
- 优先满足当前仓库里 `src/rc26_decision/behavior_trees/*.xml` 的实际结构和用法。

本文明确不讨论：
- Groot2 兼容。
- `TreeNodeModel` / `TreeNodesModel` 输出。
- Tauri / Electron / companion service 作为主方案。

原因：
- 你的当前目标不是对接官方编辑器生态，而是先把“本地前端拖拽编辑并稳定生成 XML”做出来。
- 当前仓库前端已经是 Vite + React + React Flow，本地前端延伸成本最低。

## 结论摘要

事实：
- BehaviorTree.CPP 兼容的关键不是“画布长什么样”，而是最终输出的 XML 语义是否正确。
- 当前仓库前端的行为树数据是展示态，不是可逆编辑态。现有解析会展开 `SubTree`，也会压缩部分 `Decorator`，因此不能直接回写成正式 XML。
- 当前仓库的行为树 XML 已经覆盖了后续编辑器必须支持的关键语义：`<root BTCPP_format="4">`、`main_tree_to_execute`、`include`、多棵 `<BehaviorTree>`、`SubTree ID`、`_autoremap`、黑板变量 `{key}`、以及各种任意属性动作节点。
- 纯 Web 前端可以稳定做到“拖拽编辑 + 生成 XML + 导出下载”。
- 如果希望浏览器里直接写回本地工程文件，当前最现实的前端方案是 Chromium 环境下使用 File System Access API；它可行，但有安全上下文、用户手势和浏览器支持范围限制。

结论：
- 对当前仓库，最稳妥的路线不是“在现有展示节点上做增删改再导出”，而是新建一套专门面向 XML 回写的编辑模型：
  1. 语义 AST 作为真源。
  2. React Flow 只作为画布投影和交互层。
  3. XML 解析器与序列化器独立实现，且不复用当前展示态解析结果。
  4. 落盘分两步走：
     - 一期先做“导出 XML 文件”。
     - 二期再做“Chromium + File System Access API 直接写回本地 XML”。

一句话结论：
- 这件事可行，而且适合在当前前端里做，但前提是你把“展示树”升级为“可逆的语义树”，而不是直接在现有 `nodes/edges` 上拼字符串。

## 当前仓库对方案的真实约束

### 1. 现有前端是只读可视化，不是编辑器

基于本地代码：
- `merlin-bt-visualizer` 当前直接通过 `?raw` 导入 `mf_tree.xml`、`mc_tree.xml`、`combat_tree.xml`。
- `useStore.ts` 保存的是当前区域的解析结果和运行态，不是原始 XML 结构。
- `TreeVisualizer.tsx` 只负责展示、点击、双击折叠。

结论：
- 当前前端可以复用技术栈和部分 UI 骨架，但不能直接复用其数据模型作为编辑器真源。

### 2. 当前解析逻辑不可逆

基于 `merlin-bt-visualizer/src/utils/btParser.ts`：
- `parseBTXml()` 会把多个 `<BehaviorTree>` 解析成前端树结构。
- `SubTree` 会被直接展开进入画布。
- `compressTree()` 会把 `Decorator` 压缩并附着到实际执行节点。
- 前端标签还会被中文翻译和参数描述增强。

结论：
- 当前 `BTNode` 只适合“看”，不适合“改后再写回”。
- 如果拿它直接生成 XML，会丢失至少以下语义：
  - 原始 `SubTree` 边界
  - 装饰器真实层级
  - 节点原始标签名与原始属性
  - 原始文件组织方式

### 3. 当前 XML 不是单一树，而是多文件、多树组织

基于本地 XML：
- `main_tree.xml` 使用 `<include path="..."/>` 引入三个区域文件。
- `main_tree.xml` 的 `<root>` 带有 `main_tree_to_execute="MainTree"`。
- `mf_tree.xml` 内部包含多棵 `<BehaviorTree>`，由 `SubTree ID="..."` 串联。
- 大量节点依赖任意属性，而不是固定表单字段，例如：
  - `num_attempts`
  - `delay_msec`
  - `target_name`
  - `grid_id`
  - `code`
  - `_autoremap`
  - 黑板引用 `{next_action}`、`{target_grid}`

结论：
- 编辑器不能只建“单棵可视化树”。
- 至少要支持：
  - 文档级 root 属性
  - include 列表
  - 多个 BehaviorTree 定义
  - 子树引用
  - 任意属性编辑

## 与目标直接相关的技术判断

### 1. 是否必须兼容 Groot2

结论：不必须，而且当前阶段不该纳入主方案。

理由：
- 你的根本需求是前端拖拽生成兼容 BehaviorTree.CPP 的 XML，而不是兼容第三方编辑器。
- 当前仓库自己就没有围绕 Groot2 建立工作流。
- 若强行把 Groot2 模型要求提前引入，会增加实现复杂度，但并不直接提高“本地前端生成 XML”的成功率。

因此：
- 一期只要保证输出 XML 能被 BehaviorTree.CPP 正常消费即可。
- 输出采用当前仓库已经在用的紧凑语法即可，无需为了外部编辑器改成显式语法。

### 2. 是否适合纯前端实现

结论：适合，但要分清“生成 XML”和“直接写回本地文件”是两件事。

事实：
- React Flow 官方适合做可视化节点编辑。
- 浏览器前端生成 XML 字符串没有问题。
- 浏览器直接写本地文件需要 File System Access API，且主要依赖 Chromium。

所以要拆成两层：
- 生成层：
  - 完全可由纯前端实现。
- 写盘层：
  - 通用方案是导出下载。
  - 更顺手的本地方案是 Chromium + File System Access API。

### 3. React Flow 是否足够

结论：足够做编辑器交互层，但不够做语义真源。

事实：
- React Flow 官方导出的是图状态对象，不是领域语义模型。

因此：
- 继续使用 `@xyflow/react` 是合理的。
- 但必须引入独立的 `EditorDocument` / `EditorTree` / `EditorNode` 语义模型。

## 推荐实现路线

### 主路线：语义 AST + 画布投影 + XML 序列化

推荐架构：

1. `EditorDocument`
- 表示一个 XML 文档。
- 包含：
  - `rootAttributes`
  - `includes`
  - `trees`

2. `EditorTree`
- 表示一个 `<BehaviorTree ID="...">`。
- 包含：
  - `id`
  - `rootNode`

3. `EditorNode`
- 表示一个真实 XML 节点。
- 最少包含：
  - `id`
  - `tagName`
  - `attributes`
  - `children`
  - `commentBefore`

4. `FlowLayoutState`
- 专门保存画布信息：
  - `position`
  - `collapsed`
  - `selected`
  - `viewport`

关键原则：
- 语义结构和画布结构分离。
- XML 只从 `EditorDocument` 生成。
- React Flow 节点从 `EditorDocument + FlowLayoutState` 投影得到。

### 为什么不能直接复用当前 `BTNode`

当前 `BTNode` 缺以下关键能力：
- 没有保留原始 `tagName` 全量语义。
- 没有保留原始属性表。
- 没有保留文档级 `root` 属性和 `include`。
- `SubTree` 已被展开。
- 装饰器层级已被压缩。

所以：
- 现有 `BTNode` 适合可视化。
- 新的 `EditorNode` 才适合编辑和回写。

## 面向当前仓库的最小可行模型

### 1. 文档层模型

建议：

```text
EditorDocument
  rootAttributes: Record<string, string>
  includes: string[]
  trees: EditorTree[]
```

为什么必须有这一层：
- `main_tree.xml` 需要保留 `main_tree_to_execute`。
- `main_tree.xml` 需要保留 `include`。
- `mf_tree.xml` 这类文件需要支持一份 XML 中存在多棵 `<BehaviorTree>`。

### 2. 树层模型

建议：

```text
EditorTree
  id: string
  name?: string
  rootNode: EditorNode
```

说明：
- `BehaviorTree` 本身是一个定义，不应和控制节点混为一体。
- 画布上可以只显示 `rootNode`，但序列化时必须恢复 `<BehaviorTree ID="...">` 包裹层。

### 3. 节点层模型

建议：

```text
EditorNode
  id: string
  tagName: string
  attributes: Record<string, string>
  children: EditorNode[]
  uiType: 'control' | 'decorator' | 'leaf' | 'subtree'
```

说明：
- `tagName` 直接对应 XML 标签，如 `Sequence`、`Fallback`、`NavToSmartPoint`、`Script`。
- `attributes` 必须允许任意 key-value，而不是预先写死。
- `uiType` 只用于前端分组和样式，不参与最终 XML 语义。

## XML 导入和生成策略

### 1. 导入策略

建议重新实现一套 `xmlToEditorDocument()`：
- 使用 `DOMParser` 读取原始 XML。
- 保留：
  - `root` 属性
  - `include`
  - `BehaviorTree ID`
  - 每个节点原始标签名
  - 每个节点原始属性
  - 子节点顺序
- 不做：
  - `SubTree` 展开
  - 中文翻译写回
  - 装饰器压缩
  - 标签改写

结论：
- 编辑器导入器必须“保守”，只做结构化，不做展示增强。

### 2. 生成策略

建议实现 `editorDocumentToXml()`：
- 输出当前仓库习惯使用的紧凑语法。
- 保持节点 children 顺序。
- 所有属性按原值输出。
- `SubTree` 保持为 `<SubTree ID="..." _autoremap="true"/>` 这种原生形式。
- `Script`、`ScriptCondition` 等带 `code` 的节点直接透传属性。

建议：
- 一期不追求注释和属性顺序百分百保真。
- 一期优先追求“语义等价”和“可再次导入”。

### 3. 校验策略

导出前至少检查：
- `root` 是否含 `BTCPP_format`。
- 每棵 `BehaviorTree` 是否有 `ID`。
- 控制节点是否至少有一个子节点。
- 装饰器节点是否恰好有一个子节点。
- `SubTree ID` 是否非空。
- 黑板变量是否仍保持 `{key}` 形式。
- 根树是否可达。

说明：
- 这些校验比追求外部编辑器兼容更重要。
- 目标是让生成 XML 可以被 BehaviorTree.CPP 正常消费。

## 前端交互层建议

### 1. 节点面板不要依赖运行时导出模型

因为你已经明确不考虑 Groot2，所以当前阶段不需要引入 `TreeNodeModel`。

更实际的方案：
- 前端内置一个节点目录：
  - 控制节点
  - 装饰器节点
  - 通用叶子节点
- 其中“通用叶子节点”允许用户自定义 `tagName` 和属性。

这样做的好处：
- 立刻能覆盖当前仓库里的绝大多数节点。
- 不需要先改 C++ 侧增加模型导出链路。

### 2. 节点分类建议

建议至少内置这些组：

- 控制节点：
  - `Sequence`
  - `ReactiveSequence`
  - `Fallback`
  - `ReactiveFallback`

- 装饰器节点：
  - `RetryUntilSuccessful`
  - `KeepRunningUntilFailure`
  - `Delay`
  - `Inverter`
  - `ForceSuccess`
  - `ForceFailure`
  - `Repeat`

- 特殊节点：
  - `SubTree`
  - `Script`
  - `ScriptCondition`

- 通用叶子节点：
  - 用户自己填写 `tagName`
  - 例如 `NavToSmartPoint`、`GrabTip`、`SelectNextGrid`

### 3. 属性编辑建议

不要把属性表单完全写死。

建议：
- 所有节点统一支持“键值对属性编辑器”。
- 对常见节点再补体验增强：
  - `SubTree` 默认给 `ID`
  - `Delay` 默认给 `delay_msec`
  - `RetryUntilSuccessful` 默认给 `num_attempts`
  - `Script` / `ScriptCondition` 默认给 `code`

这样能兼顾：
- 当前仓库已有节点。
- 后续新增动作节点。
- 不必每新增一个 BT 节点就改前端一版。

### 4. 双视图建议

建议保留：
- 左侧拖拽编辑画布。
- 右侧实时 XML 预览。

理由：
- 用户能立刻判断前端生成结果是否符合自己预期。
- 调试成本明显低于黑盒式导出。

## 本地保存的现实方案

### 方案 A：导出下载

这是一期默认方案。

做法：
- 点击“导出 XML”后生成 Blob。
- 浏览器下载成 `.xml` 文件。

优点：
- 浏览器兼容最好。
- 不依赖额外权限。
- 最容易先做通。

缺点：
- 不能直接覆盖仓库里的源文件。

结论：
- 这是最适合一期验证编辑器正确性的方案。

### 方案 B：Chromium + File System Access API

这是二期增强方案。

做法：
- 用户打开 XML 文件或行为树目录。
- 前端保存句柄。
- 后续点击保存时直接写回本地文件。

优点：
- 更接近本地 IDE 体验。
- 仍然保持“纯前端 + 浏览器”路线。

缺点：
- 依赖 Chromium。
- 需要用户授权。
- 不是所有浏览器都可用。

结论：
- 如果你的使用环境就是本地开发机、Chrome/Edge，这条路线是值得做的。
- 它是当前“本地前端为主”前提下最实用的直接写盘方案。

## 分阶段落地建议

### 阶段 1：把“可逆模型”补出来

目标：
- 新建 `EditorDocument` / `EditorTree` / `EditorNode`。
- 新建保守型 XML 导入器。
- 新建 XML 序列化器。

完成标准：
- `mc_tree.xml`、`combat_tree.xml`、`mf_tree.xml` 能导入。
- 导出后再导入，结构保持稳定。

### 阶段 2：接入 React Flow 编辑能力

目标：
- 节点拖拽新增。
- 父子连接。
- 子节点排序。
- 属性编辑。
- `SubTree` 与多棵 `BehaviorTree` 编辑。

完成标准：
- 可以在前端手工构建一棵与当前 `mc_tree.xml` 等价的树并导出。

### 阶段 3：补本地保存体验

目标：
- 默认支持导出下载。
- Chromium 环境支持直接打开和保存 XML。
- 支持目录句柄，以便处理 `main_tree.xml + include` 多文件结构。

完成标准：
- 能从浏览器中修改本地 `behavior_trees/` 目录下的 XML 并保存。

## 对当前仓库最稳妥的改进建议

### 建议 1：不要在现有 `btParser.ts` 上硬补编辑能力

原因：
- 它的职责是“把 XML 压成适合展示的图”。
- 不是“把 XML 保真映射成可逆 AST”。

正确做法：
- 新增单独的编辑器解析器和序列化器。

### 建议 2：一期先只支持当前仓库实际出现的语义

优先支持：
- `root` 属性
- `include`
- `BehaviorTree`
- `SubTree`
- `Sequence`
- `Fallback`
- `ReactiveSequence`
- `ReactiveFallback`
- `RetryUntilSuccessful`
- `KeepRunningUntilFailure`
- `Delay`
- `Inverter`
- `ForceSuccess`
- `ForceFailure`
- `Script`
- `ScriptCondition`
- 任意动作叶子节点

理由：
- 这是最快闭环。
- 没必要一开始做成“通用 BT IDE”。

### 建议 3：先做语义正确，再做布局美观

优先级应该是：
1. 导入正确
2. 导出正确
3. 再优化画布交互和视觉细节

理由：
- 这个项目的核心价值是生成可用 XML，不是先做一个好看的编辑器壳子。

### 建议 4：以“单文件可逆”作为最小验收标准

建议先以 `mc_tree.xml` 为验收对象。

原因：
- 结构最简单。
- 没有 `include` 跨文件复杂度。
- 但已经覆盖了：
  - 控制节点
  - 装饰器
  - 叶子节点属性

之后再扩到：
- `combat_tree.xml`
- `mf_tree.xml`
- `main_tree.xml`

## 最终判断

在你设定的边界下，这件事是可行的，而且方向已经足够清晰：
- 不要为 Groot2 做设计。
- 不要先引入桌面壳。
- 不要复用当前展示态解析结果。
- 直接围绕 BehaviorTree.CPP XML 语义做一套可逆 AST。
- 一期先导出下载。
- 二期再用 File System Access API 提升本地保存体验。

如果继续推进实现，下一步最应该做的不是 UI，而是先定义三份东西：
- `EditorDocument` 类型定义
- `xmlToEditorDocument()` 导入器
- `editorDocumentToXml()` 序列化器

## 引用来源

官方文档：
- BehaviorTree.CPP XML schema: https://www.behaviortree.dev/docs/learn-the-basics/xml_format
- React Flow documentation and examples: https://reactflow.dev/examples/interaction/save-and-restore
- Context7 mirror for React Flow docs: https://context7.com/xyflow/react-flow/llms.txt
- Chrome File System Access API: https://developer.chrome.com/docs/capabilities/web-apis/file-system-access
- WICG File System Access explainer: https://github.com/WICG/file-system-access/blob/main/EXPLAINER.md

兼容性参考：
- Can I use, File System Access API: https://caniuse.com/file-system-access
