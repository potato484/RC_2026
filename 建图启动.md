# 建图启动
本文档对应 R2 的建图链路，核心模块为 `rc26_mid360_driver + rc26_point_lio + rc26_bringup`。
当前项目仅面向 R2 自动机器人，建图阶段推荐直接走 `rc26_bringup` 的统一入口。

## 使用前提

- 当前工作目录位于仓库根目录：`RC_2026`
- 已完成编译，并存在 `install/setup.bash`
- Mid-360 与机器人网络连通，默认雷达地址为 `192.168.1.140`
- `sysctl net.core.rmem_max` 不低于 `33554432`
- IMU 设备已接好，可在 `/dev/serial/by-id/` 下找到对应设备

## 编译

```bash
colcon build --symlink-install --parallel-workers 3 \
  --packages-select rc26_mid360_driver rc26_point_lio rc26_odom_interface rc26_sensor_scan rc26_lio_state_predictor rc26_bringup \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

## 常用启动方式

### 1. 纯建图最小链路，RViz2

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=true \
  pure_mapping_mode:=true \
  visualization_backend:=rviz \
  point_lio_profile:=mapping_dense
```

说明：

- 这是当前最推荐的建图入口
- 会跳过 `lio_state_predictor`、`rc26_terrain`、`rc26_decision` 和 `visualization_status`
- `mapping_dense` 会打开高密建图和 PCD 保存相关配置

### 2. 纯建图最小链路，Foxglove

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=true \
  pure_mapping_mode:=true \
  visualization_backend:=foxglove \
  point_lio_profile:=mapping_dense
```

浏览器连接地址：

```text
ws://127.0.0.1:8765
ws://<R2机器人IP>:8765
```

### 3. 完整建图链路

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=true \
  visualization_backend:=rviz \
  point_lio_profile:=mapping_dense \
  use_decision:=false
```

说明：

- 保留 `terrain`、`visualization_status` 等附加模块
- 适合需要同时看建图和其他联调状态的场景

### 4. 雷达能 ping 通但没有点云时自动恢复数据流

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=true \
  visualization_backend:=rviz \
  point_lio_profile:=mapping_dense \
  use_decision:=false \
  recover_mid360_stream:=true
```

### 5. 仅启动最小里程计链路

```bash
ros2 launch rc26_bringup odometry.launch.py \
  slam:=true \
  point_lio_profile:=mapping_dense \
  enable_lio_state_predictor:=false \
  point_lio_publish_odometry_without_downsample:=false
```

说明：

- 适合只看 `odometry`、`sensor_scan` 和 LIO 输出，不带整车其他模块
- 当前仓库推荐优先使用 `bringup.launch.py`；只有在确实需要最小链路时再用本命令

## 最小验收

```bash
ros2 topic hz /livox/lidar
ros2 topic hz /livox/imu
ros2 topic hz /state_estimation
ros2 topic hz /registered_scan
ros2 topic echo /laser_map_full --once --field header.frame_id
```

预期：

- `/livox/lidar` 稳定在 `10Hz`
- `/livox/imu` 稳定在约 `200Hz`
- `/state_estimation` 正常输出
- `/registered_scan` 和 `/laser_map_full` 都能看到建图结果

## 建图结束后的保存检查

```bash
ls ${RC26_WS:-$HOME/RC_2026}/src/rc26_point_lio/PCD/
```

预期：正常 `Ctrl+C` 退出后，目录下存在 `scans.pcd` 或 `scans_*.pcd`。

## 补充说明

- `visualization_backend` 推荐显式写成 `rviz`、`foxglove` 或 `none`，不再依赖旧的 `use_rviz`
- `point_lio_profile` 的正式选项为 `base | cruise_light | mapping_dense`
- 建图阶段如只关心录图与保存 PCD，可使用 `visualization_backend:=none use_rviz:=false` 进一步减负
