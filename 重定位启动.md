# 重定位启动

本文档对应 R2 的测试重定位链路，核心模块为 `rc26_localization`。重定位依赖先验点云，推荐先完成一轮建图，再回到本文件启动定位。

## 使用前提

- 已完成编译，并存在 `install/setup.bash`
- 至少有一份可用先验点云：
  - `src/rc26_point_lio/PCD/scans.pcd`
  - 或 `src/rc26_bringup/pcd/default.pcd`
- 上游雷达和里程计正常，`/odom` 可持续输出
- TF 链 `odom -> base_link` 正常

## 编译

```bash
MAKEFLAGS='-j2 -l2' colcon build --executor sequential --parallel-workers 1 \
  --packages-select rc26_localization rc26_bringup
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

## 常用启动方式

### 1. 通过整车 bringup 启动，推荐

```bash
ros2 launch rc26_bringup bringup.launch.py \
  slam:=false \
  use_decision:=false \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

说明：

- `slam:=false` 时会自动切到定位模式
- 若要使用自己刚建好的地图，把 `prior_pcd_file` 改成实际 `.pcd` 路径即可
- 本文档默认不带决策模块，先把定位链路单独跑通
- 当前 bringup 默认就是 headless，不再通过 launch 参数常驻 GUI

### 2. 单独启动定位节点

```bash
ros2 launch rc26_bringup localization.launch.py
```

说明：

- 适合只调 `rc26_localization` 自身
- 如果默认配置里的先验点云不是你当前要用的文件，先改参数文件或改为走整车 bringup

## 外部可视化

```bash
rviz2 -d "${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/rviz/navigation_default.rviz"
```

## 最小验收

```bash
ros2 run tf2_ros tf2_echo map odom
ros2 topic echo /localization/pose_with_cov --once
ros2 topic echo /localization/diagnostics --once
ros2 topic hz /control_state
ros2 topic echo /control_degraded
```

预期：

- `map -> odom` 持续存在
- `/localization/pose_with_cov` 能持续输出位姿和协方差
- `/localization/diagnostics` 中退化和特征值信息正常
- `/control_state` 频率约为 `200Hz`

## 常用在线调参

```bash
ros2 param set /localization gicp_optimizer_mode "gn_auto"
ros2 param set /localization robust_enable true
ros2 param set /localization huber_c 1.0
ros2 param set /localization hessian_degen_enable true
ros2 param set /localization hessian_lambda_hard 10.0
```

说明：

- 初值较好时优先 `gn_auto`
- 场上动态障碍较多时，可打开 Huber 鲁棒核
- 退化场景较多时，可显式打开 Hessian 退化检测
