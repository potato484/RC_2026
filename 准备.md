# RC26 离线调参数据准备清单（R2）

> 适用范围：`` 当前离线链路（`extract -> propose -> apply`）与 `tools/autotune` 离线评估。  
> 目标：一次性把“全面调参”所需数据准备齐，避免后续因数据缺失导致无法分析/无法复现。

---

## 1. 需要准备的数据（全面版）

| 类别 | 是否必需 | 用途 | 关键内容 |
|---|---|---|---|
| 运行 Bag 数据 | 必需 | 生成 health_card、bundle、VLM/LLM 提案 | 至少含 `/rosout`、`/registered_scan` |
| 运行配置快照 | 必需 | 复现同一工况与参数边界 | `param_constraints.yaml`、`localization.yaml`、`odom_interface.yaml`、`mid360.yaml`、`param_overrides.yaml` |
| 地图与先验 | 必需 | 解释重定位/漂移问题 | `map.yaml/pgm`、`prior.pcd`、坐标系约定 |
| 场景标签与事件标注 | 强烈建议 | 做“按症状调参”与回归对比 | 场景类型、地面条件、遮挡、绑架起止时间 |
| 硬件与环境元数据 | 强烈建议 | 区分“参数问题”与“硬件状态变化” | 电池电压、轮胎磨损、负载、温度、队伍/场地 |
| AI 推理配置 | 必需（提案阶段） | VLM/LLM 生成提案 | `SLAM_AGENT_*` 环境变量、模型版本、endpoint |
| RAG 语料（可选） | 可选 | 提高提案一致性 | 历史 `llm_proposal.json`、参数说明文档 |

---

## 2. Bag 数据要求（核心）

## 2.1 必需话题

- `/rosout`：必须包含 `localization` 节点输出的 `PERF_METRIC` 与 `RELOC_METRIC` 日志。
- `/registered_scan`：必须为 `PointCloud2`，且包含 `x/y/z` 浮点字段。

> 代码依据：  
> - 读取话题过滤：`src//offline/bag_reader.py`  
> - PERF/RELOC 解析：`src//offline/bag_reader.py`  
> - `localization` 输出日志：`src/rc26_localization/src/localization.cpp`

## 2.2 建议同时录制的话题（全面分析用）

- `/odom`
- `/odometry`
- `/state_estimation`
- `/tf`
- `/tf_static`
- `/cmd_vel`
- `/cloud_registered`
- `/livox/lidar`
- `/DM_IMU`（或你实际 IMU 话题）

这些不是当前离线提案硬依赖，但用于排查“指标异常根因”（传感器、TF、控制链）非常关键。

---

## 3. 采集前准备

## 3.1 固定版本与配置快照

每次采集前先固化：

```bash
cd /home/potato/RC_2026
git rev-parse HEAD
```

并备份以下文件到本次数据目录：

- `src//config/param_constraints.yaml`
- `src//config/param_overrides.yaml`
- `src/rc26_bringup/config/localization.yaml`
- `src/rc26_bringup/config/odom_interface.yaml`
- `src/point_lio/config/mid360.yaml`（或你实际使用配置）
- 地图与先验：`map.yaml/pgm`、`prior.pcd`

## 3.2 确认日志级别

`PERF_METRIC/RELOC_METRIC` 是 `INFO` 级日志；不要把 `localization` 日志级别调到 `WARN/ERROR`，否则 `/rosout` 中会缺关键指标。

---

## 4. 采集步骤（推荐流程）

## 4.1 启动系统

确保能稳定产出 `/registered_scan` 与定位日志（你可用 `bringup` 常规流程）。

## 4.2 开始录包

建议每个场景单独一个 bag，命名包含场景和时间戳。

```bash
mkdir -p /home/potato/RC_2026/tuning/datasets/raw
RUN_ID=$(date +%Y%m%d_%H%M%S)_scene_xxx

ros2 bag record -o /home/potato/RC_2026/tuning/datasets/raw/${RUN_ID} \
  /rosout \
  /registered_scan \
  /odom \
  /odometry \
  /state_estimation \
  /tf \
  /tf_static \
  /cmd_vel \
  /cloud_registered \
  /livox/lidar
```

> 如果你环境支持 mcap，可改为 `-s mcap`。

## 4.3 场景覆盖矩阵（全面调参建议）

每类场景至少 3~5 条 bag（每条 3~8 分钟）：

1. **稳定跟踪基线**：低速/中速巡航，无明显干扰。  
2. **动态遮挡**：人车穿行、局部遮挡，观察 ghosting。  
3. **高转向/急停急起**：考察 `norm_err_p95`、`dt_ms_p95`、跟踪稳定性。  
4. **颠簸/振动路段**：触发 IMU spike 相关症状。  
5. **绑架恢复**：手动搬离并放回，至少 5 次有效重定位事件。  
6. **对称区/易混淆区**：走廊、重复结构，考察 SC 误匹配。  
7. **长时运行**：单条 ≥20 分钟，观察热态漂移与稳定性。  
8. **不同负载/电量**：空载与实战载荷各一组，低电压段单独一组。

---

## 5. 采集后质量门禁（必须过）

## 5.1 话题存在性

```bash
ros2 bag info /path/to/bag | rg "/rosout|/registered_scan"
```

## 5.2 关键日志是否存在

```bash
timeout 20s ros2 topic echo /rosout | rg "PERF_METRIC|RELOC_METRIC"
```

（在线抽查；离线可直接跑 `extract` 验证。）

## 5.3 离线抽取可用性

```bash
cd /home/potato/RC_2026/src/
python3 -m offline.cli extract --bag /path/to/bag --run-dir /tmp/rc26_extract_check
```

检查产物：

- `/tmp/rc26_extract_check/health_card.jsonl`
- `/tmp/rc26_extract_check/bundles/bundle_*/health_card.json`
- `/tmp/rc26_extract_check/bundles/bundle_*/evidence_bundle.json`
- `/tmp/rc26_extract_check/bundles/bundle_*/bev_density.png` 等 4 张图（若该时段有 BEV 输出）

---

## 6. AI 提案阶段额外准备

在任何 `offline.cli` 子命令前都需要环境变量（当前硬依赖）：

```bash
export SLAM_AGENT_API_KEY=...
export SLAM_AGENT_AI_ENDPOINT=...
export SLAM_AGENT_VLM_MODEL=...
export SLAM_AGENT_LLM_MODEL=...
# 或统一用 SLAM_AGENT_AI_MODEL 兜底
```

未配置会直接退出码 `2`，不会降级。

`propose` 阶段对每个 bundle 要求 4 张 PNG 证据图，缺任意一张会直接失败退出。

---

## 7. 推荐目录结构（长期维护）

```text
tuning/datasets/
  raw/
    20260301_101500_scene_baseline/
  manifests/
    20260301_101500_scene_baseline.yaml
  snapshots/
    20260301_101500/
      git_head.txt
      localization.yaml
      odom_interface.yaml
      param_constraints.yaml
      param_overrides.yaml
      map.yaml
      prior.pcd
```

`manifests/*.yaml` 建议字段：

- `run_id`
- `bag_path`
- `scene_type`
- `map_version`
- `prior_pcd_version`
- `team`（red/blue）
- `battery_voltage_range`
- `payload`
- `surface_type`
- `kidnap_events`（时间段列表）
- `notes`

---

## 8. 一键检查清单（执行前勾选）

- [ ] Bag 含 `/rosout`、`/registered_scan`  
- [ ] `/rosout` 中能看到 `PERF_METRIC`、`RELOC_METRIC`  
- [ ] 每类场景至少 3 条可用 bag  
- [ ] 绑架/重定位场景至少 5 次有效事件  
- [ ] 配置快照、地图、先验文件已归档  
- [ ] `extract` 能成功产出 `health_card.jsonl` 与 bundles  
- [ ] AI 环境变量可用（提案阶段）  

