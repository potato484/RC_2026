# 变更说明

> 本文档说明当前本地版本相对于 GitHub 远端版本的全部改动，以及每项改动的目的和作用。

---

## 一、最重要的变化：新增了独立的机构控制模块

新增了一个叫做 `rc26_mechanism` 的全新模块，专门负责机器人机构（抓取端头、组装兵器、机构升降、旋转等）的硬件控制。

**原来的方式：** 决策模块（负责"决定做什么"的那部分程序）直接持有一根串口线，每次想让机构动，就直接往串口里写命令，然后等串口反馈"完成了"。这样决策和底层硬件是绑在一起的。

**现在的方式：** 决策模块完全不再碰串口。它只需要向新的 `rc26_mechanism` 模块发出请求（"帮我抓一个端头"），`rc26_mechanism` 自己去操作串口、等待硬件完成、然后把结果告诉决策模块。决策模块不需要关心底层细节。

**为什么这样做：** 这种分层架构让"想做什么"和"怎么做到"彻底分开。将来如果换了硬件、换了通信协议，只需要改 `rc26_mechanism` 内部，决策逻辑完全不用动。同时，ROS2 Action 这种通信方式本身支持超时、取消、状态反馈等机制，比原来裸串口更可靠。

---

## 二、决策模块大规模移除串口相关代码

紧跟上面的架构调整，决策节点内部做了大量清理：

- **心跳发送机制被整体移除。** 原来决策节点每隔一段时间会向串口发一个心跳包，检测连接是否正常。现在这件事交给 `rc26_mechanism` 模块统一管理，决策层不再参与。
- **串口断线重连的监控逻辑被移除。** 原来决策黑板（行为树节点之间共享的"记事本"）上有专门记录"串口重连中"、"串口重连失败"的标志，现在全部删掉了，不再需要。
- **几十个串口反馈的布尔标志被移除。** 原来每次机构完成一个动作，串口会回复一个信号，决策就在黑板上写一个"某某动作已完成=true"。这些标志现在全部被替换成直接读取 `MechanismState`（机构状态消息）中的实际状态字段，信息更准确、更实时。
- **串口配置参数标注为"兼容保留"。** 配置文件里原有的串口端口、波特率、心跳等参数没有删除，但注明了"P4之后不再由决策直接使用"，仅为防止配置文件报错而保留。

---

## 三、新增四种 ROS2 动作接口

为了让决策模块和机构模块之间能标准化通信，新增了四种"任务请求"格式（ROS2 Action 定义）：

- **GrabTip**：请求机构去抓一个端头，返回成功或失败。
- **AssembleWeapon**：请求机构完成兵器组装，返回成功或失败。
- **PlaceKFSGrid**：请求机构把 KFS 放到九宫格的指定位置，需要告知放哪个格子、哪一层。
- **ExecuteMechanism**：通用机构命令，可以携带任意命令编号和附加数据，适用于不需要专属格式的动作（如机构升降、旋转等）。

每种接口都支持：发出请求后可以等待、可以中途取消、会收到"成功/失败+错误码"的明确结果。

---

## 四、新增三种消息格式

- **MechanismState（机构状态）：** 机构模块持续向外广播当前的真实状态，包括：端头目前处于什么状态、霍尔传感器是否打开、当前锁住的是哪个端头槽、已成功组装的数量、上次出错的错误码。决策模块通过订阅这个消息来了解机构情况，不再依赖串口回调。
- **TipDetection（端头检测结果）：** 视觉系统检测到端头时发布，包含哪个端头（编号）、在地图上的三维坐标、置信度、深度值、两端头间距是否合理等信息。
- **TipDetectionArray（端头检测结果数组）：** 把一帧里所有检测到的端头打包在一起发布。

---

## 五、新增视觉端头定位节点

新增了一个叫做 `tip_localizer` 的视觉节点，专门用于识别端头架上的端头并确定其位置。

**它做什么：** 订阅相机的彩色图像和深度图像，用 YOLO 目标检测模型找到图中的端头，再结合深度信息把"像素坐标"转换成"机器人地图上的三维坐标"，最后发布出去供其他模块使用。

**额外检验：** 它还会检测两个相邻端头之间的间距是否符合预期，如果间距偏差太大就会标记为异常，防止误识别。

**目的：** 让机器人能够精确知道端头在哪里，从而可以自主导航过去抓取，而不需要依赖预先录入的固定坐标。

---

## 六、地形感知模块新增 KFS 占用融合

地形感知模块（负责识别周围有没有台阶、障碍、可爬坡区域的那部分）现在多了一项能力：感知梅林区九宫格里哪些格子已经被 KFS 占用了。

**做了什么：** 地形节点现在会订阅 KFS 状态消息，知道哪些格子有 KFS 之后，会把那些格子对应的地图区域标记为"障碍物点云"，以比普通障碍物更高的高度（抬高0.35米）发布出去，并赋予特殊的强度值（200）以便区分。

**配套加载了梅林区格子布局文件：** 程序启动时会尝试自动读取 `rc26_kfs_keepout` 包里的格子坐标配置，建立"格子编号→地图坐标"的映射表。

**为什么这样做：** 导航系统的代价地图会把障碍物点云转化为禁止区域，这样机器人在规划路径时就会自动绕开已放有 KFS 的格子，避免误撞。

**新增了诊断统计信息：** 现在地形节点发布的诊断消息里会额外显示当前帧检测到的障碍格、跌落格、可爬坡格、KFS占用格各自的数量，方便调试时快速了解当前感知状态。

**地形包内部做了代码结构拆分：** 原本所有逻辑都塞在一个大文件里，现在被拆成三个独立的子库：点云预处理（`point_cloud_preprocessor`）、格子评估（`grid_evaluator`）、安全守卫（`safety_guard`），各自职责清晰，便于单独维护和测试。

**配置文件新增分组参数：** 地形配置文件里新增了 `preprocessor.*`、`evaluator.*`、`safety.*` 三组带前缀的参数，与拆分后的子模块一一对应，替代原来扁平堆放的旧参数。旧参数暂时保留以兼容现有 launch 文件。

**新增格子评估单元测试：** 对 `grid_evaluator` 子库添加了自动化测试，可在编译时通过 `colcon test` 验证格子评估逻辑的正确性。

---

## 七、行为树动作节点全面从"裸串口"迁移到"ROS2 Action"

原来各个区域（武馆区 MC、梅林区 MF、对抗区 Combat）的行为树动作节点，内部都是：从黑板里取出串口对象、发命令、轮询一个布尔标志等完成。现在统一改为继承自新的 `BtActionNode` 基类，通过 ROS2 Action 客户端与机构模块通信。

受影响的节点包括：取端头、组装兵器、抓取 KFS、梅林区机构升/降、旋转、对抗区机构升、放置KFS到九宫格、放置KFS到地面。

**每个节点的改动模式一致：**
- 不再访问串口
- 启动时构建一个目标请求发给机构模块
- 等待结果到来（过程中可被行为树中断/取消）
- 根据结果的成功/失败决定节点状态

---

## 八、决策黑板键重构

行为树节点之间共享的"记事本"（黑板）做了调整：

- **移除：** 原来大量的动作完成标志（grab_tip_done、assemble_done、mech_up_merlin_done 等十几个字段）全部删除。
- **移除：** 串口重连相关的标志（cmd_serial_reconnecting、cmd_serial_reconnect_failed）。
- **新增：** 来自 MechanismState 消息的三个字段——当前端头状态编号、霍尔传感器开关状态、当前锁定的端头槽位号。
- **调整：** `action_fail`（动作失败标志）改为 `last_action_error_code`（上次动作的错误码），从"是/否"变成"具体错误码"，诊断更精确。
- **新增：** 端头架中心坐标（tip_rack_center），从配置文件读入，供行为树节点计算接近路径使用。

---

## 九、视觉检测结果新增类别信息

视觉模块的检测结果结构体里新增了两个字段：检测到的目标属于哪个类别（编号）、以及对应的类别名称字符串。

**目的：** 之前视觉结果只给出了距离、位置等信息，无法直接知道检测到的是哪种具体目标。加上类别 ID 和名称之后，行为树节点可以更精确地根据目标类型做出不同的决策。

---

## 十、串口协议小改动

- `ACTION_FAIL`（动作执行失败）反馈的注释补充了说明：其数据包内容包含"哪条命令失败了"和"错误码"。
- 新增了命名空间别名，让串口模块内部引用命令ID和反馈ID枚举时写法更简洁。

---

## 十一、删除了测试指令文档

删除了原来放在 `src/` 目录下的一份中文 Markdown 测试说明文档（模块递进测试指令），该文档原本用于记录手动测试步骤，已不再需要维护。

---

## 总结

这一批改动的核心主旨是**架构解耦**：把"决定做什么"（决策）和"怎么操控硬件"（机构控制）彻底分离成两个独立的 ROS2 节点，中间用标准的 Action 接口通信。同时在感知层（地形）和视觉层分别增强了能力，让系统对 KFS 占用情况和端头位置有更准确的认知。
