# R2 机器人集成调试清单

> **调试路线：** 遥控 → 建图 → 测试重定位 → 导航 → 感知 → 决策
>
> **编译约定：** 统一使用 `colcon build --symlink-install --parallel-workers 3 --packages-select <包名> --cmake-args -DCMAKE_BUILD_TYPE=Release` 限制编译核心，完成后执行 `source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"`。
>
> **硬件平台：** Qualcomm QCS8550 · AidLux (Android 13 + Ubuntu 22.04) · 四驱麦克纳姆轮底盘
>
> **当前代码基线（以仓库现实现为准）：**
> 1) 里程计主链路以 `src/rc26_bringup/launch/odometry.launch.py` 为准。
> 2) 当前 TF 基线为 `map → odom → base_link → livox_frame`。
> 3) `rc26_point_lio` 当前仅输出 `/state_estimation`，其自身 TF 发布已关闭。
> 4) `` 当前仅提供参数覆盖与脚本资源，未集成在线 ROS 2 节点启动链路。

---

## 模块一：遥控（rc26_telecontrol）

> **关键说明：**
> 1) 本模块单独调试时，默认验证话题为 `/cmd_vel_teleop`。  
> 2) 若目标是“让车实际动起来”，需额外启动底盘执行链路（`rc26_merge_odom`），并确保控制话题与底盘订阅一致。

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| USB 手柄已连接 | `ls /dev/input/by-id/*joystick` | 存在 `usb-D_TGZ_Controller_3E529640-event-joystick` |
| joy 驱动可用 | `ros2 pkg list \| grep joy` | 输出包含 `joy` |
| 底层串口通信正常 | 参见 rc26_serial 模块 | 串口无报错 |

### 2. 核心调试步骤

**编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_telecontrol --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**启动（Stick 模式）：**
```bash
ros2 launch rc26_telecontrol wheeltec_joy.launch.py control_mode:=stick
```

**启动（Dpad 模式）：**
```bash
ros2 launch rc26_telecontrol wheeltec_joy.launch.py control_mode:=dpad
```

**验证话题与节点：**
```bash
# 确认节点启动
ros2 node list | grep joy

# 监听控制指令输出
ros2 topic echo /cmd_vel_teleop

# 验证参数生效
ros2 param get /rc26_telecontrol v_linear
ros2 param get /rc26_telecontrol joy_timeout_s
```

**Watchdog 保护测试：**
```bash
# 模拟手柄断联
pkill -f joy_node
# 观察 /cmd_vel_teleop 是否在 0.3s 内自动归零
```

**启用安全开关（Deadman）：**
```bash
ros2 launch rc26_telecontrol wheeltec_joy.launch.py require_deadman:=true
```

### 2.1 实车联调（让车实际动起来）

> 在“2. 核心调试步骤”通过后再执行本节；先架空底盘再测。

**联调编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_telecontrol rc26_merge_odom --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**终端 A：启动底盘执行链路（merge_odom）**
```bash
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
ros2 launch rc26_merge_odom merge_odom.launch.py use_can_odom:=false
```

**终端 B：启动手柄驱动（joy_node）**
```bash
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
ros2 run joy joy_node --ros-args -p device_name:="Xbox 360 Controller" -p deadzone:=0.05 -p autorepeat_rate:=50.0
```

**终端 C：启动遥控并将输出改为 /cmd_vel（关键）**
```bash
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
ros2 run rc26_telecontrol rc26_telecontrol --ros-args -p cmd_vel_topic:=cmd_vel
```

**快速验收（话题连通）**
```bash
ros2 topic info /cmd_vel -v
ros2 topic echo /cmd_vel --once
```

预期：`/cmd_vel` 的发布者为 `rc26_telecontrol`，订阅者包含 `merge_odom_node`。

**可选：tmux 一键启动（三窗口）**
```bash
cd "${RC26_WS:-$HOME/RC_2026}"
tmux new-session -d -s r2_teleop
tmux rename-window -t r2_teleop:0 merge_odom
tmux send-keys -t r2_teleop:merge_odom 'source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash" && ros2 launch rc26_merge_odom merge_odom.launch.py use_can_odom:=false' C-m

tmux new-window -t r2_teleop -n joy
tmux send-keys -t r2_teleop:joy 'source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash" && ros2 run joy joy_node --ros-args -p device_name:="Xbox 360 Controller" -p deadzone:=0.05 -p autorepeat_rate:=50.0' C-m

tmux new-window -t r2_teleop -n telecontrol
tmux send-keys -t r2_teleop:telecontrol 'source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash" && ros2 run rc26_telecontrol rc26_telecontrol --ros-args -p cmd_vel_topic:=cmd_vel' C-m

tmux attach -t r2_teleop
```

**停止联调（tmux 会话）**
```bash
tmux kill-session -t r2_teleop
```

### 3. 预期正常结果

- 操作手柄摇杆，`/cmd_vel_teleop` 的 `linear.x / linear.y / angular.z` 随之变化
- 摇杆归中后，输出连续发送 10 帧全零，随后停止刷新
- 拔掉手柄 0.3s 内，终端出现 `[WARN] Joy timeout (X.XXs), holding zero.`
- 轻推摇杆不超过死区（0.15）时输出保持为 0

### 4. 判定通过标准

- [ ] `/cmd_vel_teleop` 话题正常发布，操控响应流畅
- [ ] 死区功能正常，小幅抖动不输出指令
- [ ] 加速度限制生效，速度平滑爬升不突变
- [ ] Watchdog 超时保护正常，0.3s 内发出停车指令
- [ ] （如启用）Deadman 开关：不按 LB 键无速度输出
- [ ] （联调附加）`/cmd_vel` 话题发布/订阅链路连通（telecontrol → merge_odom），车辆可响应遥控

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| `/cmd_vel_teleop` 无输出 | 手柄未识别或未连接 | `ls /dev/input/by-id/*joystick` 确认设备，检查 USB |
| `/cmd_vel_teleop` 有输出但车不动 | 底盘节点订阅的是 `/cmd_vel`，话题未对齐 | 启动 telecontrol 时加 `--ros-args -p cmd_vel_topic:=cmd_vel`，并确认 `merge_odom_node` 在运行 |
| 摇杆归中仍有非零输出 | 死区阈值设置偏小 | 调整 `joy_params.yaml` 中 `deadzone` 参数 |
| Watchdog 不触发 | `joy_timeout_s` 参数过大 | `ros2 param get /rc26_telecontrol joy_timeout_s` 确认值 |
| Dpad 模式无响应 | 手柄按键映射不匹配 | 切换 `joy_params_dpad.yaml`，确认轴/键编号 |

---

## 模块二：建图（rc26_mid360_driver + rc26_point_lio）

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| 雷达网络连通 | `ping 192.168.1.140` | 延迟 <1ms，无丢包 |
| UDP 接收缓冲区大小 | `sysctl net.core.rmem_max` | ≥ `33554432`（32MB） |
| IMU 串口连接 | `ls -l /dev/serial/by-id/*DM-IMU*` | 存在 `usb-DM-IMU_DM-IMU_USB_CDC_2025021200-if00` |

若缓冲区不足：
```bash
sudo sysctl -w net.core.rmem_max=33554432
```

当前 R2 实机默认网络为：本机 `br-lan` / `host_ip` = `192.168.1.50`，Mid-360 = `192.168.1.140`。不要使用 `ping 192.168.1.50` 验证雷达连通性，因为该地址是本机接收口。

### 2. 核心调试步骤

**编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_mid360_driver rc26_point_lio rc26_odom_interface rc26_sensor_scan rc26_lio_state_predictor rc26_bringup --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**步骤一：启动雷达驱动：**
```bash
ros2 launch rc26_mid360_driver mid360_driver.launch.py
```

**步骤二：验证雷达数据：**
```bash
# 确认话题存在
ros2 topic list | grep livox

# 点云频率（期望：10Hz 稳定）
ros2 topic hz /livox/lidar

# IMU 频率（期望：~200Hz）
ros2 topic hz /livox/imu

# 验证点云格式（fields 必须含 x,y,z,intensity,timestamp；point_step=24）
ros2 topic echo /livox/lidar --once

# 时间戳单调性（观察是否单调递增）
ros2 topic echo /livox/lidar --field header.stamp
```

说明：驱动优先基于协议 `frame_cnt` 做事件驱动分帧；若当前固件的 `frame_cnt` 固定不变，则会自动退回到 `src/rc26_mid360_driver/config/param.yaml` 中 `lidar_publish_time_interval` 指定的时间窗口分帧，默认 `0.1s` 对应 10Hz。

**步骤三：启动建图（Point-LIO SLAM 模式）：**
```bash
# 使用建图配置（保存地图）
ros2 launch rc26_bringup bringup.launch.py slam:=true use_rviz:=true use_decision:=false
```

**步骤四：监控 LIO 输出：**
```bash
# 状态估计频率
ros2 topic hz /state_estimation

# 检查里程计子坐标系与速度输出
ros2 topic echo /state_estimation --once --field child_frame_id
ros2 topic echo /state_estimation --once --field twist.twist

# 协方差非零检查
ros2 topic echo /state_estimation --once --field pose.covariance

# 退化分数监控（正常场景 >1.0；走廊等退化场景 <0.1）
ros2 topic echo /degenerate_score
```

**步骤五：odom_interface 链路验证：**
```bash
# odom / odometry 话题输出
ros2 topic echo /odom --once
ros2 topic echo /odometry --once

# TF 唯一性（不应有冲突发布者）
ros2 run tf2_ros tf2_echo odom base_link
ros2 run tf2_ros tf2_monitor odom base_link

# sensor_scan 点云转换
ros2 topic hz /sensor_scan
ros2 topic echo /sensor_scan --once
```

**步骤六： 说明（当前仓库）：**

- `` 当前仅提供参数覆盖文件与离线脚本，`odometry.launch.py` 只引用其 `config/param_overrides.yaml`。
- 当前仓库未提供 `use_:=true`、`//diagnostics`、`//freeze_agent`、`//force_rollback` 对应的在线 ROS 2 节点与 launch 链路。
- 因此建图联调时，不再将  在线监控作为验收项。

### 3. 预期正常结果

- `/livox/lidar` 稳定 10Hz，`/livox/imu` 稳定 ~200Hz
- 点云格式：`fields` 含 `x,y,z,intensity,timestamp`，`point_step=24`
- `/state_estimation` 协方差非全零
- RViz 中点云轨迹平滑，机器人移动时建图无漂移
- `/odom` 的 `frame_id=odom`，`child_frame_id=base_link`
- `/odometry` 持续输出，线速度/角速度与实车动作一致
- `/sensor_scan` 频率与输入点云一致（~10Hz）

### 4. 判定通过标准

- [ ] 雷达 ping 通，无丢包
- [ ] `/livox/lidar` 10Hz 稳定，无半帧抖动
- [ ] `/livox/imu` ~200Hz 稳定
- [ ] `point_step=24`，格式字段正确
- [ ] `/state_estimation` 正常发布，协方差非零
- [ ] TF 树 `odom → base_link → livox_frame` 连通且唯一（`Point-LIO` 自身 TF 已关闭）
- [ ] `/odometry` 正常输出，速度信息合理
- [ ] RViz 建图效果：点云无重影，轨迹连续
- [ ] `/sensor_scan` 时间戳与 `/odom` 对齐

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| `/livox/lidar` 无数据 | 网络不通、`host_ip` 配置冲突或缓冲区不足 | `ping 192.168.1.140`；确认本机 `host_ip=192.168.1.50`；检查 `rmem_max` |
| 点云严重抖动 | 外参错误或时间同步失效 | 检查 `mid360.yaml` 中 `extrinsic_T/R` 和 `time_diff_lidar_to_imu` |
| 轨迹飞出/丢失 | IMU 异常或重力未对齐 | 确认 IMU 安装方向，检查 `gravity` 参数 |
| `/state_estimation` 频率低 | 下采样未关闭 | 确认 `publish_odometry_without_downsample: True` |
| `/sensor_scan` 无输出 | `registered_scan` 与 `/odom` 缺失或时间戳差异过大 | `ros2 topic hz /registered_scan` 与 `ros2 topic hz /odom` 确认上游正常 |
| TF 冲突警告 | 多节点重复发布同一帧变换 | `tf2_monitor` 确认唯一发布者 |

---

## 模块三：测试重定位（rc26_localization）

> **说明：** 重定位需要先有建图结果（.pcd 先验点云），确保 `rc26_bringup/pcd/default.pcd` 或指定路径文件存在。

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| 先验点云文件存在 | `ls ${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/` | 存在 `.pcd` 文件 |
| 雷达驱动 + Point-LIO 正常运行 | `ros2 topic hz /state_estimation` | 稳定输出 |
| `/odom` 话题正常 | `ros2 topic hz /odom` | 稳定输出 |
| TF 树 odom→base_link 存在 | `ros2 run tf2_ros tf2_echo odom base_link` | 持续输出 |

### 2. 核心调试步骤

**编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_localization --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**方式一：通过 bringup 启动（推荐整车联调）：**
```bash
# slam:=false 时自动启动 rc26_localization
ros2 launch rc26_bringup bringup.launch.py slam:=false use_decision:=false \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

**方式二：单独启动定位节点：**
```bash
ros2 launch rc26_bringup localization.launch.py
```

**话题监控：**
```bash
# 查看诊断信息（含协方差特征值、退化状态）
ros2 topic echo /localization/diagnostics

# 查看位姿估计（含协方差）
ros2 topic echo /localization/pose_with_cov
```

**动态参数调节：**
```bash
# 切换优化器模式（初值好用 gn，初值差用 lm）
ros2 param set /localization gicp_optimizer_mode "gn_auto"

# 开启 Huber 鲁棒核（场上动态障碍多时）
ros2 param set /localization robust_enable true
ros2 param set /localization huber_c 1.0

# 开启退化检测
ros2 param set /localization hessian_degen_enable true
ros2 param set /localization hessian_lambda_hard 10.0
```

**Bag 离线调试（推荐）：**
```bash
# 录制数据
ros2 bag record -o loc_test_bag /livox/lidar /livox/imu /odom /tf /tf_static

# 回放（0.5 倍速，便于观察）
ros2 bag play loc_test_bag --rate 0.5
```

**LIO 状态预测器验证：**
```bash
# 控制状态频率（期望：~200Hz）
ros2 topic hz /control_state

# 退化标志
ros2 topic echo /control_degraded
```

### 3. 预期正常结果

- `/localization/diagnostics` 中 `h_min_eig` 在正常场景下较大（>10），走廊退化时明显减小
- `sigma_xy` / `sigma_yaw` 数值合理（不发散）
- `/control_state` 稳定在 ~200Hz
- RViz 中机器人位姿与实际吻合，扫描点云与先验地图贴合

### 4. 判定通过标准

- [ ] 定位节点正常启动，无 Fatal 错误
- [ ] `/localization/pose_with_cov` 持续输出，协方差合理
- [ ] 机器人在场地多处移动后，`pose_with_cov` 无长时间漂移
- [ ] 退化场景（走廊等）下 `hard_degen_consec` 指标正常计数
- [ ] `/control_state` 在 ~200Hz 输出
- [ ] `control_degraded` 在退化场景中正确置 `True`

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| 定位启动后立即飘移 | 先验点云与当前场景不匹配 | 确认 `prior_pcd_file` 路径正确，重新建图 |
| `h_min_eig` 持续为 0 | Hessian 计算未启用 | `ros2 param set /localization cov_from_hessian_enable true` |
| 动态障碍导致定位跳变 | Huber 核未启用 | 设置 `robust_enable:=true`，调低 `huber_c` |
| 定位频繁拒绝更新 | `hessian_lambda_hard` 阈值过高 | 适当降低该参数值（如从 10.0 → 5.0） |
| `/control_state` 频率低 | CPU 负载过高或参数错误 | 检查 `publish_rate_hz` 参数，查看 CPU 占用 |

---

## 模块四：导航（Nav2 + rc26_nav_mode_manager + rc26_omni_controller）

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| 定位正常（map→odom TF） | `ros2 run tf2_ros tf2_echo map odom` | 持续输出 |
| 地图文件存在 | `ls ${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/map/` | 存在 `.yaml` 地图文件 |
| `/odometry` 话题正常 | `ros2 topic hz /odometry` | 稳定输出 |
| 地形模块正常（见模块五） | `ros2 topic hz /terrain_obstacles` | 稳定输出 |

### 2. 核心调试步骤

**编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_omni_controller rc26_nav_mode_manager rc26_bringup --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**启动完整导航栈：**
```bash
ros2 launch rc26_bringup bringup.launch.py slam:=false use_decision:=false use_rviz:=true \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

**单独测试控制器（无完整导航栈）：**
```bash
ros2 launch rc26_bringup test_omni_controller.launch.py
```

**导航模式管理器单独测试：**
```bash
ros2 launch rc26_nav_mode_manager nav_mode_manager.launch.py
```

**监控导航关键话题：**
```bash
# 导航安全状态（1Hz）
ros2 topic echo /nav_safety_state

# 控制器计算耗时（P95 目标 ≤2ms）
ros2 topic echo /compute_time_ms

# 碰撞检测
ros2 topic echo /collision_d_min
ros2 topic echo /v_safe

# 底盘速度指令
ros2 topic echo /cmd_vel
```

**切换导航模式：**
```bash
# 切换至正常模式
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'normal', timeout: 0.0, reason: 'test'}"

# 切换至过中场模式（含 10s Watchdog）
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'mf_traverse', timeout: 10.0, reason: 'test_watchdog'}"

# 切换至爬楼模式（需机器人停稳）
ros2 service call /set_nav_mode rc26_interfaces/srv/SetNavMode "{profile: 'stair_up', timeout: 0.0, reason: 'test_stair'}"
```

**在线调参（控制器）：**
```bash
# 制动余量
ros2 param set /controller_server FollowPath.brake_margin 0.15

# 横向误差增益
ros2 param set /controller_server FollowPath.lateral_error_gain 1.5

# 加速度限幅
ros2 param set /controller_server FollowPath.a_lim_x 1.0
ros2 param set /controller_server FollowPath.a_lim_y 0.6
```

**数据录制（验收用）：**
```bash
ros2 bag record /odometry /tf /tf_static /cmd_vel /compute_time_ms /collision_d_min /v_safe
```

### 3. 预期正常结果

- Nav2 所有节点生命周期状态为 `active`
- 在 RViz 中发布 Nav2 Goal 后，机器人沿规划路径平滑行驶
- `/nav_safety_state` 的 `current_profile` 随模式切换正确变化
- Watchdog 模式超时后自动降级到 `fallback_profile`
- `/compute_time_ms` P95 ≤ 2ms

### 4. 判定通过标准

- [ ] Nav2 lifecycle 全部 active
- [ ] 能够从 RViz 发布目标并成功导航到达
- [ ] `set_nav_mode` 服务调用返回成功
- [ ] Watchdog 超时自动降级（`timed_out=true`）
- [ ] `stair_up` 模式在机器人未停稳时被正确拒绝
- [ ] `/compute_time_ms` P95 ≤ 2ms
- [ ] 无明显过冲、振荡，轨迹贴合度良好

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| Nav2 节点处于 `unconfigured` | lifecycle 自启失败 | `ros2 lifecycle get /controller_server` 查看状态 |
| 模式切换被拒 | 机器人未停稳 | 检查 `/odom` 速度，确认线速度/角速度低于 `stop_linear_eps_mps` |
| `set_nav_mode` 返回 "Profile not found" | 模式名错误 | 参考 `config/nav_profiles.yaml` 中的有效模式名 |
| 控制器插件未加载 | `nav2_params.yaml` 中插件名不匹配 | 确认插件名为 `rc26_omni_controller::OmniPidPursuitController` |
| 参数回读超时 | `terrain_semantic` 或 `controller_server` 未启动 | 检查节点名是否与 `nav_mode_manager.yaml` 配置一致 |
| 轨迹跟踪振荡 | `lateral_error_gain` 过大 | 从小值重新标定，建议 0.5→1.0→1.5 递进 |

---

## 模块五：感知（rc26_sensor_scan + rc26_terrain + rc26_vision）

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| 雷达点云正常 | `ros2 topic hz /livox/lidar` | 10Hz 稳定 |
| TF: base_link → livox_frame | `ros2 run tf2_ros tf2_echo base_link livox_frame` | 持续输出 |
| RealSense（如启用） | `ros2 topic hz /camera/color/image_raw` | 正常频率 |
| YOLO 模型文件存在 | 检查 `vision_models.yaml` 中 `model_path` | 文件路径有效（默认走 AidLux 本地 ONNX/CPU） |

### 2. 核心调试步骤

#### 2.1 点云处理（sensor_scan）

**编译并验证：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_sensor_scan rc26_bringup --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"

# 通过里程计链路启动（已集成 sensor_scan）
ros2 launch rc26_bringup odometry.launch.py

# 验证转换后点云
ros2 topic hz /sensor_scan
ros2 topic echo /sensor_scan --once
# frame_id 应为雷达本体坐标系（如 livox_frame）

# 验证时间戳同步
ros2 topic echo /sensor_scan --once --field header.stamp
ros2 topic echo /odometry --once --field header.stamp
```

#### 2.2 地形感知（rc26_terrain）

**编译并启动：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_interfaces rc26_terrain rc26_merge_odom --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"

ros2 launch rc26_terrain terrain_semantic.launch.py
```

**验证输出：**
```bash
# 节点状态与参数
ros2 node info /terrain_semantic
ros2 param dump /terrain_semantic

# 障碍物点云（关键输出，PointCloud2）
ros2 topic hz /terrain_obstacles
ros2 topic echo /terrain_obstacles --once

# 跌落风险点云
ros2 topic hz /terrain_drop
ros2 topic echo /terrain_drop --once

# 速度限制（平坦地面应接近最大速度）
ros2 topic hz /terrain_speed_limit
ros2 topic echo /terrain_speed_limit
```

**动态调参：**
```bash
# 地面高度平滑系数（0.0~1.0）
ros2 param set /terrain_semantic ground_ema_alpha 0.5

# 真障碍高度阈值（对应参数 h_obstacle_m）
ros2 param set /terrain_semantic h_obstacle_m 0.35

# 跌落风险阈值（对应参数 h_drop_m）
ros2 param set /terrain_semantic h_drop_m 0.18
```

**限速闭环验证：**
```bash
# 启动 merge_odom
ros2 launch rc26_merge_odom merge_odom.launch.py

# 模拟极低限速
ros2 topic pub -r 10 /terrain_speed_limit std_msgs/msg/Float32 "{data: 0.2}"

# 停止发布后等待 500ms，验证是否恢复默认最大速度
```

**RViz 可视化：**
```bash
rviz2
# 添加显示项：
# - PointCloud2: /livox/lidar（原始点云）
# - PointCloud2: /terrain_obstacles（障碍物点云）
# - PointCloud2: /terrain_drop（跌落风险点云）
# Fixed Frame: odom
```

#### 2.3 视觉感知（rc26_vision）

**启动（含 RealSense）：**
```bash
ros2 launch rc26_vision vision_test_with_camera.launch.py
# 默认使用 rc26_vision 安装目录下的 vision_models.yaml，模型链路为 AidLite + ONNX/CPU

# 或通过 bringup 启动
ros2 launch rc26_bringup bringup.launch.py use_realsense:=true use_decision:=false slam:=false
```

**验证视觉话题：**
```bash
# 相机图像
ros2 topic hz /camera/color/image_raw

# 检测结果（默认输出话题）
ros2 topic hz /vision/tip_detections
ros2 topic echo /vision/tip_detections --once
```

### 3. 预期正常结果

- `/sensor_scan` 频率 ~10Hz，`frame_id` 为雷达本体坐标系，时间戳与 `/odom` 对齐
- `/terrain_obstacles` 和 `/terrain_drop` 正常发布，平坦无障碍区域点云应为空或仅有极少噪点
- `/terrain_speed_limit` 平坦地面接近配置最大速度，遇障碍物时下降
- 超时 500ms 后限速保护触发（若无新数据则恢复默认）
- 视觉检测结果在相机视野内正确标注目标

### 4. 判定通过标准

- [ ] `/sensor_scan` 正常 10Hz，时间戳对齐
- [ ] `/terrain_obstacles` 正常发布，障碍物位置符合实际
- [ ] `/terrain_drop` 悬崖/坑洼检测不误报（平坦地面应无检测结果）
- [ ] `/terrain_speed_limit` 随地形变化正确增减
- [ ] 限速超时保护机制正常（500ms 无数据后恢复）
- [ ] （如启用视觉）检测结果正常发布，目标类别与置信度合理

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| `/sensor_scan` 无输出 | 输入点云 `registered_scan` 或 `/odom` 不存在 | `ros2 topic hz /registered_scan` 与 `ros2 topic hz /odom` 确认上游 |
| sensor_scan 时间戳不对齐 | ApproxTime 同步窗口过小 | 检查两路输入时间戳差异是否超限 |
| TF `base_link→livox_frame` 缺失 | 静态 TF 未发布 | `ros2 run tf2_ros tf2_echo base_link livox_frame` 排查 |
| 平坦地面误报大量障碍物 | `h_obstacle_m` 过低 | `ros2 param set /terrain_semantic h_obstacle_m 0.35` 后复测 |
| `/terrain_speed_limit` 持续为 0 | 跌落风险误报 | 调高 `h_drop_m`，并增大 `min_drop_area_cells` |
| 视觉检测无结果 | 模型路径错误或置信度过高 | 检查 `vision_models.yaml` 中 `model_path` 和 `conf_thresh` |

---

## 模块六：决策（rc26_decision）

### 1. 前置条件检查

| 检查项 | 命令 | 预期 |
|--------|------|------|
| Nav2 全部 active | `ros2 lifecycle get /controller_server` | `active` |
| 定位正常（map→odom） | `ros2 run tf2_ros tf2_echo map odom` | 持续输出 |
| KFS keepout 心跳（如启用） | `ros2 topic hz /kfs_keepout_heartbeat` | 稳定输出 |
| `/mechanism/state` 正常 | `ros2 topic hz /mechanism/state` | 正常发布 |
| 路点文件存在 | `ls ${RC26_WS:-$HOME/RC_2026}/src/rc26_decision/config/waypoints/` | `waypoints_red.yaml` / `waypoints_blue.yaml` |

### 2. 核心调试步骤

**编译：**
```bash
colcon build --symlink-install --parallel-workers 3 --packages-select rc26_decision rc26_kfs_keepout --cmake-args -DCMAKE_BUILD_TYPE=Release
source "${RC26_WS:-$HOME/RC_2026}/install/setup.bash"
```

**单独启动决策节点（调试期不启用视觉）：**
```bash
ros2 launch rc26_decision decision.launch.py enable_vision:=false
```

**通过完整 bringup 启动：**
```bash
ros2 launch rc26_bringup bringup.launch.py slam:=false use_decision:=true \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd
```

**启动带视觉的决策（比赛模式）：**
```bash
ros2 launch rc26_decision decision.launch.py enable_vision:=true
# 如需覆盖默认配置，再显式传入：
#   vision_config_file:=$(ros2 pkg prefix rc26_vision)/share/rc26_vision/config/vision_models.yaml
```

**监控决策状态：**
```bash
# 确认决策节点在线
ros2 node list | grep rc26_decision

# 查看决策节点发布的话题
ros2 node info /rc26_decision

# 监控导航目标发送
ros2 topic echo /navigate_to_pose/_action/status
```

**KFS Keepout 验证：**
```bash
# 确认 kfs_block_fuser 正常
ros2 node list | grep kfs

# 查看禁入区掩码
ros2 topic echo /kfs_filter_mask --once

# 心跳（keepout_gate.mode: heartbeat 时需要）
ros2 topic hz /kfs_keepout_heartbeat
```

**行为树检查（需要调试日志）：**
```bash
# 查看行为树执行日志
ros2 launch rc26_decision decision.launch.py --ros-args --log-level rc26_decision:=debug
```

**速度档位验证：**
```bash
# 当前 controller_server 速度参数
ros2 param get /controller_server FollowPath.v_linear_max
# 决策切换速度档位后（FAST/SLOW/CREEP），该参数应随之变化
# FAST: 1.0 倍; SLOW: 0.4 倍; CREEP: 0.2 倍
```

### 3. 预期正常结果

- 决策节点以 100ms 为周期（tick_rate_ms=100）驱动行为树
- 行为树按照场景逻辑（main_tree.xml）自动发送导航目标
- 速度档位随任务阶段正确切换（FAST/SLOW/CREEP）
- KFS keepout 在禁区出现障碍物时正确更新掩码

### 4. 判定通过标准

- [ ] 决策节点启动无 Fatal 错误
- [ ] 行为树按预期节奏（100ms tick）执行
- [ ] 机器人能够按照路点文件自动执行任务序列
- [ ] 速度档位切换正确（FAST/SLOW/CREEP 对应倍率正确）
- [ ] KFS keepout 正确过滤禁入区（导航路径不经过禁区）
- [ ] `keepout_gate` 超时保护生效（超时后决策进入等待/降级状态）

### 5. 常见异常及排查

| 现象 | 原因 | 建议 |
|------|------|------|
| 决策节点启动即崩溃 | 行为树 XML 路径错误或语法错误 | 检查 `decision_params.yaml` 中 `tree_file` 路径，确认 XML 合法 |
| 机器人不动 | Nav2 未 active 或目标坐标无效 | `ros2 lifecycle get /controller_server`，检查路点坐标系 |
| KFS keepout 心跳超时 | `/kfs_keepout_heartbeat` 未发布 | 确认 `keepout_gate.mode` 与上游发布节点一致 |
| 速度未按档位切换 | `controller_server` 参数未被决策更新 | `ros2 param get /controller_server FollowPath.v_linear_max` 确认实际值 |
| 路点文件加载失败 | YAML 格式错误或路径不存在 | 检查 `waypoints_red.yaml` / `waypoints_blue.yaml` 文件格式 |
| 禁入区不生效 | `kfs_filter_mask` 未传入 Nav2 costmap | 检查 `costmap_filter_info_server` 是否正常发布 `/costmap_filter_info` |

---

## 附录 A：TF 树总览

```
map
 └── odom              ← rc26_localization (导航模式) / 静态 TF (建图模式)
      └── base_link    ← rc26_odom_interface 唯一发布
           └── livox_frame  ← 静态 TF（外参标定）
```

**快速验证 TF 连通性：**
```bash
ros2 run tf2_tools view_frames
# 生成 frames.pdf，确认完整链路连通
```

> 当前基线说明：`odom → base_link` 由 `rc26_odom_interface` 唯一发布，`base_link → livox_frame` 为静态 TF，`rc26_point_lio` 自身 TF 已关闭。

---

## 附录 B：整车快速启动命令

```bash
# 建图模式
ros2 launch rc26_bringup bringup.launch.py slam:=true use_rviz:=true use_decision:=false

# 导航模式（完整）
ros2 launch rc26_bringup bringup.launch.py slam:=false use_rviz:=true use_decision:=true \
  prior_pcd_file:=${RC26_WS:-$HOME/RC_2026}/src/rc26_bringup/pcd/default.pcd

# 遥控测试（独立）
ros2 launch rc26_telecontrol wheeltec_joy.launch.py control_mode:=stick

# 里程计链路（独立）
ros2 launch rc26_bringup odometry.launch.py
```

---

## 附录 C：高频诊断命令速查

| 目标 | 命令 |
|------|------|
| 查看所有活跃话题 | `ros2 topic list` |
| 查看所有节点 | `ros2 node list` |
| 查看 TF 树 | `ros2 run tf2_tools view_frames` |
| 检查某话题频率 | `ros2 topic hz <topic>` |
| 检查节点订阅/发布 | `ros2 node info <node>` |
| 查看系统诊断 | `ros2 topic echo /diagnostics` |
| 查看定位诊断 | `ros2 topic echo /localization/diagnostics` |
| 查看导航安全状态 | `ros2 topic echo /nav_safety_state` |
| 查看地形限速 | `ros2 topic echo /terrain_speed_limit` |
