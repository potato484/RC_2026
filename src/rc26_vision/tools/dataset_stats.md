# 数据集 RGB 统计报告

> 生成时间: 2026-01-29 19:21:54

---

## 1. 数据集信息

| 项目 | 值 |
|------|-----|
| 数据集路径 | `${RC26_WS}/train` |
| 图片总数 | 28,617 张 |
| 像素总数 | 12,038,835,200 个 |
| 读取失败 | 0 张 |

---

## 2. 统计结果

### 2.1 归一化格式 (像素值范围 [0, 1])

> **适用场景**: PyTorch / TensorFlow 标准归一化流程
>
> **使用方式**: `normalized = (pixel / 255.0 - mean) / std`

| 通道 | 均值 (Mean) | 标准差 (Std) | 方差 (Var) |
|------|-------------|--------------|------------|
| R (红) | 0.391792 | 0.314641 | 0.098999 |
| G (绿) | 0.361597 | 0.304108 | 0.092482 |
| B (蓝) | 0.366848 | 0.294243 | 0.086579 |

**数组格式 (方便复制)**:
```python
mean_rgb = [0.391792, 0.361597, 0.366848]
std_rgb  = [0.314641, 0.304108, 0.294243]
var_rgb  = [0.098999, 0.092482, 0.086579]

# 如果平台选择 BGR 顺序
mean_bgr = [0.366848, 0.361597, 0.391792]
std_bgr  = [0.294243, 0.304108, 0.314641]
var_bgr  = [0.086579, 0.092482, 0.098999]
```

---

### 2.2 原始像素格式 (像素值范围 [0, 255])

> **适用场景**: 部分量化平台直接使用的参数格式
>
> **使用方式**: `normalized = (pixel - mean) / std`

| 通道 | 均值 (Mean) | 标准差 (Std) | 方差 (Var) |
|------|-------------|--------------|------------|
| R (红) | 99.9070 | 80.2336 | 6437.4252 |
| G (绿) | 92.2072 | 77.5475 | 6013.6183 |
| B (蓝) | 93.5462 | 75.0319 | 5629.7865 |

**数组格式 (方便复制)**:
```python
mean_255 = [99.9070, 92.2072, 93.5462]
std_255  = [80.2336, 77.5475, 75.0319]
var_255  = [6437.4252, 6013.6183, 5629.7865]

# 如果平台选择 BGR 顺序
mean_255_bgr = [93.5462, 92.2072, 99.9070]
std_255_bgr  = [75.0319, 77.5475, 80.2336]
var_255_bgr  = [5629.7865, 6013.6183, 6437.4252]
```

---

## 3. 平台 UI 直接填写参考

许多量化平台的预处理逻辑是类似下面的形式 (每通道):

```
out = (in - mean) / variance
```

其中 UI 的“方差(variance)”经常实际上是 **Std/Scale(除数)**，不一定是真正的 Var。

### 3.1 如果你的模型仅做 `/255` 归一化 (常见于 YOLO)

> **对应**: `out = in / 255`

- 色彩模式: **RGB**
- 均值(mean): `[0, 0, 0]`
- 方差/Scale(除数): `[255, 255, 255]`

### 3.2 如果你的模型使用 `mean/std` 归一化

- 若平台输入 `in` 是 `uint8` `[0,255]`:
  - 均值(mean) 填 `mean_255`
  - 方差(Std/Scale) 填 `std_255`
  - 若平台真的要求 Var，填 `var_255`
- 若平台输入 `in` 已经是 `[0,1]`:
  - 均值(mean) 填 `mean_rgb`
  - 方差(Std/Scale) 填 `std_rgb`
  - 若平台真的要求 Var，填 `var_rgb`

---

## 4. ONNX 量化配置参考

以下为常见量化平台的配置格式:

### 4.1 通用配置
```yaml
preprocess:
  mean: [0.391792, 0.361597, 0.366848]
  std: [0.314641, 0.304108, 0.294243]
  input_range: [0, 1]
```

### 4.2 OpenVINO 配置
```
mean_values: [99.91, 92.21, 93.55]
scale_values: [80.23, 77.55, 75.03]
```

### 4.3 TensorRT 配置
```python
config.set_flag(trt.BuilderFlag.FP16)
# 预处理: (input - mean) / std
mean = np.array([0.391792, 0.361597, 0.366848])
std = np.array([0.314641, 0.304108, 0.294243])
```

---

*本报告由 calc_dataset_stats.py 自动生成*
