# rc26_xhu_viewer Tombstone

This directory is intentionally retained only as a retirement marker.

Current status:

- Real RC26 GUI and Web sources moved to `../rviz2/`
- Headless `r2/xhu_viewer/*` status ownership remains in `../rc26_xhu_viewer_status/`
- This directory must not regain build targets, launch files, or source-of-truth assets

Only `COLCON_IGNORE` and this README should remain here.
