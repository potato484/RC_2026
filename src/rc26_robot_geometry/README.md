# rc26_robot_geometry

Shared geometry profiles for the R2 robot body.

This package is intentionally config-only in the current phase. It provides a
single installable YAML truth source that other runtime packages consume through
parameter contracts instead of copying geometry constants into each package.
