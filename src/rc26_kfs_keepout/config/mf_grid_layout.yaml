# Backward-compatible shim.
# Legacy modules may still pass this path as "mf_grid_layout_file" / "grid_layout_file".
# The canonical source of truth is r2_mf_world.yaml in the same folder.
world_layout_file: "r2_mf_world.yaml"
